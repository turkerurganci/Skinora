---
name: project_phase_history
description: Skinora faz gecmisi — MVP/F6/F7 gate sonuclari, backlog kapatma turlari ve o turlarin kalici dersleri
type: project
---

MEMORY.md okuma sınırına yaklaştığı için 2026-09-01'de buraya taşınan faz geçmişi. **Güncel görev durumu için her zaman `Docs/IMPLEMENTATION_STATUS.md` okunur** ([[feedback_verify_status_before_quoting]]); bu dosya yalnızca tarihsel bağlamdır ve her madde **yazıldığı tarihte** doğruydu — sayılar ve "açık iş" listeleri o günün ölçümüdür.

- **MVP KAPANDI (2026-07-26).** Gate Check'ler F0–F6 hepsi ✓ PASS. **F6 ✓ PASS** (Uçtan Uca Doğrulama, T107–T114; tag `phase/F6-pass`, main `8dcfcdf`, rapor `Docs/CHECKPOINT_REPORTS/GATE_CHECK_F6.md`). T01–T114 + F6-öncesi borç kapatma WP1–WP20 tamam. Gate kanıtı: backend 2570/2570 test · FE eslint 0 / i18n 1291×4 / vitest 28/28 · sidecar 158+161+12 · 8 E2E suite 8-leg CI matrix ✓ · 31 migration rehearsal temiz · traceability 8/8 · 0 bloke-edici bulgu.

- **Sonrası (main):** `02bc082` (#209) F6-sonrası backlog hijyeni + DEPLOY_RUNBOOK §C.1 PRICE_DEVIATION config adımı · `e2a0cd9` (#210) FE timeline terminal durum fix (COMPLETED yeşil, REFUNDED terminal).

- **F7 — P2P GEÇİŞİ KAPANDI (2026-08-08 → 2026-08-22). Gate Check ✓ PASS.** MVP kapandıktan sonra Steam Trade Protection nedeniyle açılan faz (T115–T140 + T119a/T133a/T133b/T137a = **30 görev**, hepsi ✓ Tamamlandı): item custody kaldırıldı, teslimat doğrulaması + P2P akış geldi. **F7 Gate Check ✓ PASS (2026-08-22)** — ölçüm commit'i `10833fd`, rapor `Docs/CHECKPOINT_REPORTS/GATE_CHECK_F7.md`, tag `phase/F7-pass`, chore PR [#257](https://github.com/turkerurganci/Skinora/pull/257). **Gate kanıtı:** backend **2848/2848** (unit 1470 + integration 1369 + contract 9) · frontend eslint 0 / i18n 4×1285 / vitest 152/152 / build ✓ 34 route · sidecar 83+166+38 · 10 E2E suite main CI'da 10/10 leg success · Release build 0W/0E · migration provası 40 migration temiz + idempotent + seed 63/1/1 · 4 Docker image CI'da build+publish ✓ · güvenlik 0 açık bulgu + **yeni dış bağımlılık YOK** (saldırı yüzeyi küçüldü). **Bloke edici 0; bloke etmeyen 7** — F7-N1/N2/N3/N4 gate chore PR'ında düzeltildi, F7-N5/N6 backlog'a forward edildi (**56 aktif / 58 çözülmüş** — *o tarihteki ölçüm; güncel sayı için aşağıdaki "Açık iş kolları" satırına bak* (2026-08-23 merge sonrası ölçüm — #260/#261/#262 dört kalem kapattı; gate turunda dosyanın kendi komutuyla *67/65* diye raporlanmıştı; o komut `Öne Çıkanlar` kopyalarını da sayarak **satır** ölçüyordu — 2026-08-22 hijyen turu benzersiz **öğe** sayısını ölçtü ve komutu düzeltti), 🔴 yok), F7-N7 kayıtlı sapma. **Sıradaki adım proje sahibinin kararı** — yeni faz tanımlı değil; açık iş kolları: post-MVP backlog, deploy hazırlığı (`Docs/DEPLOY_RUNBOOK.md`), ve **ortamın F6→F7 yükseltmesi** (§G.0a).

- **F7 Gate Check'in üç kalıcı dersi:** (1) **Kapsam varsayılmadı, ölçüldü** — yerel test sayıları CI job loglarıyla **assembly bazında birebir** karşılaştırıldı; iki assembly'nin unit filtresine düşmemesi "eksik" sanılmadı, `Unit/` klasörlerinin fiilen boş olduğu gösterildi. (2) **"Healthy" bir stack, o stack'in NE OLDUĞUNU ölçmeden kanıt değildir** — lokal 11 container healthy ve beş sağlık ucu 200 dönüyordu ama image'ler **2026-07-26** tarihliydi ve DB'de **31/40** migration vardı; sinyal doğru, **öznesi** yanlıştı (F7-N4 buradan doğdu, `DEPLOY_RUNBOOK §G.0a` yazıldı). (3) **Faz düzeyi çapraz eşleme, görev düzeyi doğrulamanın göremediğini görür** — `11 §6.2`'ye yazılan FK1–FK4 (geçiş üreticileri · custody yüzeyi · bildirim kataloğu eşlemesi · event yayıncı/tüketici eşlemesi) iki taban borcunu ilk kez görünür yaptı; ikisi de `T133b-PaymentMonitorUnarmed` / `T138-DeliveryExpectedNeverPublished`→T140 ile **aynı kusur ailesinden** ve tek tek görev diff'lerine bakan doğrulamalarca dört tur boyunca kaçırılmıştı. **Disiplin:** bulgular şişirilmedi — F7-N5/N6 `phase/F6-pass` ağacında birebir aynı ölçüldü (S2 değil, taban borcu) ve F7-N6 için refute turu koşularak "para yolu deliği" tezi **çürütüldü**.

- **Prova hazırlık turu ✓ KAPANDI — 2026-08-28 → 2026-08-29 (en güncel tur; aşağıdaki "Açık iş kolları" satırı bu tarihten ESKİ, sayıları buradan al).** On bir PR (#297–#307), ikisi kod düzeltmesi. Backlog **12 → 14 aktif / 118 çözülmüş** (dosyanın kendi `awk`'iyle; sayı **arttı** çünkü tur yeni kusur buldu), 🔴 yok. Hedef **31 Ağustos'taki canlı P2P provası** ve prova o tarihe kadar **bloke** — ikinci gerçek Steam hesabının mobil doğrulayıcısı (MA) yedi günlük bekleme süresini doldurmuyor. **Turun dersi: hiçbir bulgu kod okunarak çıkmadı** — üçü ürün kullanılırken, biri `docker logs` okunurken. **Baskın üç bulgu:** (1) `AmountValidationService` mainnet USDT kontratını sabit tutuyordu, sidecar ise env'den okuyor → testnet'te **doğru ödeme `wrong_token` sayılıp otomatik iadeye gidiyordu**, üstelik sessizce (#306; aynı PR'da ödeme süresi dakika→saat çevriminde sıfıra kırpılıyordu — ilan açma yolu varsayılan ayarlarla **kapalıydı** — ve Tronscan linkleri her ağda mainnet'e gidiyordu). (2) **Yedi Grafana alarm kuralının yedisi de ölüydü**: `rules.yml` `datasourceUid: prometheus` diyor, datasource dosyası `uid` taşımıyordu; arayüzde yapılandırılmış **görünüyor**, panel çiziyor, log "Sending alerts" yazıyor, ama kural hiç ateşleyemiyor. Düzeltme tek satır; `T139-ActiveMonitorQuotaAlarm`'ın ön koşuluydu (#307). (3) Üç ekran kodun uygulamadığı kuralı anlatıyordu (#305) — yaş kapısında **tek metin iki çağırana** hizmet ediyordu. **Ayrıca dört backlog alt maddesinin iddiasının TERSİ çıktı** ve `auth.min_steam_account_age_days` 30 → 1 geri alması runbook'a girdi (unutulursa **yaş kapısı üretimde fiilen açık kalır**).

- **Açık iş kolları (2026-08-25 itibarıyla — ⚠️ bu satır bayat, yukarıdaki 2026-09-01 satırı iki iddiasını düzeltiyor).** Yeni faz **tanımlı değil**; sıralama proje sahibi kararına bağlı. (1) **`Docs/DEFERRED_BACKLOG.md` — 12 aktif / 115 çözülmüş, 🔴 YOK** (2026-08-28 yeniden ölçüldü, dosyanın kendi `awk` komutuyla; **backlog kapatma turu KAPANDI**, WP9 dört kalem daha kapattı — aşağıdaki WP satırına bak). Kalan 12'nin dağılımı: §5 üç ⚪ frontend cilası (`signalr-toast-countdown` · `fe-next-image-migration` · `dispute-detail-polish`) · §7 `tronweb-6-major-bump` ⚪ · §9 sekiz 🟡 P2P kalemi (`SettlementTiering` · `HotWalletPolicyReview` · `DeliveryPollingJob` · `FloatVerification` · `InventoryUnauthorizedMapping` · `NonDeliveryAbuseWindow` · `T133b-LiveRehearsalUnrun` · `T139-ActiveMonitorQuotaAlarm`) — §9'un çoğu **owner kararıyla ertelendi**, yeni faz işi. WP9'da kapananlar: `StubPayoutVerifier` · `F7Gate-EventsWithoutConsumer` · `AdminUsersDirectoryPermissionMismatch` · `Doc12PreP2PStale`. (2) Deploy hazırlığı — `Docs/DEPLOY_RUNBOOK.md`. (3) **Ortamın F6→F7 yükseltmesi** — `DEPLOY_RUNBOOK §G.0a` (8 adım); **lokal stack 2026-08-23'te F7'ye YÜKSELTİLDİ** (§G.0a 8/8; 41 migration · 63 ayar · 4 image F7 kodundan · gerçek Steam girişi ✓; kanıt rota diferansiyeli, "healthy" değil). Operatör ortamları için adım hâlâ geçerli; lokalde kalan: `.env`'de 8 dış-değer anahtarı boş → **ödeme bacağı** kapalı, `hot_wallet_address` kurulmadı. (4) **Hâlâ açık olan asıl ders: gerçek oturumlu E2E legi** — UI turundan devrediyor, iki gerçek Steam hesabı DB'de duruyor (`turkerurganci_2` / `76561198652999063` owner kararıyla bırakıldı), iki taraflı akışlar artık lokalde iki gerçek oturumla test edilebilir. **UI turunun kalıcı deseni:** o turdaki bulguların hiçbiri kod taranarak bulunmadı; hepsi **ürün kullanılmaya çalışılırken** çıktı. **Lokal stack notu (2026-08-28 ölçüldü — önceki "frontend image geride" satırı ARTIK GEÇERSİZ):** 11/11 container healthy ve dört uygulama image'ı `main` `50f704d` ile eşit. Kanıt cache diferansiyeli + içerik: `docker compose -f docker-compose.yml build` dördünde de **%100 CACHED** döndü ve image ID'leri değişmedi (COPY katmanının cache anahtarı kaynak içeriğinin hash'idir → kaynak birebir aynı); ayrıca WP9'un string'leri çalışan image'ların içinde bulundu (backend `PAYOUT_ISSUE_RESOLVED` + `PayoutIssueResolvedNotificationConsumer`; frontend `.next` bundle'ında VIEW_USERS+MANAGE_ROLES dizisi). DB 41/41 migration · 63 ayar · 0 null. Değişmeyen tek eksik: `.env'`deki 8 dış-değer anahtarı hâlâ boş ([[reference_local_stack_runbook_g]]).

- **Backlog kapatma turu (WP1–WP8) ✓ KAPANDI — 2026-08-25.** On bir PR (#284–#294), dokuz çalışma paketi + bir ölçüm turu; **57 aktif → 16**. WP1 §4 otomatik outage freeze (`PlatformHealthProbeJob` → `FreezeManyAsync`/`ResumeManyAsync`) · WP2a/b/c §5+§10 (sihirbaz taslağı `sessionStorage`, `/auth/me` `permissions[]` + `AdminSidebar` yetki kapısı — backend'de ek sorgu yok, yetkiler zaten JWT claim'iydi) · WP3 §6 doküman borcu · WP4 §7 **var olan ama hiç koşmayan bekçiler koşturuldu** (FE↔C# parity testleri drift'in yaratıldığı backend PR'ında koşmuyordu) · WP5 §8 sidecar `/health`'leri sabit `healthy` döndürüyordu, WP1 bunu acil hâle getirmişti · WP6a/b + WP7 §9 (timeline `EMERGENCY_HOLD` kaplaması, `payment`/`dispute` blokları dolduruldu, yetim bildirimlerin **üçü** bağlandı — `PAYMENT_INCORRECT` bilerek bağlanmadı, çift bildirim olurdu) · WP8 `T135` flake satırı **kodla değil ölçümle** kapandı (CI eşdeğeri kuruldu, integration filtresi 3 kez koştu: 3/3 temiz, 10/10 assembly, 1395 test, 0 hata). **Turun dersi:** kapatılan satırların büyük kısmı iş değil **doğrulama** gerektirdi — üçü iddiasının TERSİ çıktı, ve ölçüm üç kez kendi işini yakaladı. **Kapanan öne çıkan açıklar:** `T50-OutageFreezeCallers` · `T81-PriceConsumerWireup` · `F7Gate-OrphanNotificationTypes` (hepsi ✅); `StubPayoutVerifier` ve `F7Gate-EventsWithoutConsumer` (daraltıldı) hâlâ açık.

- **Kayıt borcu — 2026-08-25'te ölçüldü ve kapatıldı.** Turun on bir PR'ı `Docs/IMPLEMENTATION_STATUS.md`'ye veya repo `.claude/memory/MEMORY.md`'ye **hiç dokunmamıştı**; tracker satır-3'te `#282`'de, backlog sayısı `57/69`'da kalmıştı. Aynı turda **memory deposu kayması** ölçüldü: 15 kayıt yalnız bu auto-memory deposunda duruyordu, repo `.claude/memory/`'de yoktu — `CLAUDE.md` repo'yu tek doğru kaynak sayarken repo'yu klonlayan / cloud session o 15 kaydı hiç görmüyordu (aralarında [[feedback_ask_questions_via_modal]] ve [[feedback_be_concise]] de vardı). İki depo senkronlandı → **37/37 dosya gövde birebir**, iki taraflı indeks tam, 0 kırık `[[wikilink]]`. **Kalıcı ders:** elle tutulan sayaç ve elle yazılan kayıt her PR'da yeniden bozulur — kusur ailesi bu kez tracker'ın kendisinde tekrarladı ([[feedback_verify_status_before_quoting]], [[feedback_verify_metric_definition]]).

Ilgili: [[reference_local_payment_leg_nile]] · [[reference_local_stack_runbook_g]] · [[feedback_verify_metric_definition]]
