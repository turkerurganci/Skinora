/**
 * HD Wallet address generator stub — will be implemented in T70.
 * Generates unique payment addresses per transaction using BIP-44 derivation (08 §3.2).
 */
export class AddressGenerator {
  // T70: generateAddress(transactionId: string): Promise<{ address: string; index: number }>
}
