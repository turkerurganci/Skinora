/**
 * Outgoing TRC-20 transfer stub — will be implemented in T73.
 * Handles payout to seller and sweep from deposit addresses (08 §3.3).
 */
export class TransferService {
  // T73: payout(toAddress, amount, token, correlationId): Promise<string>
  // T73: sweep(fromAddress, toHotWallet, token, correlationId): Promise<string>
}
