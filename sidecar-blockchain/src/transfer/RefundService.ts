/**
 * Refund transfer stub — will be implemented in T73.
 * Handles underpayment refund, overpayment difference refund,
 * and wrong-token refund to source address (08 §3.4).
 */
export class RefundService {
  // T73: refund(fromDepositAddress, toSourceAddress, amount, token, correlationId): Promise<string>
}
