/**
 * Active payment monitoring stub — will be implemented in T71.
 * Polls TronGrid trc20 endpoint every 3s for incoming payments (08 §3.4).
 * Two-phase: expected token query + wrong token scan.
 */
export class TransactionMonitor {
  // T71: startMonitoring(address, expectedToken, amount, transactionId): void
  // T71: stopMonitoring(address): void
}
