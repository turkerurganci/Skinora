# Ertelenmiş İşler Backlog'u (Deferred Work Backlog)

> **Amaç:** Proje boyunca bilinçli olarak ertelenen / sonraya bırakılan tüm somut işlerin tek izlenebilir listesi. Tamamlanan işler için tek doğru kaynak [`IMPLEMENTATION_STATUS.md`](IMPLEMENTATION_STATUS.md); bu dosya yalnızca **ertelenen** kalemleri toplar.
>
> **Oluşturulma:** 2026-06-13 · iki-turlu çok-ajanlı kaynak taraması (status doc + 115 task report + repo/auto memory + backend/frontend kod + sidecar + discovery docs + gate-check/audit/GPT-review raporları). Her kalem kod veya rapor kanıtıyla doğrulandı.
>
> **Durum (2026-08-24, backlog kapatma turu — WP2a §5+§10):** **48 aktif / 78 çözülmüş** (ölçüm komutu aşağıda). **🔴 YOK.**
>
> **WP2a'nın bulgusu satırların içeriğinde değil, listenin kendisinde: §5'in yarısı zaten kapalıymış.** Fix yazmadan önce her satır ölçüldü ve **altısı bayat** çıktı — hepsi WP13'te ([#186](https://github.com/turkerurganci/Skinora/pull/186)) veya sonraki turlarda kapanmış, satırları hiç çevrilmemişti (`T97-formatAmount-deprecated-alias` · `T97-NEXT_LOCALE-cookie` · `FE-enums-ts-lag` · `static-routes-pages` · `admin-table-sort` · `url-state-sync`). Bir tanesinde ölçüm iddianın **tersini** gösterdi: `admin-table-sort` "API var, UI göndermiyor" diyordu; oysa `SortBy` taşıyan üç backend yüzeyinden ikisinin UI'ı zaten gönderiyor, üçüncüsünün admin sayfası hiç yok.
>
> **Bu, dosyanın kendi başlığında iki kez uyardığı kusur ailesinin en büyük örneği** (`AdminUserActivity-RefundedTerminal` dört tur açık görünmüştü; bu kez **altı satır** aynı anda). Ölçülmeden fix yazılsaydı var olmayan iş "yapılmış" olacaktı.
>
> **Kapatma değil daraltma da yapıldı:** `url-state-sync`'in tek açık parçası (`NewTransactionForm` adım durumunu `useState`'te tutuyor, yenilemede sıfırlanıyor) `wizard-hard-refresh-resets` olarak ayrı satıra bölündü — çözüm yeri (URL mi `sessionStorage` mı) bir tasarım kararı olduğu için kapatılmadı.
>
> **İki gerçek düzeltmenin ikisi de "kayıtta olmayan sonuç" barındırıyordu.** `UITour-StateScreensReachableAnonymously`'nin ürün kararı (anonim erişim) korundu, ama ölçüm kayıtta olmayan bir şey gösterdi: `noindex`/`robots` frontend'de **hiç geçmiyordu**, yani üç durum ekranı arama motorlarına tamamen açıktı. `T136-FormatRelativeTimeUnused` ise "tüketicisiz util" değil, **next-intl'in üretimde kullandığımız `relativeTime`'ının elle yazılmış ikinci nüshası** çıktı — silme gerekçesi bu yüzden "kullanılmıyor" değil "tekrarlıyor" oldu (GUARDRAILS §4).
>
> **Ölçümün kendisi de doğrulandı:** noindex'in *derlenmesi* değil *ulaştığı* ölçüldü — build alınıp sunucu kaldırıldı, üç ekran `noindex, nofollow` döndürdü ve **negatif kontrol** (`/en/privacy`) robots meta'sı döndürmedi.
>
> **Önceki durum (2026-08-24, WP1 §4):** **55 aktif / 71 çözülmüş**. **🔴 YOK.**
>
> **Bu, "backlog'un tamamını sırayla kapat" turunun ilk paketi.** Tur başında altı **karar-kilitli** satır (metni "owner kararı: yapılmayacak" ya da "bilinçli ürün kararı" diyenler) proje sahibine yeniden açıldı ve altısı da karara bağlandı; ayrıca yeni faz işi gerektiren yedi büyük kalem (`P2P-DeliveryPollingJob` · `NonDeliveryAbuseWindow` · `FloatVerification` · `SettlementTiering` · `HotWalletPolicyReview` · `StubPayoutVerifier` · `tronweb 6 major bump`) bilinçli olarak **ertelendi** — kapsam daraltması değil, kayıtlı bir karar.
>
> **WP1 §4'ün iki satırı da aynı kusur ailesindendi: kod vardı, onu tetikleyen ya da görünür kılan şey yoktu.** `T50` bir **çağıran** eksiğiydi (`FreezeManyAsync` T50'den beri hazır, otomatik tespit onu hiç çağırmıyordu), `T81` bir **sinyal** eksiği (kod yolu WP4a'da tamdı, iki config sessizce fail-open bırakabiliyordu). İkisi de `ForwardedHeadersNotRegistered` ile aynı desen: hiçbir şey hata vermiyor, kontrol sadece hiç çalışmıyor.
>
> **T50 bir owner kararının geri alınmasıydı ve öyle kaydedildi:** WP16 health probe'u bilinçli olarak "alert-only" bırakmıştı, ama 02 §3.3 tespitin otomatik olduğunu **ve** timeout'ların donduğunu söylüyor. Alert-only hâlde bedeli kullanıcı ödüyordu — platformun kendi kesintisinde timeout yiyor ve bunu ancak uyanık bir admin önlüyordu.
>
> **Turun ilk ölçüm dersi testlerden çıktı:** WP1'in 4 yeni testi mutasyonla denendiğinde **ikisi mutasyon altında da geçti** — yani yorumlarının iddia ettiğinden azını ölçüyorlardı (`T140-TestClaimsExceedMeasurement` deseninin canlı örneği). Güçlendirildiler; şimdi dördü de kırılıyor.
>
> **Önceki durum (2026-08-24, oturum kapatma ikilisi kapandı):** **57 aktif / 69 çözülmüş**. **🔴 YOK.**
>
> **İki satır aynı PR'da kapandı çünkü ayrı ayrı kapanamazlardı:** F7a ([#282](https://github.com/turkerurganci/Skinora/pull/282)) ana başlığa çıkış düğmesini ekledi **ve** çıkışı `@/lib/auth/signOut` tek kapısından geçirip **A8'in ilk üretim çağıranı** oldu. Düğme A8'i çağırmasaydı oturum sunucuda yine açık kalırdı; A8 bağlanıp düğme eklenmeseydi sıradan kullanıcı ona hiç ulaşamazdı.
>
> **Sabahtan beri açılan beş satırın dördü aynı gün kapandı.** Bugünün turu şu sırayla yürüdü: UI turunun son satırı (F5) → F4'ün canlı probe'u hazırlanırken ana yol açığı (F4a) → onun açtığı regresyon (F4b) → gerçek girişte çıkan hata-ekranı boşluğu (F4c) → çıkış yaparken fark edilen oturum ikilisi (F7a). **Hiçbiri kod taranarak bulunmadı; hepsi ürün kullanılmaya çalışılırken çıktı.**
>
> **Önceki durum (2026-08-24, yaş kapısı config kalemi):** **59 aktif / 67 çözülmüş** (ölçüm komutu aşağıda). **🔴 YOK.**
>
> **Bir ⚪ satır açıldı: `AgeGateDisableValueUnreachable` (§8).** Yaş kapısının koda yazılı "kapalı" değeri (`0`) AD9'un jenerik pozitif-sayı kuralı yüzünden API'den **yazılamıyor**; kapatmak için DB'ye doğrudan yazmak gerekiyor. Kusur olarak açılmadı — hiçbir şey bozulmuyor — ama kodun belgelediği bir yetenek desteklenen yoldan erişilemez ve **hangi okumanın doğru olduğu karar sahibinin**.
>
> **Nasıl bulundu:** F4 probe'u için 0 günlük bir Steam hesabının girebilmesi gerekiyordu; eşiği düşürmek istendi ve API reddetti.
>
> **Aynı turda F4'ün CANLI PROBE'U ✓ GEÇTİ.** Kullanıcı `/tr` arayüzünden **yeni** bir Steam hesabıyla kayıt oldu → `PreferredLanguage = tr` (ve `/auth/me` → `"language":"tr"`). F4 öncesi kayıtlı kullanıcı aynı gün defalarca giriş yaptığı hâlde `en` kaldı — **negatif kontrol de geçti**. `UITour-SignupLanguageHardcodedEn` artık yalnız test değil, **ölçümle** kapalı. Eşik AD9 ile `30`'a geri alındı.
>
> **Önceki durum (2026-08-24, oturum kapatma taraması):** **58 aktif / 67 çözülmüş** (ölçüm komutu aşağıda). **🔴 YOK.**
>
> **İki satır açıldı ve ikisi de aynı iş akışının iki yarısı: `Session-NoSignOutInMainHeader` (🟡, §5) ve `Session-LogoutDoesNotRevokeRefreshToken` (🟡, §4).** Sıradan kullanıcının arayüzünde **çıkış düğmesi yok** — `logout()`'un üç çağıranı da sıradan oturumun dışında (admin başlığı · askıya alınmış başlık · hesap kapatma sonrası). Var olan "Sign out" ise **A8 `POST /auth/logout`'u hiç çağırmıyor**: refresh token iptal edilmiyor, HttpOnly cookie sunucuda 7 gün geçerli kalıyor. **Düğme tek başına eklenirse oturum yine sonlanmaz** — ikisi birlikte düzeltilmeli.
>
> **Nasıl bulundu — kod taranarak değil, ürün kullanılmaya çalışılarak.** F4'ün canlı probe'u için ikinci bir Steam hesabıyla giriş gerekince *"nasıl çıkış yapacağım"* sorusu soruldu ve cevabı olmadığı görüldü. Bu, `ForwardedHeadersNotRegistered`'ın çıkış biçiminin aynısı: kontrol, **kendi çıktısını kullanmaya çalışan biri** tarafından yakalandı — ne bir test, ne bir gate, ne de bir tarama onu görmüştü.
>
> **Önceki durum (2026-08-24, F4'ün ana yolu — F4a):** **56 aktif / 67 çözülmüş** (sayaç değişmedi). **🔴 YOK.**
>
> **Dün ✅ işaretlenen bir satırın pratikte AÇIK olduğu ölçülerek bulundu ve aynı gün kapatıldı.** F4 (#272) backend yarısını doğru kurmuştu ama ürünün **birincil kayıt yolunda** dil hâlâ `en`'e düşüyordu: `/tr` açılış CTA'sı `returnUrl` geçirmiyor, login sayfası locale'siz `"/dashboard"`a düşüyordu. F4a [#276](https://github.com/turkerurganci/Skinora/pull/276) bunu kapattı. Satır ✅ kaldı ama **gerekçesi düzeltildi** — "F4" değil "F4 + F4a".
>
> **Bulgunun nasıl çıktığı, bulgunun kendisi kadar kayda değer:** aranmıyordu. Kullanıcının tarayıcısında koşulacak F4 probe'u **hazırlanırken** zincir simüle edildi. Probe olduğu gibi koşulsaydı sonuç `en` çıkacak ve **sebebi bilinmeyecekti** — muhtemelen "F4 çalışmıyor" diye kaydedilecekti. **Ders:** elle koşulacak bir doğrulamanın zincirini önce ölç; probe'un ne ölçtüğünü bilmeden koşmak, kusuru bulmak değil **gizlemektir**.
>
> **Önceki durum (2026-08-24, UI turunun SON satırı — F5):** **56 aktif / 67 çözülmüş** (ölçüm komutu aşağıda). **🔴 YOK.**
>
> **UI turunun dokuz satırının DOKUZU DA kapandı.** Son satır `UITour-AdminUsersPageIsStub` → F5 ([#274](https://github.com/turkerurganci/Skinora/pull/274)): `/admin/users` artık gerçek bir kullanıcı dizini ve S20 detay sayfasının **ilk giriş kapısı** — o sayfa T39'dan beri çalışıyordu ama oraya ancak SteamID ezbere bilinerek gidiliyordu. `UITour-StateScreensReachableAnonymously` ⚪ olarak açık kalıyor ama **kusur değil**: bilgilendirme ekranlarının girişsiz açılması bilinçli bir ürün kararı ve F6'da öyle kaydedildi.
>
> **Turdan kalan açık kayıt kod değil, ağ:** F1/F2 kapandığına göre *gerçek oturumla* uçtan uca bir akış (kayıt → MA → item seç → işlem oluştur) otomatik E2E ağına girmeli. Turun var oluş sebebi buydu — E2E Steam OAuth'u JWT enjeksiyonuyla atlıyor ve dört faz boyunca hiçbir ekran gerçek bir oturumla görülmemişti. **F4'ün ve F5'in canlı probe'ları da koşulmadı** (F5'te Docker kapalıydı); ikisi de gerçek oturum gerektiriyor ve aynı açık kaydın parçası.
>
> **Önceki durum (2026-08-24, UI turunun küçük kalemleri — F6):** **57 aktif / 66 çözülmüş** (ölçüm komutu aşağıda). **🔴 YOK.**
>
> **UI turunun dokuz satırından sekizi kapandı; geriye bir tanesi kaldı:** `UITour-AdminUsersPageIsStub` (🟡 — backend AD15 hazır, iş tamamen frontend; düzeltme planı §F5). F4 ([#272](https://github.com/turkerurganci/Skinora/pull/272)) kayıt anındaki dil sabitini kaldırdı; F6 ([#273](https://github.com/turkerurganci/Skinora/pull/273)) `/transactions` iskeletini dashboard'a yönlendirdi, ikonları ekledi, starter artıklarını sildi. `UITour-StateScreensReachableAnonymously` **bilinçli olarak açık**: bilgilendirme ekranlarının girişsiz açılması bir ürün kararı, kaza değil.
>
> **F4'ün satırı kendi PR'ında değil, bu turda çevrildi** — merge'den sonra liste bir gün boyunca kapanmış bir işi 🟡 gösterdi. Dosyanın kendi başlığında adı geçen kusur ailesi tam olarak budur (`AdminUserActivity-RefundedTerminal` dört tur boyunca açık görünmüştü); bu kez bir gün sürdü, çünkü sayaçlar yine dosyanın kendi `awk` komutuyla türetildi ve fark oradan çıktı. **Kalıcı ders değişmedi:** başlıktaki sayı gövdeden türetilebilir bir şey; elle tutulduğu sürece her PR'da yeniden bozulabilir.
>
> **Önceki durum (2026-08-23, UI turu bulgularının düzeltilmesi):** **61 aktif / 62 çözülmüş** (ölçüm komutu aşağıda). **🔴 YOK.**
>
> **Dört 🔴 aynı gün açıldı ve aynı gün kapandı** — biri projenin ilk kırmızısıydı. `ForwardedHeadersNotRegistered` → F3a ([#270](https://github.com/turkerurganci/Skinora/pull/270)) · `UITour-NoUiPathToVerifyMobileAuthenticator` → F1 ([#268](https://github.com/turkerurganci/Skinora/pull/268)) · `UITour-InventoryClientRefusedBySteam` → F2 ([#269](https://github.com/turkerurganci/Skinora/pull/269)) · `UITour-AuthBucketIncludesSessionReads` → F3b+F3c ([#270](https://github.com/turkerurganci/Skinora/pull/270)). **İlk ikisi birlikte ürünün ana akışını açtı:** bu PR'lardan önce hiçbir kullanıcı arayüzden işlem oluşturamıyordu ve bu dört fazdır böyleydi.
>
> **Bu satırların kapatılması ayrı bir iş olarak yapıldı ve sebebi kayıtlıdır:** düzeltmeler merge edildikten sonra liste hâlâ dört açık 🔴 gösteriyordu. Projenin iki kez yaşadığı kusur ailesi tam olarak budur — `AdminUserActivity-RefundedTerminal` T118'de düzeltilmiş, satırı çevrilmemiş ve **dört tur boyunca açık görünmüştü**. Sayaçlar dosyanın kendi `awk` komutuyla türetildi.
>
> **Önceki durum (2026-08-23, gerçek Steam oturumuyla UI turu):** **65 aktif / 58 çözülmüş**. **🔴 4** — `ForwardedHeadersNotRegistered` · `UITour-NoUiPathToVerifyMobileAuthenticator` · `UITour-InventoryClientRefusedBySteam` · `UITour-AuthBucketIncludesSessionReads`.
>
> **Dokuz satır açıldı; üçü 🔴 ve ikisi ürünün ana akışını kapatıyor.** Kullanıcının **kendi Steam hesabıyla** tarayıcıdan girilip 28/32 rota gezildi ([`TEST_REPORTS/UI_TOUR_2026-08-23.md`](TEST_REPORTS/UI_TOUR_2026-08-23.md), düzeltme planı [`…_FIX_PLAN.md`](TEST_REPORTS/UI_TOUR_2026-08-23_FIX_PLAN.md)). En ağırı: **hiçbir kullanıcı arayüzden işlem oluşturamıyor** — MA doğrulaması `MobileAuthenticatorVerified` bayrağına bakıyor, o bayrağı yazan tek uç U17, ve **U17'yi çağıran hiçbir frontend kodu yok**; kullanıcı `/transactions/new` → `/auth/mobile-authenticator` → "yeniden kontrol et" (Steam'e sormuyor) döngüsünde kilitleniyor. İkincisi: envanter istemcisi Steam'den **429** alıyor, oysa aynı konteynerden düz `fetch` **200** dönüyor. Ayrıntılar §10'da.
>
> **Turun kendi disiplini kayda değer: beş tez bulgu sanılıp ölçümle elendi** — dört admin rotasının "yetki hatası" sanılması (gerçek sebep 429), "Steam avatarları görünmüyor" (ekran görüntüsü zamanlaması), `/en/settings` 401'leri (token yenileme doğru çalışıyordu), devralınan *"envanter `STEAM_API_KEY` yüzünden kapalı"* kaydı (gerçek sebep gizli profildi), ve U17'nin geçerli bir URL'i reddetmesi (isteği yanlış kuran bendim). **Ölçüm aracının kendi dört kusuru da bulunup düzeltildi** — dinleyici sızıntısı, erken ekran görüntüsü, tek sekmede token arama, turun kendi rate limit kovasını tüketmesi. Düzeltmeden önceki sayılar kullanılmadı.
>
> **Önceki durum (2026-08-23, F6→F7 yükseltmesinin devamı — ilk 🔴):** **56 aktif / 58 çözülmüş** (ölçüm komutu aşağıda). **🔴 1** — `ForwardedHeadersNotRegistered`.
>
> **Bir satır açıldı ve projenin ilk 🔴'sı oldu.** Yükseltmeden sonra yapılan gerçek Steam girişinin kaydını okurken çıktı: `UserLoginLogs.IpAddress` = `::ffff:172.20.0.5`, yani **nginx container'ının kendi IP'si**. Sebep backend'de `UseForwardedHeaders`'ın hiç kayıtlı olmaması (nginx tarafı doğru header'ları gönderiyor). Sonucu üç güvenlik kontrolü taşıyor — `auth` rate limit izolasyonu, geo-block, VPN sinyali — ve üçü de **hata vermeden** etkisiz kalıyor. Ayrıntı §8'deki ev satırında, özet `🔝 Öne Çıkanlar`'da.
>
> **Bulgunun nasıl çıktığı, bulgunun kendisi kadar kayda değer.** Aranmıyordu: soru *"kim giriş yapmış?"* idi ve cevabı vermek için login satırı okundu. IP alanı cevabı **veremeyince** — kişi ayırt etmiyordu — alanın neden işe yaramadığı sorusu açıldı. Yani kontrol, **kendi çıktısını kullanmaya çalışan biri** tarafından yakalandı; ne bir test, ne bir gate, ne de bir tarama onu dört faz boyunca görmüştü. **Kalıcı ders:** bir alanın *dolu* olması *doğru* olduğu anlamına gelmez — `IpAddress` hiçbir zaman boş değildi, her zaman **yanlış**tı. Günün diğer üç bulgusuyla aynı aile (sabit health check · kendine bakan nginx healthcheck · elle tutulan sayaçlar): hepsi **sorgulanmayan bir sinyal**.
>
> **Önceki durum (2026-08-23, F6→F7 ortam yükseltme turu):** **55 aktif / 58 çözülmüş** (ölçüm komutu aşağıda). 🔴 yok.
>
> **Bir satır açıldı: `SidecarHealthChecksArePlacebo` (🟡, §8).** Lokal ortamı `DEPLOY_RUNBOOK §G.0a` ile F7'ye yükseltirken ölçüldü: iki sidecar'ın `/health` ucu sabit `healthy` döndürüyor ve platformun kesinti tespiti bu ucu okuyor. Aynı turda `§G.0a` adım 2'nin sabit **"40 migration"** beklentisi de bayat çıktı (#262'den sonra 41) ve **türetilebilir** hâle getirildi — satır açılmadı, runbook'ta doğrudan düzeltildi.
>
> **Kalıcı ders — "11 container healthy" bir gözlem değil, bir ölçüm iddiasıdır; öznesi sorulmalıdır.** F7 gate'i bu dersi bir kez öğrendi (*"healthy bir stack, o stack'in NE OLDUĞUNU ölçmeden kanıt değildir"* — F7-N4: image'ler F6 dönemindeydi). Bu tur bir kat derinini buldu: orada sinyal doğruydu, **öznesi** yanlıştı; burada **sinyalin kendisi sabit** — hiçbir özne için doğru değil. Günün üç bulgusu (backlog başlık sayacı · runbook migration sayısı · sidecar health) **aynı aileden**: elle tutulan bir sabit, türetilebilir bir gerçeğin yerine geçiyor ve kimse onu üreten komutu koşmuyor.
>
> **Önceki durum (2026-08-23, üç chore PR'ının merge'i):** **54 aktif / 58 çözülmüş** (ölçüm komutu aşağıda). 🔴 yok.
>
> **Dört kalem kapandı, başlık sayacı merge sonrası düzeltildi (56/56 → 54/58).** 2026-08-22'de açılan üç chore dalı 2026-08-23'te sırayla merge edildi: [#260](https://github.com/turkerurganci/Skinora/pull/260) iki kalem (`AdminUserActivity-RefundedTerminal` + `T133a-ActiveCounterRefunded`) · [#261](https://github.com/turkerurganci/Skinora/pull/261) `T113-AdminRoleNameReuse500` · [#262](https://github.com/turkerurganci/Skinora/pull/262) `T140-OutboxDispatchOrderNonDeterministic`. Yeni kalem açılmadı.
>
> **Sapma merge'in ürünü DEĞİLDİ — iki dalın kendi içinde zaten vardı ve bu varsayılmadı, ölçüldü.** Merge'den önce dört dalda aynı komut koşuldu: `main` 58/54 (başlık 58/54 ✓) · #260 56/56 (başlık 56/56 ✓ — başlığı **güncelleyen tek dal**) · #261 57/55 (başlık **58/54** ✗) · #262 57/55 (başlık **58/54** ✗). #261 ve #262 birer kalemi ✅'ye çevirip başlığa dokunmadı; ikisi de **kendi dalında bayat bir sayaçla** yeşil CI aldı. Merge iki sapmayı üst üste koydu (toplam 2 kalem), yaratmadı.
>
> **Kalıcı ders — dosya kendi ölçüm komutunu yazdı ama onu koşan bir kapı yok.** Hijyen turu (2026-08-22) sayım tanımını düzeltti ve komutu bu başlığa yazdı; komut yazılı olduğu hâlde ertesi gün iki PR onu koşmadan geçti ve hiçbir kontrol sormadı. Başlıktaki toplam gövdeden **türetilebilir** bir sayıdır; elle tutulduğu sürece her PR'da yeniden bozulabilir. Bu tur için bekçi satırı **açılmadı** — sapma 2 kalemdi ve saatler içinde yakalandı — ama kusurun adı burada kayıtlı: bir sonraki tur aynı sapmayı görürse bedeli artık ölçülmüş olacak.
>
> **Önceki durum (2026-08-22, F7-sonrası parite turu):** **56 aktif / 56 çözülmüş** (ölçüm komutu aşağıda). 🔴 yok.
>
> **§4'ün ilk kalemi iki satır birden kapattı (58 → 56).** `AdminUserActivity-RefundedTerminal` **T118'de düzeltilmişti, satırı hiç çevrilmemişti** — çözülmüş bir iş dört tur boyunca açık göründü. Satır kapatılırken kardeş listeler tarandı ve **kayıtsız üç sapma** çıktı: `FraudFlagAdminQueryService`'in iki yüklemi (AD2, AD3) ve `FraudFlagService.ApplyEmergencyHoldCascadeAsync` hâlâ yalnız beş terminali dışlıyordu. Bunlar `T133a-ActiveCounterRefunded`'ın iki yüklemiyle birlikte tek turda kapatıldı; ikinci satır da ✅ oldu.
>
> **Kalıcı ders — parite yorumla korunmaz.** AD3 yükleminin XML doc'u *"AD19d ile aynı aktif tanımı: **beş** terminal state hariç"* diyordu, oysa AD19d **altı** dışlıyordu: parite ölmüş, parite iddiası yaşamaya devam etmişti. `REFUNDED` WP5'te terminal oldu ve değişiklik kopyaların bir kısmına ulaşıp bir kısmını ıskaladı; sapma **dört faz boyunca üç ayrı turda yarım yarım** bulundu (T117 doğrulaması → T118 bir kopyayı düzeltti · T133a iki kopyayı ölçüp kaydetti · fraud modülünün üç yüklemini hiç kimse görmedi) ve **her seferinde bir insan tek bir çağrı yerini okuyarak**, hiçbir zaman bir test yakalamadı. Yapısal kapanış: sekiz tanım tek kaynağa (`TransactionStatusSets`) indirgendi ve guard **türetilmiş** yazıldı — bir statü, `TransactionStateMachine` ondan hiçbir tetikleyiciye izin vermiyorsa terminaldir. İkinci bir elle yazılmış liste **bilinçli olarak yok**, çünkü kusurun kendisi oydu. Bu, `T140`'ın "sahipsiz yorum ucuz değildir" dersinin kod tarafındaki kardeşidir.
>
> **Önceki durum (2026-08-22, backlog hijyen turu — sayım tanımı düzeltildi + §1 kapatıldı):** **58 aktif / 54 çözülmüş** (ölçüm komutu aşağıda). 🔴 yok.
>
> **§1'in üç kalemi kapatıldı (aktif 61 → 58).** `T69-K4` · `T68-K1` · `T64-BotWebhookHandler` — üçü de bot custody katmanına aitti ve F7'de (T132/T133) konusuz kaldı. **T116 bunu §9'un sonunda not etmiş ama satırları ⚪ AKTİF bırakmıştı:** dosya "yeni iş üretmezler" derken sayaç onları üç açık iş olarak sayıyordu ve backlog'u **sırayla** işleyen herkes ilk üç kalemde ölü işe çarpıyordu. Üçü de kapatılmadan önce **ölçüldü** (`AdminBotStatusChanged` → 0 eşleşme · `BOT_SESSION_FAILED` AuditAction `Skinora.Shared/Enums/` altında yok · `session_failed`/`removed_from_pool`/`bot-events` → 0 eşleşme), satırlar tarihsel izlenebilirlik için yerinde bırakıldı (GUARDRAILS §4). Ayrıca tek başına duran `✓` işareti `✅`'ye normalize edildi (`P2P-BotCodeArchive`) — iki sayaçtan da düşüyordu, artık kapalı sayılıyor.
>
> **Bu turun bulduğu şey bir kalem değil, listenin kendisiydi.** Üç kusur ölçüldü ve düzeltildi:
>
> 1. **Beş satır hiçbir bölümde değildi.** T136 turunda eklenen beş satır (`T136-FormatRelativeTimeUnused` · `T136-E2EDeadItemReturnedAssertions` · `T136-DeadRolePermissionRows` · `T136-NoRouteOrMessageCompileGuard` · `T136-ParityGuardsSkippedOnBackendPRs`) **başlıktaki değişiklik günlüğünün ortasına**, iki `> **Durum...**` satırının arasına düşmüştü — başlıksız bir tablo parçası olarak render oluyor ve §5/§7/§8'e bakan hiç kimse onları bulamıyordu. Ev bölümlerine taşındılar.
> 2. **`🔝 Öne Çıkanlar` karma bir depoydu.** 28 satırının 22'si ev bölümündeki satırın kopyasıyken **altısı yalnız orada yaşıyordu** (`T103b-2/-3` · `T55-DormantThreshold` · `T133b-AdminDtoItemReturnedXmlDoc` · `T138-DeliveryExpectedNeverPublished` · `T138-E2ENginxPathFilterGap` · `T138-E2ENoArtifactUpload`) — yani ne saf özet ne saf depo. Altısına da ev bölümü satırı açıldı; **artık her öne çıkan kalemin tam olarak bir ev satırı var** ve bölüm saf özettir.
> 3. **Headline metriği ÖĞE değil SATIR sayıyordu.** Eski komut (`grep -cE "^\| (🔴|🟡|⚪) "`) `Öne Çıkanlar`'daki kopyaları da sayıyordu; 2026-08-22 F7 Gate Check'inin raporladığı **"67 aktif / 65 çözülmüş"** rakamı bu yüzden **satır** sayısıydı, benzersiz **öğe** sayısı değil. Gerçek: **61 aktif / 50 çözülmüş** (aktifte 6, çözülmüşte 15 kopya). Rakam etiketiyle ("aktif satır") teknik olarak tutarlıydı ama herkes — bu turdan önceki her tur ve F7 gate raporu dahil — onu öğe sayısı olarak okudu. **Trend karşılaştırmaları geçerliliğini korur** (metrik turlar boyunca tutarlıydı), yalnız mutlak sayı öğe sayısı değildi.
>
> **Yeni ölçüm komutu (kopyaları saymaz — `Öne Çıkanlar` bölümünü atlar):**
>
> ```bash
> # aktif
> awk 'BEGIN{s=0}/^## /{s=($0 ~ /Öne Çıkanlar/)?0:1}/^\| (🔴|🟡|⚪) /{if(s)n++}END{print n+0}' Docs/DEFERRED_BACKLOG.md
> # çözülmüş
> awk 'BEGIN{s=0}/^## /{s=($0 ~ /Öne Çıkanlar/)?0:1}/^\| ✅ /{if(s)n++}END{print n+0}' Docs/DEFERRED_BACKLOG.md
> ```
>
> **Kalıcı ders — F7 gate'in kendi dersinin listeye uygulanması:** gate *"bir iddia, onu ölçen taramanın kapsamı kadar doğrudur"* dedi ve testleri CI ile assembly bazında karşılaştırdı; ama **backlog rakamını dosyanın kendi komutundan devraldı ve komutun ne saydığını sorgulamadı**. Aynı hata sınıfı, bir seviye yukarıda. Bir metriğin uzun süre tutarlı olması onu doğru yapmaz — yalnız **karşılaştırılabilir** yapar.
>
> **Önceki durum (2026-08-22, F7 Gate Check — sayım tanımı düzeltilmeden önce; aşağıdaki 67/65 SATIR sayısıdır):** **67 aktif / 65 çözülmüş** (ölçüm komutu: `grep -cE "^\| (🔴|🟡|⚪) "` → 67, `grep -c "^| ✅ "` → 65). **F7 Gate Check ✓ PASS — bloke edici bulgu 0**, 🔴 yok. İki satır açıldı, ikisi de gate'in kendi uçtan uca eşleme taramasından: `F7Gate-OrphanNotificationTypes` (🟡 — 06 §2.13'ün dört bildirim tipinin üreticisi yok) · `F7Gate-EventsWithoutConsumer` (⚪ — iki domain event'in tüketicisi yok). **İkisi de `phase/F6-pass` ağacında `git grep` ile BİREBİR AYNI hâlde ölçüldü → F7 regresyonu değil, F7'nin ortaya çıkardığı taban borcu; bu yüzden S2 (Kırılma) değil ve gate'i bloklamadılar.** **Kalıcı ders (T140'ın dersinin faz düzeyine yükseltilmesi):** her iki satır da tek tek görev doğrulamalarının **göremeyeceği** cinsten — bir görev yalnız kendi diff'ine bakar, oysa "katalogda var ama kimse üretmiyor" / "yayınlanıyor ama kimse tüketmiyor" ancak **tüm katalog ile tüm üreticilerin çapraz eşlemesi** koşulunca görünür. F7 bu kusur ailesini iki kez yaşadı (`T133b-PaymentMonitorUnarmed`, `T138-DeliveryExpectedNeverPublished` → T140) ve her ikisinde de teşhis turlarca gecikti. Bu yüzden eşleme taraması **11 §6.2'ye F7 satırı olarak FK3/FK4 adıyla yazıldı** — bir daha gate'in insafına kalmasın diye. `F7Gate-EventsWithoutConsumer` için **refute turu koşuldu ve para yolu deliği OLMADIĞI gösterildi**: geç-ödeme izlemesi fiilen `PostCancelMonitor` yolundan armlanıyor. Aynı gate'te dört doküman/ortam bulgusu (F7-N1 §7 traceability matrisi · F7-N2 §3/§5 faz aralığı · F7-N3 §6.2 F7 satırı · F7-N4 Post-MVP §G ortam driftı + `DEPLOY_RUNBOOK` §G.0a yükseltme adımı) **backlog'a düşmedi, gate chore PR'ında doğrudan düzeltildi**.
>
> **Önceki durum (2026-08-22, T140 doğrulaması):** **65 aktif / 65 çözülmüş** (ölçüm komutu: `grep -cE "^\| (🔴|🟡|⚪) "` → 65, `grep -c "^| ✅ "` → 65). **Bloke edici bulgu 0 → T140 ✓ PASS**, 🔴 yok. Beş satır açıldı, beşi de doğrulama turunun kendi ölçümünden: `T140-TestClaimsExceedMeasurement` (⚪) · `T140-MultiPaymentLeakPin` (⚪) · `T140-E2ENotificationsSoftDeleteFilter` (⚪) · `T140-DocFooterVersionDrift` (⚪) · `T140-OutboxDispatchOrderNonDeterministic` (⚪). **Dalda düzeltilen bulgu — turun kendi dersinin düzeltmenin üstünde tekrarlanması:** T140, `TransactionStatusChangedEvent`'in XML doc'una ve 05 §5.3'e *"ödeme onayı **tek** kasıtlı istisnadır"* diye yazdı; ölçüm bunu **yanlışladı** — `SettlementVerificationJob` mutabakatta geri alma tespit edince `SettlementReversalDetectedEvent` **ile aynı** unit-of-work'te `TransactionStatusChangedEvent(ITEM_DELIVERED → REFUNDED)` yayınlıyor (`SettlementVerificationJob.cs:371-390`, tek `SaveChangesAsync` :397). Yani olayın **dördüncü** bacağı var ve tablo onu hiç taşımıyordu, üstelik altındaki not başlığı *"neden üç ayrı satırda"* diyordu. Aynı taramada tüketici kolonunun iki satırı da kodla uyuşmuyordu (`TransactionCancelledEvent` → var olmayan "Blockchain"; `SettlementReversalDetectedEvent` → "Fraud", oysa işareti yayıncının kendisi yazıyor) ve `PaymentConfirmed` push'u "alıcıya" deniyordu (işlem odasına gidiyor). Üçü de düzeltildi, 05 v3.5 → **v3.6**. **Kalıcı ders:** T140 bir *bağlanmama kararının* onu emreden yorumun güncelliği kadar doğru olduğunu ölçtü; doğrulaması bir *hizalama iddiasının* hizalamayı ölçen taramanın kapsamı kadar doğru olduğunu ölçtü — tablo olay **adları** ve **yayıncılar** için tam doğruydu, **tüketici kolonu** ve **bacak sayımı** için değildi. T138'in B1'i ile aynı aile: kanıt komutunun kapsamı, iddianın tavanıdır.

> **Önceki durum (2026-08-22, T140 yapımı):** **60 aktif / 65 çözülmüş** (ölçüm komutu: `grep -cE "^\| (🔴|🟡|⚪) "` → 60, `grep -c "^| ✅ "` → 65). **Yeni satır YOK, tek 🔴 kapandı:** `T138-DeliveryExpectedNeverPublished` ✅ → T140. Listenin tek 🔴'sıydı; **listede artık 🔴 yok.** Turun asıl getirisi düzeltmenin kendisi değil, **kök nedeni**: açık unutkanlık değildi — olayın XML doc'unun paragraf 1'i ödeme onayını "bu olayı yayınlaMAmalı" listesinde adıyla sayıyor, paragraf 2'si ise üreticiyi "T124 yazar" diyordu, ve T124 **yazılı talimata uydu**. Bayat bir custody-dönemi kuralı, çelişkisi kimse tarafından okunmadan dört görev boyunca yaşadı. Yalnız yayıncıyı bağlamak açığı **yeniden üretilebilir** bırakırdı; paragraf düzeltildi ve neden değiştiği dosyada kayıtlı. **Kalıcı ders, T138'in B1 dersinin kardeşi:** T138 bir *kapatma iddiasının* kanıt komutunun kapsamı kadar doğru olduğunu ölçmüştü; T140 bir *bağlanmama* kararının onu emreden yorumun güncelliği kadar doğru olduğunu ölçtü. İkisi de aynı yere çıkıyor — **sahipsiz yorum ucuz değildir**, ve `T133b-AdminDtoItemReturnedXmlDoc` (⚪) satırının gerekçesi tam olarak budur.

> **Önceki durum (2026-08-22, T138 doğrulaması):** **61 aktif / 64 çözülmüş** (ölçüm komutu: `grep -cE "^\| (🔴|🟡|⚪) "` → 61, `grep -c "^| ✅ "` → 64). Bağımsız doğrulama **bloke edici bulgu 0** verdi; T138 ✓ PASS. Üç satır açıldı, hepsi turun kendi ölçümünden: `T138-E2ENginxPathFilterGap` (🟡 — yeni UI leg'i `nginx/nginx.conf`'a bağımlı ama `e2e-stack` paths-filter'ında `nginx/**` yok, yani proxy config'ini bozan PR advisory ağı **hiç tetiklemez**; `T136-ParityGuardsSkippedOnBackendPRs`'in aynı sınıfı — "bekçi var ama tetiklenmiyor") · `T138-E2ENoArtifactUpload` (⚪ — on leg'in hiçbiri playwright trace/HTML raporunu yüklemiyor, kırmızı tur tanısı `logs --tail=200`'e kalıyor; T138 öncesinden var, maruziyet 8→10 leg'e çıktı) · `T133b-AdminDtoItemReturnedXmlDoc` (⚪ — yapım turunun **bilerek karara bağlamadığı** ve doğrulamaya bıraktığı soru; karar: **satır açılsın**, çünkü bu turun en değerli bulgusu bir yorumun tam olarak bu şekilde sahipsiz kalıp yanlış kapatma iddiası üretmesiydi). Turda kapatılan iki bulgu dalın kendisinde düzeltildi: `T136-E2EDeadItemReturnedAssertions`'ın **kapanmamış altıncı maddesi** (satırın kendisine bak) ve `emergency-hold.spec.ts`'in beş-dakikalık cron'u 18 sn'de gözlediğini iddia eden yanlış gerekçe yorumu (assertion doğru, **iddia** yanlıştı; ürün karşılığı `SettlementVerificationJobTests.IneligibleTransactions_AreNotEvenRead("hold")`'da deterministik olarak kapalı).

> **Durum (2026-08-21, T138 CI turu):** **58 aktif / 64 çözülmüş** — E2E ağı ilk kez P2P'ye göre koşunca **bir ÜRÜN AÇIĞI** düştü ve satır açıldı: `T138-DeliveryExpectedNeverPublished` (🔴, §Öne Çıkanlar). `DELIVERY_EXPECTED` bildirimi — P2P'de satıcıya "para emanette, item'ı gönder" diyen **tek** çağrı — üretilemiyor; tüketici v3.0'da yazılmış, yayıncısı hiç bağlanmamış. Bedeli satıcı ödüyor: uyarılmayan satıcı teslimat penceresini kaçırır ve kusur ona yazılır. **Sınıf olarak T133b'nin B4'ünün (`POST /api/monitor/start` çağıransız) aynısı** — ikisi de "v3.0 için yazılmış tüketici + hiç bağlanmamış yayıncı", ikisini de yalnız uçtan uca ağ görebilirdi. **Bu, T138'in E2E yeniden yazımının ilk somut getirisidir:** ağ karanlıkken bu açık dört görev boyunca görünmezdi. Proje sahibi kararı (2026-08-21): T138 sıfır production değişikliğiyle kapanır, açık **önerilen T140**'a devredilir ve E2E'ye **kendini kapatan bir işaret** konur (`happy-path.smoke.spec.ts` yokluğu assert eder → açık kapandığı gün test kırılır). Ayrıca aynı turda `cancellation.spec.ts`'in **spec hatası** düzeltildi (ayrıntı T138 raporu): post-payment iptal v3.0'da **asimetrik** — alıcı reddedilir, satıcı geri çekilebilir ve alıcıya tam iade yapılır; ilk taslak custody dönemindeki simetrik kuralı iddia ediyordu ve CI yakaladı.

> **Durum (2026-08-21, T138 yapımı):** **57 aktif / 64 çözülmüş** — T138 **yeni satır açmadı** ve bir satır kapattı: `T136-E2EDeadItemReturnedAssertions` ✅, T138'i açıkça sahibi olarak adlandıran satırdı ve altı maddesinden **beşi** kapandı (altıncısı doğrulama turunda kapandı — satırın kendisine bak; bu blok yazıldığında "altısı da" deniyordu ve o iddia yanlıştı). Kapatma sırasında satırın **kendi envanterinin eksik** olduğu görüldü: `e2e/src/db.ts`'in T137a notu ölü alanı "hikâyenin tamamı" diye tarif ediyordu, yani ölü-referans avı ölü bir referansı daha bırakmıştı. **Bloke etmeyen gözlem, bilerek satır AÇILMADI:** `AdminTransactionDtos.cs:63-65` XML doc'u v3.0'da record'dan düşen `itemReturned` alanından hâlâ söz ediyor — yalnız yorum, davranış etkisi yok; e2e görevinde production kaynağına dokunulmadığı için düzeltilmedi ve bir sonraki backend turunun yolu üstünde. Karar doğrulama turuna bırakıldı: satır açmaya değer mi, yoksa T135 N2 gibi "iş üretmediği için" mi geçilmeli.

> **Durum (2026-08-21, T136 + doğrulama):** **58 aktif / 63 çözülmüş** (ölçüm komutu: `grep -cE "^\| (🔴|🟡|⚪) "` → 58, `grep -c "^| ✅ "` → 63; T136 öncesi taban **54 / 62**, 🔴 yok). **Bir satır kapandı, beş satır açıldı** — beşincisi bağımsız doğrulamanın bulgusudur (N1, aşağıda). Kapanan: `T133a-FePermissionCatalogKeys` ✅ — FE nüshası 14 → 12 **ve** satırın açık bıraktığı yapısal soru (bekçi yok) `permissionCatalog.parity.test.ts` ile kapandı. Açılanlar T136 envanterinin ölçtüğü ama kapsamadığı açıklar: `T136-E2EDeadItemReturnedAssertions` (🟡 — **en değerlisi**: altı ölü custody assertion'ı üç E2E spec'inde duruyor ve **beşi bugüne kadar hiç raporlanmamıştı**; biri `emergency-hold.spec.ts:142`'de **boşuna geçiyor**, yani hard-fail'lerden daha tehlikeli — sahibi T138) · `T136-NoRouteOrMessageCompileGuard` (⚪ — ölü rota href'i ve eksik i18n anahtarı derleme/CI'da sessiz kalır) · `T136-DeadRolePermissionRows` (⚪ — ölçüldü, tuzağa dönüşmüyor) · `T136-FormatRelativeTimeUnused` (⚪ — T136'nın kendi silmesinin yetim bıraktığı util; kaldırmak turun kapsamı değildi). · **N1 (doğrulama turu):** `T136-ParityGuardsSkippedOnBackendPRs` (🟡 — T136'nın *eklediği* bekçi ile T134'ünki backend-only bir PR'da `skipped` olur ve CI Gate `skipped`'ı geçer; drift'i yaratan T132 PR'ı tam da öyle bir PR'dı, yani bekçi o gün var olsaydı **koşmazdı**). **Sayım notu:** T135 satırının parantez içi "53" rakamı bugün aynı komutla **54** çıkıyor; anomali değil — T135'in kendi notu dördüncü satırın grep koşulduktan **sonra** eklendiğini yazıyor, yani başlıktaki "54" baştan doğruydu, parantez içi ölçüm erken alınmıştı. **Kalıcı ders, T134/T135'inkinin üçüncü katmanı:** T134 kopya kataloğun **nüsha** biçimini, T135 **dallanma** biçimini ölçtü; T136 aynı mekanizmanın **kaynağı silindiğinde** hâline baktı. Silinen tarafın FE karşılığı yalnız ölü kalmıyor — bazen **çalışan yanlış** üretiyor: `CancelInfoBlock` her iptalde "Item iade edildi: Hayır" yazıyordu, `SteamAccountsStatus` her admine kalıcı hata paneli çiziyordu. İkisi de **tip yalanı** üzerinden sessizleşmişti (FE tipi backend'de olmayan alanı zorunlu bildiriyordu), yani TypeScript bir kapı değil bir **suç ortağıydı**.

> **Önceki durum (2026-08-21, T135):** **54 aktif / 62 çözülmüş** (durum-emojisi ölçümü: `grep -cE "^\| (🔴|🟡|⚪) "` → 53, `grep -c "^| ✅ "` → 62; T135 öncesi **50 / 62**, 🔴 yok). **Not:** T134 satırı çözülmüşleri 56 sayıyordu; aynı komutla ölçüldüğünde 62 çıkıyor — devralınan sayımın kendisi de bayatlamış, bu yüzden buraya ölçüm **komutu** yazıldı: **dört satır açıldı, hiçbiri kapanmadı.** Üçü de S07 matris turunun ölçtüğü ama kapsamadığı açıklar: `T135-FeDisputeTypeChoices` (🟡 — turun bulduğu tek **kullanıcıya yansıyan** davranış açığı: FE `availableActions.disputableTypes` sözleşmesini hiç okumuyor, üç dispute türünü koşulsuz sunuyor), `T135-FeTimeoutLabelKeyStale` (⚪ — hem ölü hem bayat helper), `T135-TimelineHoldPreviousStatus` (⚪ — T134 doğrulamasının **sahipsiz kalan G2 gözlemi**; artık bir adı ve satırı var). Dördüncüsü, `T135-IntegrationSuiteParallelFlake` (⚪), turun **Docker açıldıktan sonraki** lokal ölçümünden çıktı ve güçlü bir çeliştiricisi kayıtlı. **T135 kendi kapsamındaki drifti kodda kapattı**, backlog'a atmadı: REFUNDED `helpers.ts`'in `isCancelledStatus` ve `isTerminalStatus` kümelerinin ikisinden birden düşüyordu ve iade edilen alıcı ekranda ne iptal kaydını ne parasını görebiliyordu (07 §7.5 ikisini de vaat ediyor). **Kalıcı ders, T134'ünkinin bir katman ötesi:** bir enum üzerinde `switch` yazan her katman enum büyüdüğünde **sessizce** eksik kalır — TypeScript bir `default` gördüğü an tatmin olur, eslint ve `check-i18n.mjs` hiç bakmaz. T134 bu mekanizmayı katalog **kopyalarında** ölçtü; T135 aynı mekanizmanın **dallanma** biçimini ölçtü. Karşı önlem tip sistemi değil, bir **tamlık testidir**: her hücrenin karara bağlandığını iddia eden ve bağlanmayanı **adıyla** söyleyen bir assertion.
>
> **Durum (2026-08-21, T134):** **Aktif satır sayısı değişmedi** (ham `|`-satır ölçümü: **50 aktif / 56 çözülmüş**; T134 öncesi 50/54 — süregelen "45" ifadesi devralınan bir sayımdır, T134 onu doğrulamadı): **iki satır kapandı** (`T133a-FeI18nEmergencyHoldLabel` ✅ dört dilin EMERGENCY_HOLD etiketi kod kataloğuna hizalandı; `T133a-Doc02NotificationCopy` ✅ FE bildirim nüshası 28 → 26 **ve** satırın açık bıraktığı yapısal soru — parity bekçisi yok — iki yeni test ile kapandı), **iki satır açıldı** (`T134-Doc06DeliveryDeferred` doküman borcu, `T134-FeEnumUnionDup` bekçi kapsam sınırı). Kalıcı ders: bir katalog nüshası **kopya** olduğu sürece drift eder; `check-i18n.mjs` dört dilin **birbirine** uyduğunu kanıtlıyordu ve dördü birden yanlışken yeşil kalıyordu.
>
> **Önceki durum (2026-08-20, T139 doğrulaması tur 2):** **45 aktif satır** — tur 2 **yeni satır açmadı**; üç bulgunun üçü de (B1-2 reconciler sayfalama · N1-2 port testleri · N2-2 gauge toplamı) kodda kapatıldı. `T139-ActiveMonitorQuotaAlarm` satırı güncellendi: dayandığı gauge tur 2'de düzeltildi (iki registry artık toplama yazıyor), dolayısıyla eşik aktif **ve** post-cancel izleyicileri birlikte kapsamalı.
>
> **Önceki durum (2026-08-20, T139 doğrulaması tur 1):** **45 aktif satır** — doğrulama turu bir satır açtı (`T139-ActiveMonitorQuotaAlarm`, §9 sonu): T139'un D3 kararı ödeme penceresini sweep'e kadar açık tutuyor ve sweep 7 günlük mutabakat tabanından önce kuyruklanamadığı için eşzamanlı aktif izleyici sayısı **bir haftalık** işlem hacmiyle ölçekleniyor — bedel 08 §3.4'e yazıldı ama gauge'un üzerinde alarm eşiği yok. Aynı turun iki bulgusu (**B1** dispatcher DI kaydı, **N2** devir yarışı) backlog'a değil **kodda** kapatıldı.
>
> **Önceki durum (2026-08-20, T139 yapımı):** **44 aktif satır** — `T133b-PaymentMonitorUnarmed` ✅ **kapandı** (§4): aktif ödeme izleyicisinin kurma, yeniden kurma ve durdurma halkalarının üçü de bağlandı. Satırın kendisi yalnız **kurma** yarısını adlandırıyordu; T139'un ölçümü durdurma halkasının da sahipsiz olduğunu gösterdi (mutlu yolda `MonitoringStatus` hiç `ACTIVE`ten çıkmıyordu). **Kalıcı ders, satırın kendi dersinin devamı:** bir "bağlanmamış caller" satırı yazılırken eksik olanın yalnız **çağrı** mı yoksa **yaşam döngüsünün tamamı** mı olduğu ayrıca sorulmalıdır — çağrıyı tek başına bağlamak bu vakada hiç durmayan bir izleyici ve iptal yolunda çift registry üretecekti.
>
> **Önceki durum (2026-08-20, T133b + doğrulaması):** **45 aktif satır** — T133b §G'nin canlı provasının hiç koşulmadığını kaydeden bir satır açtı (`T133b-LiveRehearsalUnrun`, §9 sonu); **doğrulama turu bir tane daha ekledi** (`T133b-PaymentMonitorUnarmed`, §4 — deposit adresinin ödeme izleyicisini kuran backend caller'ı yok, yani ödeme bacağı gerçek stack'te hiç ateşlenmiyor). İkisi de aynı dersin iki yüzü: yapım turu anlatıyı **kod üzerinden** doğruladı ve her **ad** doğru çıktı, ama adı doğru olan bir adımın **çalışabilir** olduğu ayrıca sorulmadıkça bilinmiyor.
>
> **Önceki durum (2026-08-19, T133a):** **43 aktif satır** — T133a doküman hizalama turu **altı** yeni satır açtı (§9 sonu): iki kod borcu 07 §7.5'in sözleşmesini KORUYARAK kaydedildi (`T133a-PaymentDetailNulls`, `T133a-DisputeBlockNulls`), bir davranış açığı ölçüldü (`T133a-ActiveCounterRefunded`), üç FE nüsha sapması sahiplendirildi (`T133a-FeI18nEmergencyHoldLabel` + `T133a-FePermissionCatalogKeys` → T136/T134 kabul kriterlerine yazıldı, `T133a-Doc02NotificationCopy` → T134). Gerekçe projenin tekrarlanan dersi: **ölçülen açık, sahibi olan bir kriterde yazılı değilse ölür.**
>
> **Önceki durum (2026-08-19, T133):** **37 aktif satır** · 60 satır ✅ çözüldü. `P2P-BotCodeArchive` **kapandı** — bot custody kodunun üç halkasının üçü de silindi ve hepsinin git işaretçisi satıra yazıldı (T117 `82bff4d` · T132 PR #247 · T133 PR #248). İşaretçi olarak **PR numarası** kullanıldı, squash sha'sı değil: sha merge anında doğar, yani satır kendi kapanışını hiçbir zaman yazamıyordu; `git log --grep "(#248)"` squash commit'i sha'dan bağımsız bulur.
>
> **Önceki durum (2026-08-13, T122 ölçümü):** **38 aktif satır** (36 + §9'da 2 yeni: `P2P-InventoryUnauthorizedMapping`, `P2P-SteamRateLimitWindow`) · 59 satır ✅ çözüldü. İkisi de T122'nin canlı Steam ölçümünde çıktı; ayrıca `P2P-FloatVerification`'ın önkoşulu çürütüldü (Game Coordinator gerekmiyor, veri anonim yanıtta mevcut → ⚪'dan 🟡'ye).
>
> **Önceki durum (2026-08-10, T119 denetimi):** **36 aktif satır** (34 + §9'da 2 yeni: `P2P-NonDeliveryAbuseWindow`, `P2P-DeliveryTimeoutWarning`) · 59 satır ✅ çözüldü. İkisi de T119'un 02 §3.1/§13 kapsam denetiminde çıktı: v3.0 teslimat fazını satıcıya devretti, ama fazın **yaptırım** ve **uyarı** bacakları hiçbir F7 görevine bağlı değil.
>
> **Önceki durum (2026-08-09, F7/P2P sonrası):** **34 aktif satır** (28 + §9'da 6 yeni) · 59 satır ✅ çözüldü · P2P geçişiyle **3 satır konusuz kaldı** (bot katmanı — §9 sonundaki nota bakınız). Yeni kalemlerin hiçbiri MVP'yi bloklamıyor.
>
> **Önceki durum (2026-07-26):** **28 aktif satır** · 59 satır ✅ çözüldü (Öne Çıkanlar tablosu gövde satırlarını tekrarladığı için satır sayısı kalem sayısından fazladır) · **F6 Gate Check'i bloklayan: 0** (F6 ✓ PASS 2026-06-24 → MVP kapandı; kalan kalemler post-MVP). Bu dosya bir kalem ele alındıkça güncellenmelidir (satırı **✓ Çözüldü** işaretle veya kaldır).
>
> **Sıralama/sahiplik:** F6 öncesi MVP-içi kalemler [`PRE_F6_PLAN.md`](PRE_F6_PLAN.md)'de 19 iş paketine (WP1–WP18) bağlandı. Aşağıdaki bazı satırlar 2026-06-14 kod taramasıyla **kısmen stale** bulundu ve düzeltildi (emergency-hold, blockchain-monitor, item-refund, steam-sidecar, T55).
>
> **Hijyen taraması (2026-07-26, F6 sonrası):** WP1–WP20 + F6 merge'lerinden sonra kalan 🟡 satırlar üretim wiring'i (DI kaydı + çağıran + migration + test) düzeyinde tek tek doğrulandı. Sonuç: **5 satır tam çözülmüş** (SWEEP-dispatcher → WP3 · item-refund-consumers → WP2 · T38-AdminFlagAlert-FlagId → WP8 · T61-SteamTransitionRealtimePush → WP9 · FE-admin-signalr-subscription → WP9) + Öne Çıkanlar tablosunda gövdeye göre stale kalan 2 satır (T33-SuccessRate → WP17 · T107 → F6) ✅ işaretlendi; **T50-OutageFreezeCallers yalnız manuel yarısı** kapandığı için daraltıldı; **T81-PriceConsumerWireup** "kod ✓ / prod config bağımlı" olarak yeniden yazıldı. `StubPayoutVerifier` doğrulandı — hâlâ açık.

## Lejant

- **Öncelik:** 🔴 high · 🟡 medium · ⚪ low
- **Tip:** `task` (planlı görev) · `backend-gap` (kod boşluğu) · `k-note` (task K-notu) · `code-todo` · `doc-drift` (doküman ↔ kod uyumsuzluğu) · `test-gap` · `bypass`
- **Bloklar mı:** kalemin engellediği şey; "—" = hiçbir şeyi bloklamıyor.
- **Kapanış işareti:** `✅` hem *çözüldü* (iş yapıldı) hem *konusuz kaldı* (iş artık anlamsız) satırları kapsar — ikisi de **açık iş değildir**, ayrım satır metnindedir. Sayaç ikili çalışır: açık (`🔴`/`🟡`/`⚪`) ↔ kapalı (`✅`). Başka kapanış işareti kullanılmaz.

---

## 🔝 Öne Çıkanlar (dikkat gerektiren)

| Önc. | ID | Özet | Bloklar mı |
|---|---|---|---|
| ✅ | ForwardedHeadersNotRegistered | **ÇÖZÜLDÜ → F3a ([#270](https://github.com/turkerurganci/Skinora/pull/270))** — `UseForwardedHeaders` pipeline'ın EN BAŞINDA kaydedildi ve güven `ReverseProxySettings.KnownNetworks/KnownProxies` ile SINIRLANDI; yapılandırılmamışsa **hiç kaydedilmez** (yanlış yapılandırmanın bedeli "eksik bilgi", "yanlış güven" değil). **Canlı ölçüm:** IP A kovayı doldurdu → 429, IP B → **401** (ayrı kova), IP A tekrar → 429 — yani rate limit izolasyonu artık istemci başına çalışıyor. **Spoofing savuşturuldu:** nginx üzerinden sahte `X-Forwarded-For` gönderen istemci kendi kovasından kaçamıyor (nginx zincire gerçek istemciyi ekliyor, `ForwardLimit=1` son hop'u alıyor). **Kökeni de kapatıldı:** bulgunun dört faz görülmemesinin sebebi böyle bir sinyalin hiç olmamasıydı — backend artık açılışta `Forwarded headers ENABLED — trusting networks [...]` ya da yapılandırılmamışsa üç kontrolün ETKİSİZ olduğunu söyleyen bir uyarı basıyor; `DEPLOY_RUNBOOK` §B (iki operatör env'i) + §F (doğrulama adımı) + `.env.example` güncellendi. **Eski açıklama:** **Reverse proxy arkasında istemci IP'si hiç görülmüyor** — `UseForwardedHeaders` kayıtlı değil, `RemoteIpAddress` her istekte proxy IP'si. `auth` rate limit izolasyonu, geo-block ve VPN sinyali belgelenen üretim topolojisinde **sessizce etkisiz**; 17 denetim çağrı yeri proxy IP'si kaydediyor. Canlı kanıt: gerçek Steam girişinin IP'si `::ffff:172.20.0.5` = nginx container'ı. Ev satırı **§8** | Üretimde üç güvenlik kontrolünün etkinliğini bloklar; MVP/faz kapanışını bloklamaz (F0–F7 hepsi ✓ PASS, bulgu bunlardan sonra ölçüldü) |
| ✅ | UITour-NoUiPathToVerifyMobileAuthenticator | **ÇÖZÜLDÜ → F1 ([#268](https://github.com/turkerurganci/Skinora/pull/268))** — yeni `SteamTradeUrlSection` U17'nin ilk UI çağıranı; hem Ayarlar'a hem de kullanıcının engellendiğinde yönlendirildiği `/auth/mobile-authenticator` sayfasına mount edildi. Backend'e dokunulmadı (turun ayırt edici testi backend yarısının eksiksiz olduğunu göstermişti). U17'nin üç cevabı ayrı gösteriliyor ve "Steam erişilemedi" **sessiz başarı olarak gösterilmiyor**. **Uçtan uca doğrulandı** (kullanıcı MA'sız hâle döndürülüp gerçek arayüzden): engel → Ayarlar'dan kayıt → rozet sayfa yenilenmeden `true` → `/transactions/new` 4 adımlı sihirbazı çizdi. **Eski açıklama:** **Hiçbir kullanıcı UI üzerinden işlem oluşturamıyor** — MA doğrulaması `MobileAuthenticatorVerified` bayrağına bakıyor, bayrağı yazan tek uç U17, ve **U17’yi çağıran hiçbir frontend kodu yok**. Kullanıcı `/transactions/new` → `/auth/mobile-authenticator` → "yeniden kontrol et" (Steam’e sormuyor) döngüsünde kilitleniyor. MA gerçekten açıkken bile reddediliyor. Backend yarısı **sağlam** (U17 doğrudan çağrılınca engel düştü, sihirbaz açıldı). Ev satırı **§10** | Ürünün birincil eylemini bloklar |
| ✅ | UITour-InventoryClientRefusedBySteam | **ÇÖZÜLDÜ → F2 ([#269](https://github.com/turkerurganci/Skinora/pull/269))** — `steamcommunity` paketinin eski `request` yığını yerine doğrudan `fetch` (`HttpInventoryFetcher`). Kütüphanenin gözlenebilir davranışı birebir korundu (sayfalama · `classid_instanceid` birleştirme · `asset_properties` · `total_inventory_count` son sayfadan · gizli envanter TAM olarak `This profile is private.` dizgesiyle). `getImageURL()` bilerek **metot** olarak eklendi — düz JSON döndürseydik her item'ın görseli sessizce `null` olurdu. 429/5xx için üstel backoff + 50 sayfalık döngü sınırı eklendi. **Canlı doğrulama:** sidecar `PUBLIC` 1/1 item, `/transactions/new` gerçek envanteri çizdi, item görseli `naturalWidth=512`. **Eski açıklama:** **Sidecar’ın envanter istemcisi Steam tarafından reddediliyor** — `steamcommunity` paketi **429**, aynı konteynerden birebir aynı URL ile düz `fetch` **200**. UA · URL biçimi · profil gizliliği · `STEAM_API_KEY` hipotezleri **ölçümle elendi**. Üretimde "Steam çöktü" gibi görünür. Ev satırı **§10** | Satıcı item seçemez; MA düzeltilse bile işlem oluşturulamaz |
| ✅ | UITour-AuthBucketIncludesSessionReads | **ÇÖZÜLDÜ → F3b + F3c ([#270](https://github.com/turkerurganci/Skinora/pull/270))** — `/auth/me` → `user-read` (kullanıcı bazlı), `/refresh` + `/logout` → yeni `session` kovası (30/60sn); sınıf düzeyi `auth` kovası (10/60sn) yalnız brute-force yüzeyinde kaldı. **F3c:** `AdminGuard` artık "cevap alamadım" ile "yetkin yok"u ayırıyor — 401 → oturum yok, diğer hatalar → yeniden denenebilir hata, yönlendirme YOK; mutasyon turu koşuldu (eski davranışa dönünce tam olarak yeni davranışı iddia eden iki test kırıldı). Not: `RateLimitTests`'in refresh testi sabit "10 istek" varsayımından kurtarıldı — ölçtüğü şey limitin DEĞERİ değil, aşıldığında dönen cevabın BİÇİMİ. **Eski açıklama:** **Kimliksiz biri 10 istekle tüm platformun oturum katmanını 60 sn düşürebiliyor** — `/auth/me`, `/refresh`, `/logout` giriş denemeleriyle aynı 10/60sn IP-bazlı kovada. 429 sessizce "yetkisiz" gibi görünüyor (admin sayfaları dashboard’a atılıyor). `ForwardedHeadersNotRegistered` yayılma yarıçapını açıklar; **bu satır kova tasarımıdır** ve o düzeltilse bile NAT arkasını kırar. Ev satırı **§10** | Oturum katmanında hizmet reddi + sessiz bozulma |
| ✅ | T138-DeliveryExpectedNeverPublished | **ÇÖZÜLDÜ → T140** — `AdvanceStateMachineAsync` artık geçişle **aynı** `SaveChanges` içinde `TransactionStatusChangedEvent(SELLER_CONFIRMED → PAYMENT_RECEIVED)` yayınlıyor; `HappyPathMilestoneNotificationConsumer`'ın ölü `PAYMENT_RECEIVED` kolu canlandı ve satıcı `DELIVERY_EXPECTED` satırını alıyor. **Satır kendisinden geniş kapandı, üç noktada:** (a) **KÖK NEDEN** — açık unutkanlık değildi: olayın XML doc'unun paragraf 1'i ödeme onayını "bu olayı yayınlaMAmalı, yoksa çift-push olur" listesinde **adıyla sayıyordu**, aynı doc'un paragraf 2'si ise üreticiyi "T124 yazar" diyordu. İki paragraf çelişiyordu ve T124 **yazılı talimata uydu** — T123 kendi bacağını bağladı, T124 bağlamadı. Kural custody döneminde doğruydu (o çağda bu bacağın bildirimi `TRADE_OFFER_SENT_TO_BUYER` bot dispatch job'ından geliyordu); v3.0 bildirimi olayın üstüne taşıyıp alıcısını satıcıya çevirdi, kural bayat kaldı. Düzeltilmeseydi açık **yeniden üretilebilir** kalırdı. (b) **ÇİFT-PUSH VARSAYIM DEĞİL, ÖLÇÜLDÜ** — `PaymentReceivedRealtimeConsumer` **zaten** aynı `TransactionStatusChanged` payload'ını hardcoded `FromStatus` ile push ediyordu. Proje sahibi kararıyla (2026-08-22) push'un sahibi generic relay oldu ve hardcoded kaldıraç **silindi**; alternatif — relay'e `ToStatus == PAYMENT_RECEIVED` skip filtresi — reddedildi, çünkü relay'in tek sözleşmesi "producer'ın kararını sorgulamadan verbatim ilet" ve ilk per-status istisna onu kalıcı olarak delerdi. Net etki: bir kaldıraç **eksildi**. (c) **FAZLA ÖDEME KOLU DA KAPSANDI** — yayın "tam tutar" koluna değil `AdvanceStateMachineAsync`'in **içine** kondu: fazla ödeme durumu ilerletir, yani satıcı item'ı aynen borçludur ve teslimat saati ona karşı işler; yalnız temiz kola bağlı bir yayıncı o satıcıyı tam aynı sessiz yolla cezalandırırdı. E2E işareti çevrildi ve **tipten alıcıya yükseltildi**: yeni `getNotificationRecipients` ile alıcı kümesinin `[sellerId]`'e eşitliği iddia ediliyor — tipin varlığı yalnız "bir üretici ateşledi"yi kanıtlar, yanlış tarafa adreslenmiş bir bildirim satıcıyı açık kadar uyarısız bırakır ve tip-bazlı iddia yeşil kalırdı | backend-gap | T138, T133b-B4 (sınıf), T140 |
| 🟡 | T138-E2ENginxPathFilterGap 🆕 | **Yeni `happy-path UI` E2E leg'i `nginx/nginx.conf`'a bağımlı, ama `e2e-stack` paths-filter'ı `nginx/**`'ı içermiyor.** Ölçüm (T138 doğrulaması): `ci.yml:86-92` filtresi `e2e/**` · `sidecar-fake/**` · `docker-compose.e2e.yml` · `backend/**` · `frontend/**` · `.github/workflows/**` sayıyor; `nginx/**` yok. UI leg'i ise `docker-compose.e2e.yml:200-201` ile `./nginx/nginx.conf`'u `skinora-reverse-proxy`'ye mount ediyor ve Playwright'ı o origin'e (`baseUrl: http://localhost:8080`) bağlıyor — yani `/api/v1` veya `/hubs` proxy kuralını bozan bir PR **hiçbir E2E leg'i tetiklemez** ve ağ sessizce görüş bildirmez. `T136-ParityGuardsSkippedOnBackendPRs` ile **aynı sınıf**: bekçi var, tetikleyicisi eksik. Ucuz kapatma: `e2e-stack` filtresine `nginx/**` eklemek (job'ın `if`'i değişmez). Validator dalda uygulamadı çünkü paths-filter davranışsal bir CI değişikliği ve T138'in kabul kriterlerinde yok | ci-gap | T138, T136-ParityGuardsSkippedOnBackendPRs (sınıf) |
| ⚪ | T138-E2ENoArtifactUpload 🆕 | **On E2E leg'inin hiçbiri playwright artifact'ı yüklemiyor; kırmızı bir turun tanısı 200 satırlık compose log'una kalıyor.** `playwright.config.ts:24` CI'da `[['list'], ['html', { open: 'never' }]]` reporter'ını kuruyor, yani HTML rapor + trace üretiliyor ve leg bitince atılıyor; `ci.yml:750-758`'de yalnız `docker compose logs --tail=200` var, `actions/upload-artifact` adımı yok. T138 öncesinden var olan bir boşluk ama maruziyeti T138 8→10 leg'e çıkardı ve legler advisory olduğu için kırmızı bir leg zaten sessiz düşüyor. Ucuz kapatma: `if: failure()` koşullu `upload-artifact` adımı (`e2e/playwright-report`, `e2e/test-results`) | ci-gap | T138 |
| ⚪ | T133b-AdminDtoItemReturnedXmlDoc 🆕 | **`AdminTransactionDtos.cs:61-65` XML doc'u v3.0'da record'dan düşen `itemReturned` alanından hâlâ söz ediyor.** `ReleaseEmergencyHoldResponse` bugün `(Status, ReleasedAt, Action, bool? PaymentRefunded)` taşıyor; doc "CANCEL … itemReturned, paymentRefunded" diyor. Yalnız yorum, davranış etkisi yok. **Karar geçmişi:** T138 yapım turu bunu ölçtü, e2e görevinde production kaynağına dokunmamak için düzeltmedi ve *satır açılsın mı yoksa T135 N2 gibi "iş üretmediği için" mi geçilsin* sorusunu **bilerek doğrulama turuna bıraktı**. Doğrulama turu **satır açılsın** dedi ve gerekçesi turun kendi en değerli bulgusudur: `T136-E2EDeadItemReturnedAssertions`'ın altıncı maddesi tam olarak böyle bir yorumdu, sahipsiz kaldı ve sonunda **yanlış bir kapatma iddiası** üretti. Sahipsiz yorum ucuz değildir; bir sonraki backend turunun yolu üstünde | dead-doc | T138, T133b, T136-E2EDeadItemReturnedAssertions (ders) |
| ✅ | T58-AdminDisputeQueue | **ÇÖZÜLDÜ → WP5** — `GET /admin/disputes` (AD27) + resolve (AD29) + ESCALATED→RESOLVED_FOR_* | — |
| ✅ | T55-DormantThreshold | **ÇÖZÜLDÜ → WP14:** `dormant_account_value_threshold` dahil **19 zorunlu SystemSetting** (plan "21" stale — WP4a `price_deviation_threshold` + WP12 `timeout_warning_ratio` seed-default ile düştü) `Docs/DEPLOY_RUNBOOK.md §A`'da `SKINORA_SETTING_*` env listesi olarak belgelendi + `.env.example`'a eklendi. Owner kararı: seed-default DEĞİL → fail-fast bilinçli güvenlik korundu (06 §8.9 iş-kritik değerler). | — |
| ✅ | T69-DispatchCaller | **ÇÖZÜLDÜ → T106a** (Escrow Trade-Offer Dispatch Engine): `SelectAsync` çağrılıyor + `EscrowBotId` persist + `ActiveEscrowCount` ITEM_ESCROWED'da artar + escrow/delivery/refund dispatch | — |
| ✅ | T69-BotRecoveryStateMachine | **ÇÖZÜLDÜ → T103b-2** — `BotRecoveryItem` recovery domaini + AD10 canlı `RestrictionReason`/`FailoverStatus`/`RecoveryTransactionCount` | — |
| ✅ | T103b-2/-3 | **ÇÖZÜLDÜ → T103b-2 (birleşik)** — recovery queue domain + MANAGE_STEAM_RECOVERY enforcement + emanet item listesi + otomatik EMERGENCY_HOLD | — |
| ✅ | SWEEP-dispatcher | **ÇÖZÜLDÜ → WP3** — `SweepQueueJob` PENDING `SWEEP` satırı üretir (`OutgoingTransferJobsRegistrar` recurring kaydeder, registrar `IHostedService` olarak `TransactionsModule`'de) + dispatcher `OutboundTypes`'ında SWEEP var | — |
| ✅ | T81-PriceConsumerWireup | **ÇÖZÜLDÜ → WP1** — kalan yarı bir kod boşluğu değil, **görünürlük** boşluğuydu ve öyle kapatıldı: `PriceDeviationConfigDiagnosticHook` her açılışta kuralın ateşleyip ateşleyemeyeceğini basıyor (`ACTIVE` / `INEFFECTIVE` + hangi yarının eksik olduğu). Fail-open bir deploy hâlâ geçerli bir tercih — ama artık **sessiz** değil | — |
| 🟡 | StubPayoutVerifier | Üretim payout doğrulayıcı yok (fail-closed, manuel admin) | Otomatik on-chain payout doğrulama |
| 🟡 | P2P-SettlementTiering | **F7/yeni** — herkes 8 gün bekliyor; itibarlı satıcılar için sürenin kısaltılması satıcı deneyimini belirgin iyileştirir (§9) | — |
| 🟡 | P2P-HotWalletPolicyReview | **F7/yeni** — para artık işlem başına 8 gün platformda; sıcak cüzdan eşiği ve soğuk cüzdan aktarma sıklığı yeni profile göre hesaplanmalı (§9) | — |
| 🟡 | P2P-DeliveryPollingJob | **F7/yeni** — sürekli teslimat taraması yok; pasif alıcıda satıcı süre sonuna kadar bekliyor (§9) | — |
| ✅ | steam-sidecar-stubs | **ÇÖZÜLDÜ → WP6** — sidecar `GET /api/trade-hold/:steamId` (`GetTradeHoldDurations`) + `SidecarTradeHoldChecker` (U17) + `SidecarMobileAuthenticatorCheck` (A7); envanter reader zaten gerçekti | — |
| ✅ | item-refund-consumers | **ÇÖZÜLDÜ → WP2** — `PaymentRefundToBuyerConsumer` `BUYER_REFUND` satırı üretir; DI'da kayıtlı + iki katmanlı idempotency + `UQ_BlockchainTransactions_BuyerRefund_TransactionId` | — |
| ✅ | T50-OutageFreezeCallers | **ÇÖZÜLDÜ → WP1** — `PlatformHealthProbeJob` kesinti kenarında `FreezeManyAsync`, toparlanma kenarında `ResumeManyAsync` çağırıyor (STEAM→`STEAM_OUTAGE`, BLOCKCHAIN→`BLOCKCHAIN_DEGRADATION`). WP16'nın alert-only kararı owner tarafından 2026-08-24'te geri alındı: 02 §3.3'ün vaadi buydu ve aksi hâlde kullanıcı platformun kendi arızası yüzünden timeout yiyordu | — |
| ✅ | T56-MultiAccountRetroScan | **ÇÖZÜLDÜ → WP4b** — günlük `MultiAccountRetroScanJob` cüzdanlı aktif kullanıcıları retroaktif tarar (`IMultiAccountDetector` yeniden çağrılır) | — |
| ✅ | T61-SteamTransitionRealtimePush | **ÇÖZÜLDÜ → WP9** — `SteamWebhookHandler` geçişlerde `TransactionStatusChangedEvent`'i outbox'a yayınlar → `TransactionStatusChangedRealtimeConsumer` SignalR push eder | — |
| ✅ | T38-AdminFlagAlert-FlagId | **ÇÖZÜLDÜ → WP8** — `Notification.FlagId` kolonu + filtered index (migration `WP8_AddNotificationFlagId`); `FraudFlagCreatedAdminNotificationConsumer` alanı dolduruyor | — |
| ✅ | T30-TosVersionReprompt | **ÇÖZÜLDÜ → WP11** — CurrentUserDto += `tosAcceptedVersion`, `tos/accept` versiyon-upgrade'e izin verir (409 yalnız aynı versiyonda), FE `TosRepromptGate` versiyon uyuşmazlığında re-prompt | — |
| ✅ | T87-K1 | **ÇÖZÜLDÜ → WP11** — callback `/auth/refresh`→token store + `acceptTos` wire-up + 401 refresh interceptor; MA recheck /auth/me ile (A7 trade-URL akışına ait) | — |
| ✅ | FE-admin-signalr-subscription | **ÇÖZÜLDÜ → WP9** — `RealtimeProvider.tsx` üç admin event'ine de abone (`onAdminBotStatusChanged` / `onAdminReconciliationMismatch` / `onAdminHotWalletThresholdBreached`) | — |
| ✅ | TradeOfferMonitor-hotadd-T69 | **ÇÖZÜLDÜ → WP6 (resolved-by-design)** — statik pool (`BotManager` dinamik-add yok); idempotent `attachToSession` hook'u T69 dinamik pool için hazır + test edilmiş | — (statik pool'da sorun yok) |
| ✅ | T33-SuccessRate-FractionVsPercent | **ÇÖZÜLDÜ → WP17 (no-op)** — kod (`HasPrecision(5,4)`) + 06 §3.1 + 07 örnekleri zaten fraction (0..1) üzerinde hizalı; detay §7 satırı | — |
| ✅ | T107 | **ÇÖZÜLDÜ → F6** — E2E happy path (harness + smoke + UI), bağımsız validator PASS (PR #198); F6 Gate Check ✓ PASS | — |

---

## 1. T69 — Bot health / failover / recovery pipeline

| Önc. | ID | Açıklama | Tip | Hedef | Kaynak |
|---|---|---|---|---|---|
| ✅ | T69-DispatchCaller | **ÇÖZÜLDÜ → T106a** (Escrow Trade-Offer Dispatch Engine): `SelectAsync` çağrılıyor + `EscrowBotId` persist + `ActiveEscrowCount` ITEM_ESCROWED'da artar + sidecar `selectBot(botAccountName)` hint'i onurlandırır (round-robin yalnız fallback) | done | — | T106a |
| ✅ | T69-BotRecoveryStateMachine | **ÇÖZÜLDÜ → T103b-2:** `BotRecoveryItem` domaini + AD10 canlı alanlar (`AdminSteamBotQueryService` türetir) | done | — | T103b-2 |
| ✅ | ~~T69-K4~~ | **KONUSUZ KALDI (F7 — T132, 2026-08-19).** `AdminBotStatusChanged` olayının kendisi silindi: backend'de payload, publisher portu ve event sabiti kalmadı, 07 §11.2 RT2 admin kataloğu üçten ikiye indi. Daraltılacak bir `Clients.All` yayını yok. **Ölçüm (F7 gate sonrası, 2026-08-22):** `grep -rn "AdminBotStatusChanged" backend/src frontend/src sidecar-steam/src` → **0 eşleşme**. Satır tarihsel izlenebilirlik için duruyor | k-note | — | T69 K4 → T132 |
| ✅ | FE-RecoveryQueue-T69 | **ÇÖZÜLDÜ → T103b-2:** `RecoveryQueuePanel` canlı AD25 satırları + Manual Recovery/Resolve/Not aksiyonları (AD26) | done | — | T103b-2 |
| ✅ | FE-SteamAccountCard-EscrowList-T69 | **ÇÖZÜLDÜ → T103b-2:** emanet item'lar per-bot recovery kuyruğunda listelenir | done | — | T103b-2 |
| ✅ | FE-admin-ts-RecoveryFields-T69 | **ÇÖZÜLDÜ → T103b-2:** AD10 alanları canlı; failover banner `RESTRICTED_NEW_TXN_DIVERTED` ile görünür | done | — | T103b-2 |
| ✅ | ~~T68-K1~~ | **KONUSUZ KALDI (F7 — T132/T133, 2026-08-19).** Bildirime dönüştürülecek bot lifecycle olayı kalmadı: sidecar-steam salt-okunur proxy'dir ve hiçbir Steam hesabı kimlik bilgisi taşımaz (T133), bot oturumu diye bir şey yoktur. Kaydı taşıyacak `ADMIN_STEAM_BOT_ISSUE` bildirim tipi de v3.0'da kaldırıldı (06 §2.13). **Ölçüm (2026-08-22):** `BOT_SESSION_FAILED` AuditAction `Skinora.Shared/Enums/` altında **yok**; `ADMIN_STEAM_BOT_ISSUE` yalnız iki *kaldırma yorumunda* geçiyor. Satır tarihsel izlenebilirlik için duruyor | backend-gap | — | T68 K1 → T132/T133 |
| ✅ | ~~T64-BotWebhookHandler~~ | **KONUSUZ KALDI (F7 — T132/T133, 2026-08-19).** Handler'ın dinleyeceği inbound Steam webhook yüzeyi silindi (`SteamWebhooksController` T117'de, `Webhook.SteamSharedSecret` T132'de) ve yayınlayan sidecar yarısı da kapandı (T133) — Steam artık yalnız **outbound** okunuyor. **Ölçüm (2026-08-22):** `grep -rniE "session_failed|removed_from_pool|bot-events|bot\.session" backend/src sidecar-steam/src` → **0 eşleşme**. Satır tarihsel izlenebilirlik için duruyor | backend-gap | — | T64 → T132/T133 |
| ✅ | TradeOfferMonitor-hotadd-T69 🆕 | **ÇÖZÜLDÜ → WP6 (resolved-by-design)** — statik pool, dinamik-add yolu yok; idempotent `attachToSession` hook'u T69 için hazır + test edilmiş; doc-comment WP6 doğrulamasıyla güncellendi | k-note | T69 | `sidecar-steam/src/trade/TradeOfferMonitor.ts` |

## 2. T103b — Steam hesapları backend tamamlama (S18)

| Önc. | ID | Açıklama | Tip | Hedef | Kaynak |
|---|---|---|---|---|---|
| ✅ | T103b | **ÇÖZÜLDÜ → T103b-2 (birleşik impl, 2026-06-13):** emanet item listesi + Recovery Queue satır verisi + `MANAGE_STEAM_RECOVERY` enforcement + otomatik EMERGENCY_HOLD | done | — | T103b-2_REPORT |
| ✅ | T103-K4 | **ÇÖZÜLDÜ → WP17:** ölü TR-sabit `warningMessage` alanı AD10'dan kaldırıldı (FE banner zaten `status`'ten client-derive) | backend-gap | — | T103 K4 |
| ✅ | T103b-2/-3 | **ÇÖZÜLDÜ → T103b-2 (birleşik)** — recovery queue domain + MANAGE_STEAM_RECOVERY enforcement + emanet item listesi + otomatik EMERGENCY_HOLD | — |

## 3. F6 — Uçtan uca testler (T107–T114)

| Önc. | ID | Açıklama | Tip | Kaynak |
|---|---|---|---|---|
| ✅ | T107 | **ÇÖZÜLDÜ** — E2E happy path (smoke+UI); bağımsız validator PASS (PR #198) | task | IMPLEMENTATION_STATUS F6 |
| ✅ | T108 | **ÇÖZÜLDÜ** — E2E iptal senaryoları (satıcı/alıcı/admin); bağımsız validator PASS (PR #200) | task | F6 |
| ✅ | T109 | **ÇÖZÜLDÜ** — E2E timeout senaryoları (4 faz); bağımsız validator PASS (PR #201) | task | F6 |
| ✅ | T110 | **ÇÖZÜLDÜ** — E2E ödeme edge case'leri (§5.1–§5.5 + §5.3a) firsthand 6/6; bağımsız validator PASS 2026-06-23 (PR #202). Bulgu K1 (refund-adresi doc çelişkisi) → §6 `T110-RefundAddressDocConflict` (post-MVP) | task | F6 |
| ✅ | T111 | **ÇÖZÜLDÜ** — E2E fraud/flag senaryoları (PRICE_DEVIATION dahil); bağımsız validator PASS (PR #204). Bulgu K1 (admin-flags cross-doc çelişkisi) → §6 `T111-AdminFlagsSurfaceDocConflict` (post-MVP, F6 gate forward) | task | F6 |
| ✅ | T112 | **ÇÖZÜLDÜ** — E2E emergency hold (hold/resume/cancel + ITEM_DELIVERED guard); bağımsız validator PASS (PR #205) | task | F6 |
| ✅ | T113 | **ÇÖZÜLDÜ** — E2E admin akışları (6 akış + AD17); bağımsız validator PASS (PR #206). Bulgu B1 (`UQ_AdminRoles_Name` re-create 500) → §4 `T113-AdminRoleNameReuse500` (F6 gate forward) | task | F6 |
| ✅ | T114 | **ÇÖZÜLDÜ** — E2E downtime/bakım (3 senaryo); bağımsız validator PASS (PR #207) | task | F6 |
| ✅ | T87-K1 | **ÇÖZÜLDÜ → WP11** — callback refresh→token store + ToS-accept wire-up + 401 refresh interceptor; check-authenticator owner-kararıyla /auth/me recheck'ine bağlandı (A7 trade-URL/U17 akışına ait, login'de değil) | k-note | T87 K1-K3 / T85 K1 |

## 4. T-future — Backend orkestrasyon (caller/consumer wire-up)

### Orta öncelik

| Önc. | ID | Açıklama | Tip | Kaynak |
|---|---|---|---|---|
| ✅ | Session-LogoutDoesNotRevokeRefreshToken | **ÇÖZÜLDÜ → F7a ([#282](https://github.com/turkerurganci/Skinora/pull/282))** — **A8'in ilk üretim çağıranı**. Yeni `@/lib/auth/signOut` önce `POST /auth/logout` ile refresh token'ı sunucuda iptal ediyor, sonra yerel token'ı temizliyor; **dört** çağıran da bu tek kapıdan geçiyor (ana başlık · admin başlığı · askıya alınmış başlık · hesap kapatma/silme). Sonuncusu kendi başına ayrı bir açıktı: **hesabını silen** kullanıcının refresh token'ı da iptal edilmiyordu. **Sunucu çağrısı bilerek best-effort:** ağ koptuğunda, token süresi dolduğunda veya rate limit reddettiğinde yerel oturum yine kapanıyor — kullanıcıyı çıkmak istediği oturumda mahsur bırakmak, kendi kendine sönecek bir refresh token'dan kötüdür. `client.ts`'teki 401 yolu bilerek **değişmedi**: orada oturum zaten ölü. 7 yeni test mutasyonla doğrulandı (A8 çağrısı ve çıkış yönlendirmesi kaldırılınca 3 test kırıldı); biri sıralamayı sabitliyor — yerel temizlik sunucu cevabından **sonra**, yoksa A8 tokensiz giderdi. **Eski açıklama:** **"Çıkış" sunucudaki oturumu SONLANDIRMIYOR — A8'in çağıranı yok.** `AuthController.cs:265` **A8** `POST /auth/logout` (07 §4.9) refresh token'ı `RevokeAsync` ile iptal edip cookie'yi siliyor; **frontend'de `auth/logout` dizgisi hiç geçmiyor** (ölçüldü) — yani uçun üretim çağıranı yok. Arayüzdeki "Sign out" yalnız `useAuthStore.logout()` çağırıyor ve o da (`auth-store.ts:50`) **sadece localStorage'daki access token'ı siliyor**. **Sonuç:** çıkıştan sonra HttpOnly refresh cookie'si sunucuda **geçerli kalıyor** (`JWT_REFRESH_TOKEN_EXPIRY_DAYS=7`); aynı tarayıcıdan `POST /auth/refresh` yeni bir access token üretebilir. Kullanıcı normal akışta geri giriş yapmış olmuyor (`AuthInitializer` boot'ta refresh çağırmıyor, yalnız localStorage'ı okuyor) — **ama oturum sonlanmadı**; ortak/paylaşılan bir bilgisayarda fark budur. Aynı boşluk hesap **kapatma/silme** sonrasında da var (`AccountManagementSection.tsx:179`→`logout()`), yani hesabını silen kullanıcının refresh token'ı da iptal edilmiyor. Kardeş satır: `Session-NoSignOutInMainHeader` (§5) — o düzeltilse bile bu satır kendi başına açık kalır. | caller yok | ölçüm 2026-08-24; `AuthController.cs:265`, `auth-store.ts:50`, `AdminHeader.tsx:68` |
| ✅ | ~~AdminUserActivity-RefundedTerminal~~ | **ÇÖZÜLDÜ → T118 (kod) + F7-sonrası parite turu (satır).** `AdminUserActivityProvider._terminalStates`'e `REFUNDED` T118'de eklendi (`git log -S` → `549e401`; `phase/F6-pass` ağacında yoktu) ama **backlog satırı hiç çevrilmedi** — dört tur boyunca çözülmüş bir iş açık göründü. Satır kapatılırken kardeş listeler de tarandı ve **kayıtsız üç sapma** bulundu (aşağıdaki `T133a-ActiveCounterRefunded` ile birlikte beş yüklem): `FraudFlagAdminQueryService` AD2 + AD3 ve `FraudFlagService.ApplyEmergencyHoldCascadeAsync`. Hepsi tek kaynağa (`TransactionStatusSets.Terminal`) bağlandı ve türetilmiş bir guard yazıldı | backend-gap | T117 doğrulaması → T118, F7-sonrası parite turu |
| ✅ | T58-AdminDisputeQueue 🆕 | **ÇÖZÜLDÜ → WP5** — `AdminDisputeService` (AD27/28/29) + `RESOLVED_FOR_*`/`REFUNDED` + audit/notify; FE `/admin/disputes` | backend-gap | `T58_REPORT.md:178` |
| ✅ | SWEEP-dispatcher | **ÇÖZÜLDÜ → WP3** — `SweepQueueJob` (recurring, `OutgoingTransferJobsRegistrar` → `IHostedService` `TransactionsModule.cs`) settle olmuş işlemler için PENDING `SWEEP` `BlockchainTransaction` satırı üretir (`UQ_BlockchainTransactions_Sweep_TransactionId` ile tekil); `OutgoingTransferDispatchJob.OutboundTypes` SWEEP'i kapsıyor; `SweepQueueJobTests` | backend-gap | T73/T76/T77 K |
| ✅ | T81-PriceConsumerWireup | **ÇÖZÜLDÜ → WP1.** Kod yolu WP4a'da zaten tamdı; açık kalan kısım iki config'in (`SteamMarket:Provider` + `price_deviation_threshold`) **sessizce** fail-open kalabilmesiydi. WP1 bunu ölçülebilir yaptı: `PriceDeviationConfigDiagnosticHook` (`Program.cs`, `SettingsBootstrapHook`'tan hemen sonra — yani env-var hydration'dan **sonraki** eşiği okur) açılışta ya `PRICE_DEVIATION rule ACTIVE — ...` ya da `PRICE_DEVIATION rule INEFFECTIVE — it will never flag a transaction ...` basıyor ve **hangi yarının** eksik olduğunu (fiyat kaynağı mı, eşik mi) ayrı ayrı söylüyor. Karar bilerek değiştirilmedi — fail-open bir deploy geçerlidir (`DEPLOY_RUNBOOK §C.1` kontrolü) — ve **teşhis asla host'u düşürmez** (DB okunamazsa uyarıp devam eder). Verdict saf bir yüklem olarak ayrıldı (`CanRuleFire`) ve 12 testle sabitlendi; `DEPLOY_RUNBOOK §C.1`'e doğrulama komutu eklendi. **Bu, `ForwardedHeadersNotRegistered` dersinin fraud config'e uygulanmış hâli:** varsayılan duruşta etkisiz kalan bir kontrol bunu açılışta söylemeli | backend-gap | T81 K1 → WP1 |
| ✅ | T133b-PaymentMonitorUnarmed | **ÇÖZÜLDÜ → T139** — aktif ödeme izleyicisinin üç halkası da bağlandı: **kurma** (`ACCEPTED → SELLER_CONFIRMED` geçişiyle aynı `SaveChanges` içinde `PaymentMonitorStartRequestedEvent` → `PaymentMonitorStartDispatcher` → `POST /api/monitor/start`), **yeniden kurma** (`EnsurePaymentMonitorJob`, cron `* * * * *` — backend restart + sidecar restart + düşen outbox teslimini tek mekanizmayla kapatır) ve **durdurma** (iptal devrinde aktif izleyici post-cancel'dan önce durdurulur; pencere kapandığında — sweep `CONFIRMED` veya terminal statü — `POST /api/monitor/stop` + `MonitoringStatus = STOPPED`). Ölçüm satırın yazdığından geniş çıktı: durdurma halkası da sahipsizdi, yani mutlu yolda satır sonsuza kadar `ACTIVE` kalıyor ve `ReconciliationService` kapsamı sınırsız büyüyordu. Yaşam döngüsü 08 §3.4'e, `ACTIVE`in iki anlamı 06 §2.16'ya yazıldı; `DEPLOY_RUNBOOK` §G.4'teki elle `curl` notu kurulum **doğrulama** notuna çevrildi | backend-gap | T139 |
| 🟡 | StubPayoutVerifier | `IPayoutVerifier`=stub (her zaman `UnableToVerify`→manuel admin) | backend-gap | T60 K1, `StubPayoutVerifier` |
| ✅ | steam-sidecar-stubs | **ÇÖZÜLDÜ → WP6** — sidecar `GET /api/trade-hold/:steamId` (`GetTradeHoldDurations`, 08 §2.2) + paylaşılan `ISteamTradeHoldProbe`/`HttpSteamTradeHoldClient` + `SidecarTradeHoldChecker` (U17) + `SidecarMobileAuthenticatorCheck` (A7); fail-closed; envanter reader zaten gerçekti (`SidecarSteamInventoryReader`) | backend-gap | T35/T31/T58 K |
| ✅ | item-refund-consumers | **ÇÖZÜLDÜ → WP2** — `PaymentRefundToBuyerConsumer` `PaymentRefundToBuyerRequestedEvent`'i tüketip `BUYER_REFUND` `BlockchainTransaction` satırı üretir (DI: `TransactionsModule.cs`; iki katmanlı idempotency + `UQ_BlockchainTransactions_BuyerRefund_TransactionId`; `PaymentRefundToBuyerConsumerTests`). Diğer 4 iade yolu zaten inline/T106a bağlıydı | backend-gap | T49/T51/T71 K |
| ✅ | T50-OutageFreezeCallers | **ÇÖZÜLDÜ → WP1** (owner kararı 2026-08-24 — WP16'nın alert-only kararını geri alır). `PlatformHealthProbeJob` artık kesinti kenarında `FreezeManyAsync`, toparlanma kenarında `ResumeManyAsync` çağırıyor; eşleme `STEAM`→`STEAM_OUTAGE`, `BLOCKCHAIN`→`BLOCKCHAIN_DEGRADATION` (`TimeoutFreezeReasonScopes` bu iki nedeni zaten farklı durum kümelerine uyguluyor). **Gerekçe:** 02 §3.3 tespitin otomatik olduğunu ve timeout'ların donduğunu söylüyordu; alert-only hâlde bir Steam kesintisinde kullanıcı **platformun kendi arızası** yüzünden timeout yiyor ve bunu ancak uyanık bir admin önlüyordu. **Yanlış pozitif riski yeni bir ayar gerektirmedi:** `HealthProbe:FailureThreshold` ardışık-hata debounce'ı zaten vardı ve freeze kenar-tetiklemeli (sürekli kesinti bir kez dondurur, her poll'de değil). **Sıralama bilinçli:** freeze/resume alarm birimi **stage edilmeden önce** koşuyor — `FreezeManyAsync` aynı scoped `AppDbContext` üzerinde kendi `SaveChanges`'ini çağırıyor ve tersi sıralama alarm satırlarını erken flush edip revert-on-failure yolunu bozardı; ikisi de idempotent olduğu için revert + yeniden tespit çift uygulamaz. **Bozulmadan bırakılan:** alarm hâlâ birincil garanti — bulk freeze patlarsa yutuluyor, alarm yine gidiyor ve audit'e `timeoutsAffected: null` yazılıyor (admin manuel WP7 yolunu kullanabilsin). 4 yeni test **mutasyonla ölçüldü**: ilk turda ikisi mutasyon altında da geçiyordu (iddia ettiklerinden azını ölçüyorlardı) → güçlendirildi, şimdi dördü de kırılıyor | backend-gap | `T50_REPORT.md:124-125` → WP1 |
| ✅ | T56-MultiAccountRetroScan 🆕 | **ÇÖZÜLDÜ → WP4b** — günlük retro-scan Hangfire job (`MultiAccountRetroScanJob`, `AutoUnsuspendJob` deseni) | backend-gap | `T56_REPORT.md:150` |
| ✅ | ~~T113-AdminRoleNameReuse500~~ | **ÇÖZÜLDÜ (2026-08-22, backlog §4 turu).** Servis ön-kontrolü `IgnoreQueryFilters()` ile soft-delete filtresini atlıyor → silinen bir rolün adı yeniden kullanılmaya çalışıldığında **500 yerine temiz 409 `ROLE_NAME_EXISTS`**. `CreateAsync` ve `UpdateAsync` (rename) kollarının ikisi de düzeltildi. **T24'ün kasıtlı tasarımı korundu** — filtresiz `UQ_AdminRoles_Name` yerinde kaldı (silinen rolün adı rezerve kalır ki audit satırları tek bir anlama işaret etsin); önerilen ikinci seçenek (filtered index) bir karar değişikliği olurdu, o yüzden seçilmedi. `AdminRoleErrorCodes.RoleNameExists`'in XML doc'u da düzeltildi: "**aktif** bir AdminRole'e karşı çakışma" diyordu — kodun yaptığı tam olarak buydu ve 500'ün sebebi de oydu. İki regresyon testi (create + rename), ayırt edicilikleri **mutasyonla ölçüldü** (`IgnoreQueryFilters` kaldırılınca ikisi de kırıldı) | backend-gap | `T113_REPORT.md:75` → backlog §4 turu |
| ✅ | T61-SteamTransitionRealtimePush | **ÇÖZÜLDÜ → WP9** — `SteamWebhookHandler.PublishStatusChangedAsync` status flip'iyle aynı unit-of-work'te outbox'a `TransactionStatusChangedEvent` yazar (escrow + delivery geçişleri); `TransactionStatusChangedRealtimeConsumer` bunu SignalR'a push eder | backend-gap | `T61_REPORT.md:146` (K2) |
| ✅ | T38-AdminFlagAlert-FlagId | **ÇÖZÜLDÜ → WP8** — `Notification.FlagId` (`Guid?`) kolonu + `IX_Notifications_FlagId` filtered index + migration `20260617194020_WP8_AddNotificationFlagId`; `FraudFlagCreatedAdminNotificationConsumer` `FraudFlagId`'yi geçiriyor → admin flag-link hedefi çalışıyor | backend-gap | `T38_REPORT.md:20`, `NotificationTargetMapper.cs:25` |
| ✅ | T30-TosVersionReprompt 🆕 | **ÇÖZÜLDÜ → WP11** — `tosAcceptedVersion` /auth/me'de sunulur + `tos/accept` versiyon-upgrade (409 yalnız aynı versiyon) + FE `TosRepromptGate` | backend-gap | `T30_REPORT.md:155` |

### Düşük öncelik

| ID | Açıklama | Kaynak |
|---|---|---|
| payout-completed-consumer | `PayoutCompletedEvent`→COMPLETED geçişi yok (T73 yalnız finality flush) | T73 K4 |
| ✅ | ~~T140-OutboxDispatchOrderNonDeterministic~~ | **ÇÖZÜLDÜ (2026-08-22, backlog §4 turu).** `OutboxMessages`'a `Sequence` kolonu eklendi (migration `OutboxSequenceOrdering`, mevcut satırlar için `DEFAULT 0`), `OutboxService` her satıra üretici sırasına göre 1'den başlayan ordinal yazıyor, dispatcher `OrderBy(CreatedAt).ThenBy(Sequence)` ile sıralıyor ve filtered index `(Status, CreatedAt, Sequence)` olarak genişletildi (ORDER BY index'ten karşılansın, ek sort olmasın). **IDENTITY kolonu KULLANILMADI ve sebebi ölçüldü:** SQLite yalnız INTEGER PRIMARY KEY için değer üretir, bu tablonun anahtarı ise çağıran tarafından verilen `Id`; IDENTITY seçilseydi SQLite tabanlı testlerde her satır sessizce `0` alır ve belirsizlik tam da testlerin koştuğu yerde geri gelirdi. Üretici ordinali her sağlayıcıda aynı davranır ve belirsiz olan tek durumu — aynı `CreatedAt`'i paylaşan, yani aynı unit-of-work'ten gelen satırları — ayırır. **DÜRÜSTLÜK NOTU:** eklenen testin uçtan uca sıra iddiası **ayırt edici DEĞİL** ve bu varsayılmadı, **ölçüldü** — dispatcher'dan `.ThenBy(Sequence)` silindiğinde test yine geçti, çünkü SQLite bağlı satırları rastlantısal olarak ekleme sırasında döndürüyor. Yük taşıyan iddia `Sequence` damgasıdır (`Sequence = ++_sequence` silindiğinde test kırılıyor); sıra iddiası destekleyici olarak bırakıldı ve testin içine bu sınırı yazan bir not kondu | T140 validate → backlog §4 turu |
| payout-retry-consumer | `RETRY_SCHEDULED` set ediliyor ama broadcast retry consumer'ı yok | T60 K2/K3 |
| calculator-caller-wiring | `CalculateRefund`/`SellerPayout`/`IRefundDecisionService` tüketilmiyor; gas/refund ratios deferred; AD7 gas split=0 | T52/T53/T63 K |
| reputation-aggregator-trigger | **✅ WP15** — COMPLETED/CANCELLED_TIMEOUT/Steam-cancel recompute+cooldown bağlandı; ön-koşul **TransactionHistory yazımı** (06 §3.6, hiç yazılmıyordu) tüm geçişlere eklendi (timeout sorumluluk-atfı buna bağımlıydı, sessizce kopuktu). | T43/T44/T68 K |
| blockchain-monitor-consumers | `payment-confirmed`→PAYMENT_RECEIVED **bağlı** (finality webhook); kalan: mempool `PaymentDetected` consumer (by-design opsiyonel) + DROPPED metrik → WP16 | T61/T71/T72 K |
| flagged-allocation-detail | **payment-address ✅ ÇÖZÜLDÜ → WP4b** (`FraudFlagService.ApproveAsync` post-commit eager `AllocateAsync`, best-effort); **tx-detail payment/payout/refund/dispute alt-DTO'ları null kısmı → WP13** | T70/T46 K |
| emergency-hold-callers | **Bulk/per-user hold/release/cancel bağlı** (T59/T100/T103b-2, stale); RowVersion guard mevcut; post-payment refund tetik **✅ WP2**, dispute-queue surfacing **✅ WP5** (AD27 kuyruğu). Kalan kalem yok. | T50/T51/T58 K |
| fraud-acceptance-gate | **✅ ÇÖZÜLDÜ** — accept-gate → WP4a; background scan (retro-scan) + note max-length → WP4b. (Sinyal üreticileri PRICE_DEVIATION T45/multi-account T56 zaten mevcut) | T54/T56 K |
| energy-gas-token-config | **Kısmen ✅ → WP10:** gas fee config'lenebilir (`TRANSFER_FEE_LIMIT_SUN` — hardcoded 100 TRX kaldırıldı) + HD address cache (per-index `derive` memoization, private-key cache'lenmez) çözüldü. **By-design/kapsam-dışı:** USDC/USDT 1:1 + hardcoded 6-decimal (by-design TRC-20, §3); tek-sweeper/T74 multi-sweeper (post-MVP ölçek, §3); 20-blok finality **zaten var** (`minConfirmations=20`, stale not). | T72-T76 K |
| setting-sidecar-propagation | **WP14 ✅ ÇÖZÜLDÜ:** (1) **cron** (`reconciliation.schedule_cron`, `hot_wallet.monitor_cron`) admin değişiminde Hangfire job **restart'sız re-register** olur (`ISettingChangePropagator`→`CronSettingChangePropagator`→registrar `ICronJobReconfigurer.Reconfigure`; geçersiz cron → 400 validator). (2) **sidecar cadence/sweep** (`monitoring_post_cancel_*`, `blockchain.sweep_*`) owner kararı = **env parity + runbook** (runtime push/pull post-MVP, T74 K1/T96): `Docs/DEPLOY_RUNBOOK.md §D` + `.env.example` — sidecar env otoriter, değişim sidecar restart gerektirir, backend SystemSetting kopyası admin-görünür. Backend gas/retry zaten her run'da taze okunuyor (propagasyon gerektirmiyor). | T74/T75/T76/T77 K |
| admin-alert-consumers | `RefundBlockedAdminAlert`/`TransferDispatchFailed`/stranded-delegation/STOPPED/spam-token/payout-issue alert kanal consumer'ları yok | T53/T60/T72-T77 K |
| misc-monitoring-probes | Steam/Telegram health probe, Redis webhook idempotency, in-app dispatch, timeout reschedule, ItemEscrowed publish, DROPPED metric | T64/T68/T78/T79 K |
| signalr-scaling | SignalR in-memory (multi-instance Redis backplane tek-satır DI); CountdownSync/handler/group-failure obs. | T61/T62/T96 K |
| maintenance-toggle | `PublishMaintenanceStatusChangedAsync` wired ama hiç tetiklenmiyor; admin maintenance-toggle endpoint + setting yok | T62/T84 K |
| uptime-cache-scaleout | `platformUptimePercent` config sabiti (heartbeat tablo/job yok); `IMemoryCache` (Redis scale-out yok); SET invalidation hook yok | T63a/T86 K |
| tron-resilience | **✅ ÇÖZÜLDÜ → WP10** — TronGrid 429/403 → ikincil `TRON_API_KEY` anında failover + sınırlı poll-dostu backoff (okuma yolu, `TronGridClient`); **event_index dedup** txid+gerçek-on-chain-log-index'e yükseltildi (06 §3.8 `(TxHash,EventIndex)` UNIQUE + migration; sidecar `gettransactioninfobyid` `log[]`'tan çözer — trc20 list endpoint event_index vermiyor). Büyüme backup node (Ankr/GetBlock) MVP-dışı (§3). | T71/T72 K |
| sanctions-expansion | `SanctionedAddress` MANUAL-only (OFAC/EU/UN feed auto-sync yok); Network TRC-20 sabit; AD22 reason UI | T82/T34 K |
| vpn-fraud | `VpnDetection:Enabled` default off; fraud-modül entegrasyonu + datacenter ASN / commercial VPN listeleri | T83 K |
| misc-user-features | **WP12 ✅ ÇÖZÜLDÜ:** per-tx refund override (accept artık profil `DefaultRefundAddress`/cooldown'ı mutate etmiyor — snapshot-only, 02 §12.2 + 04 §7.3 "profil adresi etkilenmez"); trade-offer URL DTO (`steamTradeOfferUrl` cross-module port, 04 §7.3); delete atomicity (`AccountLifecycleService.DeleteAsync` 3 adım `BeginTransactionAsync` ile atomik); OPEN_LINK race (→ T46 satırı). brute-force lock ✅ WP11. **Kalan:** multi-account user UI | T90/T29/T36/T46 K |
| timeout-warning-setting | **WP12 ✅ ÇÖZÜLDÜ:** kopyalı `DefaultTimeoutWarningPercent`=75 const'ları kaldırıldı → read-path (Detail+List) mevcut `timeout_warning_ratio` SystemSetting'ini okur (oran×100; seeded default 0.75 → mandatory değil); `accept_timeout_minutes` env-mandatory **by-design doğrulandı** (06 §8.9 fail-fast, "—" default) | T83a/T45 K |
| T61-AdminHubJoinBypass 🆕 | `TransactionsHub.JoinTransaction` admin bypass yok (yalnız seller/buyer) | `T61_REPORT.md:147` (K3) |
| T54-FlaggedApproveNoTimeoutJob 🆕 | **✅ ÇÖZÜLDÜ-by-design → WP4b** — 05 §4.4 + 06 §3.5:650: accept-deadline'lar **bilinçli olarak poller-driven** (yalnız ITEM_ESCROWED per-tx job alır); `DeadlineScannerJob` `ApproveAsync`'in setlediği `AcceptDeadline`'ı zaten enforce ediyor (regresyon testi eklendi). Yeni per-tx job spec'i ihlal ederdi → kurulmadı | `T54_REPORT.md:234` |
| T54-FraudNoteNoMaxLength 🆕 | **✅ ÇÖZÜLDÜ → WP4b** — `ApproveAsync`/`RejectAsync` 2000 char (kolon genişliği) validasyonu → 400 `VALIDATION_ERROR`; rapordaki "1000" stale | `T54_REPORT.md:194,219` |
| T58-canDisputeEnvelopeBit 🆕 | **✅ ÇÖZÜLDÜ → WP5** — `availableActions.disputableTypes: DisputeType[]` eklendi (per-type), `canDispute` korunur (07 §7.5) | `T58_REPORT.md:203` |
| T46-OpenLinkConcurrentAcceptRace 🆕 | **WP12 ✅ ÇÖZÜLDÜ:** `AcceptAsync` SaveChanges `catch(DbUpdateConcurrencyException)` → no-tracking status re-query → ACCEPTED ise 409 ALREADY_ACCEPTED, başka state ise re-throw (maskeleme yok). RowVersion optimistic concurrency zaten mevcuttu | `T46_REPORT.md:145` |
| T40-PermClaimCache 🆕 | **✅ ÇÖZÜLDÜ-by-design → WP11** — owner kararı: T40'ın "dinamiklik > performans" kararı korunur, cache **EKLENMEZ** (her login/refresh DB lookup; perf darboğazı kanıtı yok, ≤2dk staleness istenmedi) | `T40_REPORT.md:113` |
| discord-interactions-userinstall 🆕 | Discord slash commands/interactions (Ed25519 webhook) + user-install; şu an yalnız Bot DM + OAuth2 | `MEMORY_ARCHIVE.md:178` (T80 K6-K8) |

## 5. T-future — Frontend polish / enhancements

| Önc. | ID | Açıklama | Kaynak |
|---|---|---|---|
| ✅ | Session-NoSignOutInMainHeader | **ÇÖZÜLDÜ → F7a ([#282](https://github.com/turkerurganci/Skinora/pull/282))** — ana `Header`'a çıkış düğmesi eklendi ve tıklanınca yeni ortak `@/lib/auth/signOut` kapısından geçip kullanıcıyı locale ana sayfasına gönderiyor. Yeni i18n anahtarı gerekmedi: `nav.signOut` dört dilde zaten vardı (`Sign out` / `Çıkış` / `Cerrar sesión` / `退出`) — eksik olan gerçekten yalnız düğmeydi. **Kardeşiyle birlikte kapandı** (`Session-LogoutDoesNotRevokeRefreshToken`); ayrı ayrı düzeltmek işe yaramazdı, düğme A8'i çağırmasa oturum yine sonlanmıyordu. Testler düğmenin **varlığını** değil tıklanınca **ne olduğunu** ölçüyor. **Eski açıklama:** **Sıradan kullanıcı arayüzden oturumunu kapatamıyor.** Ana [`Header.tsx`](../frontend/src/components/layout/Header.tsx) yalnız bildirimler / profil / dil / ayarlar veriyor; `logout` ve `signOut` dizgileri **dosyada hiç geçmiyor** (ölçüldü). `useAuthStore.logout()`'un üç çağıranı var ve üçü de sıradan oturumun dışında: `AdminHeader.tsx:68` (yalnız admin sayfaları), `SuspendedHeader.tsx:56` (yalnız askıya alınmış kullanıcı) ve `AccountManagementSection.tsx:179` — sonuncusu **bağımsız bir çıkış değil**, hesap kapatma/silme sonrası tetikleniyor. **Altyapı hazır, eksik olan tek şey düğme:** `nav.signOut` anahtarı dört dilde de mevcut (en `Sign out` · tr `Çıkış` · es `Cerrar sesión` · zh `退出`) ve store'daki `logout` çalışıyor. **Kusur ailesi tanıdık:** `UITour-NoUiPathToVerifyMobileAuthenticator` ile aynı — arka taraf tam, çağıran UI yok. **Nasıl bulundu:** F4 probe'u için ikinci bir hesapla giriş yapılması gerekince *"nasıl çıkış yapacağım"* sorusu soruldu ve cevabı olmadığı görüldü. Düzeltirken `Session-LogoutDoesNotRevokeRefreshToken` (§4) ile **birlikte** ele alınmalı: düğme A8'i çağırmazsa sunucudaki oturum yine sonlanmaz. | ölçüm 2026-08-24; `Header.tsx`, `auth-store.ts:50`, `nav.signOut` |
| ✅ | FE-admin-signalr-subscription | **ÇÖZÜLDÜ → WP9** — `RealtimeProvider.tsx` üç admin event'ine de abone (`onAdminBotStatusChanged` / `onAdminReconciliationMismatch` / `onAdminHotWalletThresholdBreached`) ve ilgili query'leri invalidate ediyor | T96 K2 |
| ⚪ | FE-timeline-cancel-step-position 🆕 | **Lokal inceleme bulgusu (2026-07-26).** `TransactionTimeline` iptal/FLAGGED/REFUNDED durumunda kırmızı X'i **her zaman 1. adımda** gösteriyor (`indexForStatus` → `-1`, `effectiveIndex = max(0,-1) = 0`), yani işlemin hangi adımda iptal edildiği kayboluyor; 04 §C05 "aktif adımda kırmızı X" diyor. Frontend-only çözülemez: kullanıcı detay DTO'sunda (`TransactionDetailResponse`) iptal anındaki durum yok (`cancelInfo` yalnız `cancelledBy`/`reason`/`cancelledAt`/`itemReturned`/`paymentRefunded`). Gerekli: AD7'ye iptal-anı durumu (veya kullanıcıya açık `statusHistory`) eklenmesi + timeline'ın bunu tüketmesi. Terminal-render kusurunun diğer yarısı (COMPLETED/REFUNDED) `fix/timeline-terminal-step` PR'ında kapatıldı. | T96 / 04 §C05 |
| ✅ | admin-table-sort | **ÇÖZÜLDÜ (ölçüldü 2026-08-24, WP2a) — satır bayattı.** İddia "`sortBy`/`sortOrder` API var, UI göndermiyor"du; ölçüm bunun tersini gösterdi. Backend'de `SortBy` taşıyan **üç** yüzey var: `IFraudFlagAdminQueryService` · `IAdminTransactionQueryService` · `AdminSanctionsDtos`. İlk ikisinin UI'ı `sortBy`+`sortOrder`'ı hem isteğe koyuyor hem `pushParams` ile URL'e yazıyor (`admin/flags/page.tsx:64,140` · `admin/transactions/page.tsx:81,179`). Üçüncüsünün (sanctions) **admin sayfası yok** — `frontend/src`'te tek referans bile geçmiyor, yani gönderecek UI de yok. Kalan admin sayfaları (audit-logs, disputes, users, roles, settings) sıralama **API'si olmayan** yüzeyler; onlara başlık eklemek backend işi olurdu ve bu satırın konusu değildi | T101 K10 / T106 K1 → ölçüm WP2a |
| ⚪ | wizard-hard-refresh-resets 🆕 | **`url-state-sync`'ten ayrıldı — o satırın TEK açık kalan parçası.** İşlem oluşturma sihirbazı (`NewTransactionForm.tsx`) tüm adım durumunu `useState`'te tutuyor (`item`, `stablecoin`, `price`, `paymentTimeoutHours`, `method`, `buyerSteamId`, `sellerWalletAddress`, `walletConfirmed`); sayfa yenilenince hepsi sıfırlanıyor ve kullanıcı envanterden item seçmeye geri dönüyor. Kardeş parçalar (`?tab=`/`?page=` senkronu, dashboard deep-link) **çözülmüş** ölçüldü. Çözüm yeri bir karar gerektirir: URL'e yazmak (item bir nesne, serileştirmesi gerekir) mi, `sessionStorage` mı — ikisi de tasarım kararı, mekanik düzeltme değil | ölçüm 2026-08-24 WP2a; `NewTransactionForm.tsx:44-57` |
| ⚪ | signalr-toast-countdown | C09 toast realtime'da boş; verification countdown; email cooldown Retry-After; LanguageSelector drift | T96/T94/T97 K |
| ⚪ | profile-prefill-image | Seller payout adresi profil pre-fill; `next/image` migration + Steam-CDN whitelist (`<img>`+ESLint disable) | T89/T93 K |
| ⚪ | dispute-detail-polish | Seller payout-issue UI, autoCheck refresh, closed-dispute audit, asset-id, drawer auto-close, Tronscan URL sabit | T92/T90/T98/T101 K |
| ⚪ | FE-permission-guard | Admin sayfalarında client-side permission/route guard yok (backend enforce authoritative) — tekrarlayan | T85/T88/T99/T103/T104/T105/T106 K |
| ✅ | static-routes-pages | **ÇÖZÜLDÜ → WP13 ([#186](https://github.com/turkerurganci/Skinora/pull/186)) — satır çevrilmemişti.** Üçü de gerçek sayfa ve ortak `LegalPage` bileşenini kullanıyor: `/privacy` (14 satır), `/terms` (15), `/support` (7, dört bölüm: contact/responseTimes/disputes/faq). Satırdaki dördüncü parça olan login→dashboard yönlendirmesi de kapalı — `auth/login/page.tsx:7,19` `sanitizeReturnUrl` kullanıyor (F4a/F4b turunda tek kaynağa taşınmıştı) | T85/T86/T87 K → WP13, ölçüm WP2a |
| ⚪ | dev-route-visibility | `/dev/components` public-but-unindexed (dev-menü kararı); responsive audit (T98'de büyük ölçüde çözüldü) | T84 K |
| ✅ | T97-NEXT_LOCALE-cookie | **ÇÖZÜLDÜ → WP13 — satır çevrilmemişti.** next-intl `NEXT_LOCALE` cookie migrasyonu tamam: `i18n/navigation.ts:12` `LOCALE_COOKIE = "NEXT_LOCALE"` sabitini tanımlıyor ve iki tüketici de yorumlarında WP13'e atıf yaparak cookie'ye yazıyor (`LanguageSelector.tsx:49`, `LanguagePreferenceSection.tsx:50`) | `MEMORY_ARCHIVE.md:219` (T97 K5) → WP13, ölçüm WP2a |
| ✅ | T97-formatAmount-deprecated-alias | **ÇÖZÜLDÜ → WP13 — satır çevrilmemişti.** `formatAmount` dizgisi `frontend/src` genelinde **hiç geçmiyor**; `git log -S formatAmount -- frontend/src/lib/utils/format.ts` alias'ın WP13 ([#186](https://github.com/turkerurganci/Skinora/pull/186)) ile düştüğünü gösteriyor. Satırın işaret ettiği `format.ts:133-139` aralığında bugün `toDate` yardımcısı var | `MEMORY_ARCHIVE.md:219` (T97 K3) → WP13, ölçüm WP2a |
| ✅ | FE-enums-ts-lag | **ÇÖZÜLDÜ → WP13 ("enum sync") — satır çevrilmemişti.** İddia `NotificationType` (-7), `AuditAction` (-14), `FraudFlagType` (-`SANCTIONS_MATCH`) gecikmesiydi; ölçüm üçünün de **birebir eşit** olduğunu gösterdi. Kanıt sayı değil **üye üye diff**: FE `types/enums.ts` ↔ BE `Skinora.Shared/Enums/*.cs` sıralanıp karşılaştırıldı → `FraudFlagType` 6/6, `NotificationType` 26/26, `AuditAction` 29/29, üç dosyada da fark **yok** (sayı eşitliği isim eşitliği değildir; ikisi de ölçüldü) | `GATE_CHECK_F5.md` → WP13, ölçüm WP2a |
| ✅ | T136-FormatRelativeTimeUnused | **ÇÖZÜLDÜ → WP2a — silindi, ve gerekçe "kullanılmıyor" DEĞİL.** T135'in N2 dersi ("ya bağla ya sil") kararı açık bırakmıştı; ölçüm kararı verdi: üretimin göreli-zaman yolu **zaten var ve başkası** — `NotificationRow.tsx:101` next-intl'in `useFormatter().relativeTime`'ını kullanıyor. Yani `formatRelativeTime` tüketicisiz bir yardımcı değil, **kütüphanenin üretimde kullandığımız fonksiyonunun elle yazılmış ikinci nüshası**; tutmak iki göreli-zaman uygulamasını canlı tutmak olurdu. GUARDRAILS §4 bu yüzden karşılandı ("gereksiz görünüyor" değil, "tekrarlıyor"). Fonksiyon + kendi testi (`format.test.ts`) birlikte kaldırıldı; `formatStablecoin`/`formatPercent`/`formatNumber` ailesi olduğu gibi duruyor | dead-code | T136, T135 N2 → WP2a |

## 6. Doküman / spec borcu

| Önc. | ID | Açıklama | Kaynak |
|---|---|---|---|
| ✅ | T33-SuccessRate-FractionVsPercent | **ÇÖZÜLDÜ → WP17 (no-op):** kod (`UserConfiguration.HasPrecision(5,4)`) + 06 §3.1 + 07 §5.x örnekleri zaten **fraction (0..1)** üzerinde hizalı (M1 2026-05-01 kapandı); aksiyon gerekmedi | `T33_REPORT.md:142` |
| ✅ | AD6-AD7-contract-recon | **ÇÖZÜLDÜ → WP17:** 3 alan koda eklendi — AD7 party `reputationScore` (yeni `AdminTransactionPartyDetailDto`), AD6 list `cancelledAt`, AD7 notification `content` (`Notification.Body`) + FE + 07 §9.6/9.7 doc + 2 test | T101 K3/K4/K6 |
| ✅ | backend-i18n-migration | **ÇÖZÜLDÜ → WP17 (hibrit):** notification resx tr/es/zh→56 (parity) · dispute auto-check buyer-locale lokalizasyon (`DisputeAutoCheckMessages`) · settings 59 + permission 2 label FE-key-mapping · steam `warningMessage` kaldırıldı. **Kalan:** notification `{Outcome}` per-recipient (auto-escalated iki-taraf + DisputeResolved) → notification-mimari follow-up | T49/T92/T95/T102/T106 K |
| ~ | audit-doc-drift | **Kısmen → WP17:** 07 §9.19 `SELLER_PAYOUT_SENT`→`WALLET_ESCROW_RELEASE` ✅ · 06 §3.25 stale index ✅ · PermissionCatalog count ✅ (zaten 14). **Kalan (WP17-dışı):** RefreshToken purge (by-design soft-delete) · ACTIVE_DISPUTE_EXISTS (WP5'te 07'den kaldırıldı, erişilemez) | T42/T63b/T82 K |
| ⚪ | T140-DocFooterVersionDrift 🆕 | **Doküman başlık ↔ altbilgi sürüm uyuşmazlığı, 2 dosya.** T140 yapım turu 12 dokümanı taradı: 9'u eşleşiyor, `05` bu turda hizalandı (v2.3 → v3.5, doğrulamada v3.6), geriye `09_CODING_GUIDELINES` (başlık v1.0 / altbilgi v0.9) ve `10_MVP_SCOPE` (v2.0 / v1.3) kaldı; `12_VALIDATION_PROTOCOL`'ün altbilgisi hiç yok. Yapım turu bunları **bilerek** elle sürmedi — o dosyalara bu dalda dokunulmadı ve ilgisiz dokümanları bir task dalına almak bundled-PR yasağının ruhuna aykırı. Sahiplenilmesi proje sahibinin kararı: ucuz bir doküman hijyen turu ya da bir sonraki doküman görevinin kapsamı. | T140 yapım + validate |
| ⚪ | audit-detail-schema | AuditLog `detail` pass-through NewValue; central AuditLog wiring; RestartRecovery audit; OldValue yok | T42/T39/T47/T106 K |
| ⚪ | mvp-scope-postmvp | Bilinçli MVP-dışı: reviews, KYC, mobil, diğer oyunlar, multi-item/barter, ek blockchain, fiat, premium, Discord guild, Sentry vb. | 10_MVP_SCOPE / 02_PRD |
| ⚪ | T110-RefundAddressDocConflict 🆕 | **MVP sonrası (owner kararı 2026-06-23 — yakın zamanda yapılmayacak).** Edge-case iade hedef-adresi doküman çelişkisi: impl + **08 §562** reddedilen-ödeme iadelerini (insufficient/excess/wrong-token/late) **ödeme kaynak adresine** (`FromAddress`) gönderir; ama 02 §4.4 (s.108) · 02 §4.6 (s.127) · 03 §4.3/§5.3/§5.4/§5.5 (s.287/340/360/368) · 06 §3.8 (s.736) "alıcının belirlediği iade adresine" der. `BUYER_REFUND` (kabul edilmiş ödeme iadesi) doğru şekilde belirlenen adrese gider (02 §4.6 s.127 doğru, korunur). Yapılacak: iki-hedef ayrımını dokümanlarda netleştir + `AmountValidationService.QueueRefundIntent` yorumu 02§4.6→08§562. T110 testi spec-yetkili (08 §562) davranışı **doğru** test eder → bu doc-borcu, kod değil. | T110 validate K1 |
| ⚪ | T111-AdminFlagsSurfaceDocConflict 🆕 | **MVP sonrası (F6 Gate Check forward 2026-06-24 — kardeş T110-K1 deseni).** Admin flag yönetim yüzeyi cross-doc çelişkisi: **03 §8.2** (`03_USER_FLOWS.md:517`) hesap flag'lerinin **ayrı bir hesap flag yönetim yüzeyinden** yönetildiğini söyler; ama **07 §9.2 + 04 §8.2 + T100a** tek `/admin/flags` + `scope=ACCOUNT_LEVEL` yüzeyini tanımlar ve üretim kodu (T100a) tek-yüzey lehine fiilen çözmüştür (03 §8.2 stale). Yapılacak: 03 §8.2 metnini 04 §8.2 / 07 §9.2 ile hizala. T111 E2E mevcut gerçekliği (`/admin/flags/:id/reject`) doğru test eder, AC3'ü zayıflatmaz → bu doc-borcu, kod değil. | T111 validate K1 |
| ~ | content-authoring | **WP17 (taslak):** ToS/Privacy/Support `legal.*` taslak metin 4 dil yazıldı (owner "taslak yaz" kararı) — **otoriter metin hukuk review gerektirir** (jurisdiction/governing-law/entity belirsiz) | SPEC |
| ⚪ | suspend-signalr-spec | Suspension'da otomatik EMERGENCY_HOLD/live force-restrict yok (request-time enforce); `/auth/suspended` vs `/account-suspended` | T105a K2 |
| ✅ | like-escape-helper | **ÇÖZÜLDÜ → WP18 (PR-3):** kanonik bracket-wrapping escaper `AdminTransactionQueryService` private'ından `Skinora.Shared.Persistence.SqlLikeEscaper`'a çıkarıldı + 3 escape'siz `EF.Functions.Like` call-site'ı (AdminUserService/AuditLogQueryService/AdminSanctionsService) düzeltildi (LIKE-wildcard injection kapandı). `NoRawSqlConventionTests` source-scan arch testi (`ExecuteSqlRaw`/`FromSqlRaw`/* yasak, backend/src-only, NetArchTest yok). | T63 K6 / T106 K8 / T42 K1 |
| ~ | i18n-untranslatable-localized 🆕 | **WP18 (advisory, PR-1):** `check-i18n.mjs` 15 anahtarın 04 §10.4 "untranslatable" terimini yerelleştirdiğini buldu — yalnız **"Gas fee"** (es `Tarifa de gas` / zh `Gas 费` / tr `…gas` — 12 anahtar) + **"Mobile Authenticator"** (zh `手机令牌` — 3 anahtar). Sert marka token'ları (USDT/USDC/TRC-20/Tron/Steam/Steam ID/CS2/Trade offer) temiz. Owner kararı: çeviriler **değiştirilmedi** (zh'de Steam'in resmî terimi olabilir, İngilizce'ye zorlamak UX'i bozar), kural **advisory** kaldı; spec-vs-çeviri uzlaşısı (çeviri düzelt **veya** 04 §10.4 listesini daralt) follow-up'a bırakıldı. | `check-i18n.mjs` |
| ✅ | permissioncatalog-xmldoc-drift | **ÇÖZÜLDÜ → WP17 (no-op):** xmldoc zaten "14 catalog entries", `All` 14 içerir (T82 sonrası güncel); aksiyon gerekmedi | `PermissionCatalog.cs:56` |
| ✅ | datamodel-sanctioned-index-drift | **ÇÖZÜLDÜ → WP17:** 06 §3.25 obsolete `IX_SanctionedAddresses_Address` satırı kaldırıldı; filtered UQ `WHERE IsActive=1` hot-path'i karşılar | `06_DATA_MODEL.md` |
| ✅ | admin-route-table-drift | **ÇÖZÜLDÜ → WP17:** 04 §1 S12 `/admin`→`/admin/dashboard`, S21 `/admin/audit-log`→`/admin/audit-logs` | `GATE_CHECK_F5.md` |
| ✅ | T84-emergencyhold-status-doc-drift | **ÇÖZÜLDÜ → WP17:** 04 §5'e overlay-rozet notu eklendi (`EMERGENCY_HOLD` = IsOnHold overlay, enum değeri değil; `FLAGGED` ise kanonik statü — review F4) | `MEMORY_ARCHIVE.md` T84 K6 |
| ✅ | T58-ActiveDisputeExistsUnreachable 🆕 | **ÇÖZÜLDÜ → WP5** — 07 §7.8 Hatalar'dan kaldırıldı (03 §6 farklı-tip eşzamanlı dispute'a izin verdiği için tasarım-gereği erişilemez) | `T58_REPORT.md:177` |
| ⚪ | T133b-AdminDtoItemReturnedXmlDoc 🆕 | **`AdminTransactionDtos.cs:61-65` XML doc'u v3.0'da record'dan düşen `itemReturned` alanından hâlâ söz ediyor.** `ReleaseEmergencyHoldResponse` bugün `(Status, ReleasedAt, Action, bool? PaymentRefunded)` taşıyor; doc "CANCEL … itemReturned, paymentRefunded" diyor. Yalnız yorum, davranış etkisi yok. **Karar geçmişi:** T138 yapım turu bunu ölçtü, e2e görevinde production kaynağına dokunmamak için düzeltmedi ve *satır açılsın mı yoksa T135 N2 gibi "iş üretmediği için" mi geçilsin* sorusunu **bilerek doğrulama turuna bıraktı**. Doğrulama turu **satır açılsın** dedi ve gerekçesi turun kendi en değerli bulgusudur: `T136-E2EDeadItemReturnedAssertions`'ın altıncı maddesi tam olarak böyle bir yorumdu, sahipsiz kaldı ve sonunda **yanlış bir kapatma iddiası** üretti. Sahipsiz yorum ucuz değildir; bir sonraki backend turunun yolu üstünde | dead-doc | T138, T133b, T136-E2EDeadItemReturnedAssertions (ders) |

## 7. Test / CI borcu

| ID | Açıklama | Kaynak |
|---|---|---|
| ✅ FE-test-runner | **ÇÖZÜLDÜ → WP18 (PR-2):** `vitest 4.1.9` + `@vitejs/plugin-react` + `jsdom` + `@testing-library/react`/`jest-dom`; `vitest.config.ts` (jsdom + `@/` alias) + setup + `test` script. Seed B = **25 test/7 dosya** (6 pure-util + 1 `StatusBadge` render). Yeni CI `frontend-test` job (FE + 2 sidecar vitest, per-area gated, ci-gate.needs'te blocking). Geniş component/hook kapsamı F6'ya ertelendi. | T92/T98/T99-T106 K |
| ✅ prettier-drift | **ÇÖZÜLDÜ → WP18 (PR-1):** FE + 2 sidecar `format:check` **blocking** CI gate'i mevcut lint job'a eklendi (backend `dotnet format` paritesi). Gerçek LF drift 25 dosya (FE 13 / steam 7 / BC 5) `prettier --write` ile normalize edildi; görünen 144 CRLF working-tree artefaktıydı. 3 `.prettierrc` alan-bazlı bilinçli farkla korundu (FE singleQuote:false vs sidecar:true). | T84 K8 / T64 K1 |
| ✅ filterbar-dateto-chore | **ÇÖZÜLDÜ → WP18 (PR-3):** paylaşılan `toEndOfDay` helper (`lib/utils/date.ts` + vitest) admin transactions + flags'e uygulandı, audit-logs inline refactor edildi. Client-side; query-useMemo'ya anchor (dep/write-back dokunulmadı). | T106 K2 / T100 K6 |
| ✅ test-infra-misc | **ÇÖZÜLDÜ → WP18 (PR-3):** `AdminWalletsEndpointTests` (HTTP-boundary auth/mapping/envelope, IHotWalletService stub) + suspend permission-isolation testi (VIEW_FLAGS→403, ayrı token helper). TestContainers/Redis/migration-verify zaten kapsanmış (no-op). | T07/T82/T77/T105a K |
| ✅ i18n-lint-ci | **ÇÖZÜLDÜ → WP18 (PR-1):** `frontend/scripts/check-i18n.mjs` + `npm run i18n:check` CI lint job'da. Key-parity **blocking** (1291×4 anahtar, identical key-set); untranslatable kuralı (`UNTRANSLATABLE_TERMS` `untranslatable.ts`'ten tek-kaynak parse) **advisory** (owner kararı — bkz. `i18n-untranslatable-localized`). | T97 K1 |
| ~ sidecar-npm-audit | **WP18 (PR-1 advisory + PR-2 BC blocking):** her iki sidecar `npm audit` CI adımı. **Steam = kalıcı advisory@high** (`continue-on-error`) — 4 critical (`request@2.88.2`/`protobufjs`) **upstream-fix yok**, `--force` yıkıcı `steam-user` downgrade (owner accept-risk). **Blockchain = blocking@critical** (PR-2): 2 non-breaking override fixable high'ları kapattı — `ethers 6.17`→`ws 8.21.0` (GHSA-58qx) + `form-data 4.0.6` (GHSA-hmw2 CRLF; yapım-içi review bulgusu, high 4→3, tronweb 5.3.5 korundu). **Residual: axios + lodash high** (tronweb 5.3.5 altında) **non-breaking fix yok** → npm yalnız breaking `tronweb@6.0.2` sunar → owner accept-risk (pratik risk düşük); BC gate prod-critical'a scope'landı (0 bugün). hermes-parser yalnız frontend-lock'ta. | T64 K5 / T70 / T84 K9 |
| ⚪ | tronweb-6-major-bump 🆕 | **WP18 PR-2 follow-up:** BC residual lodash high'ları + diğer tronweb-transitive high'ları yalnız `tronweb 5.3.5→6.x` major bump temizler (breaking API). Ayrı scoped task: API recon + sidecar smoke test gerekir. | `WP18-2_REPORT.md` |
| ⚪ | T140-TestClaimsExceedMeasurement 🆕 | **T140 doğrulaması — turun kendi dersi, bu kez testlerin üstünde.** Üç yeni/değişen test, yorumunun iddia ettiğinden azını ölçüyor; hiçbiri davranışı yanlış doğrulamıyor, yalnız iddia ile ölçüm arasında fark var. (1) `HappyPathNotificationConsumerTests.cs:222` `Assert.DoesNotContain(… r.UserId == _buyer.Id)` — üstündeki `Assert.Single` (:211) + `Assert.Equal(_seller.Id, …)` (:212) geçtiyse buyer predikatı tanım gereği eşleşemez; **ölü koruma**. Doğrulayıcının mutasyon turu bunu ölçtü: alıcı `data.SellerId → buyerId` çevrildiğinde test **:212'de** düştü, :222'ye hiç ulaşılmadı. (2) `AmountValidationServiceTests.cs` `…InTheCallersUnitOfWork` adı/yorumu atomiklik iddia ediyor ama enjekte edilen `CapturingOutboxService` yalnız listeye ekliyor — change-tracker/rollback semantiği taklit edilmiyor; test gerçekte "yayın caller'ın `SaveChanges`'inden ÖNCE oldu"yu ölçüyor (sözleşmenin gözlenebilir yarısı; testin kendi yorumu bunu zaten söylüyor). Gerçek atomiklik garantisi `OutboxService.PublishAsync`'in yapısal sözleşmesinden geliyor. (3) `RealtimeConsumerTests` `…ExactlyOnce_AcrossBothConsumers` iki consumer'ı elle çağırıyor; aynı dosyadaki iki per-consumer testinin öldürmediği bir mutasyonu öldürmüyor (doğrulayıcının M3 turunda üçü birlikte kırıldı). Ucuz kapatma: (1) sil veya `Single`'ı kaldırıp anlamlı kıl · (2) testi yeniden adlandır ya da gerçek `AppDbContext` ile rollback assert et · (3) sakla ama "belgeleyici" olarak işaretle. | T140 validate |
| ⚪ | T140-MultiPaymentLeakPin 🆕 | **Negatif-kol simetrisi eksik.** T140 `TransactionStatusChangedEvent` sızıntısını eksik ödeme (`AmountValidationServiceTests.cs:233`) ve guard-rejected/emergency-hold (:426) kollarında `Assert.Empty(_outbox.Events.OfType<TransactionStatusChangedEvent>())` ile çiviledi; `HandleMultiPaymentAsync` kolu (`AmountValidationService.cs:399-455`) aynı çiviyi almadı. **Kod bugün doğru** — bu kol `AdvanceStateMachineAsync`'i hiç çağırmıyor (tek çağıranlar :103 ve :350), dolayısıyla yayın da yok; eksik olan yalnız regresyon çivisi. Tek satırlık assert. | T140 validate |
| ⚪ | T140-E2ENotificationsSoftDeleteFilter 🆕 | **`e2e/src/db.ts`'in dört Notifications yardımcısı ham SQL'de `IsDeleted` filtresi kullanmıyor** (`getNotificationTypes` :283, `pollCancelledNoticeRecipients` :344, `pollNotificationRecipients` :507, T140'ın eklediği `getNotificationRecipients` :313). `Notification` `ISoftDeletable` ve EF tarafında global `HasQueryFilter(n => !n.IsDeleted)` var; aynı dosyadaki PaymentAddresses (:426, :605) ve Disputes (:882) sorguları `IsDeleted = 0` ekliyor — yani db.ts içinde tutarsızlık. **Bugün erişilemez:** backend'de `Notification.IsDeleted` yazan hiçbir yol yok ve `db.ts:130` her seed'de satırları fiziksel siliyor; kırılma yönü de yanlış-YEŞİL değil yanlış-KIRMIZI. T140 gerilemesi DEĞİL — yerleşik kalıbın sürdürülmesi; dördü birlikte hizalanmalı. | T140 validate |
| ⚪ | npm-version-ci-parity 🆕 | **WP18 PR-2 dersi:** CI Node 20 → **npm 10.x**; lokal Node 24 → **npm 11.6.2**. npm 11 ile üretilen lockfile, npm 10 `npm ci` tarafından reddedilir (PR-2 ilk run lint fail). Kalıcı çözüm: CI Node 20→24 bump (npm 11 paritesi) **veya** `engines`/`.nvmrc` ile lokal npm pin. Şimdilik lockfile'lar `npm@10` ile üretiliyor. | `WP18-2_REPORT.md` |
| ✅ template-side-escape-audit | **ÇÖZÜLDÜ → WP18 (PR-3):** `BoldHeaderMessageComposer` raw-truncate-then-escape ile kanal limitinde (Discord 2000 / Telegram 4096) envelope kurar → escape-pair bölünmez, bold korunur, over-length artık preference auto-disable etmez. Composer unit + escaper negatif-parity + 2 handler over-length fact. (Escape-after-substitute zaten doğruydu; canlı injection yoktu.) | `T79_REPORT.md:131` K5 / `T80_REPORT.md:225` K5 |
| ✅ | T136-E2EDeadItemReturnedAssertions | **ÇÖZÜLDÜ → T138 (altısı da; altıncısı DOĞRULAMA turunda kapandı).** Maddeler: `cancellation.spec.ts` üç `itemReturned` hard-fail'i + iki custody başlığı (spec yeniden yazıldı, başlıklar "nothing refunded"); `emergency-hold.spec.ts` hard-fail'i **ve** boşuna geçen `expect(resumeBody.itemReturned ?? null).toBeNull()` (satırın en değerli maddesiydi — sessiz yanlış güven; yerine `paymentRefunded` iddiası, ki RESUME'de gerçekten null döner); `admin-flows.spec.ts` `steamAccounts` bloğu (silindi, alanın neden geri gelmeyeceğini yazan notla); `e2e/src/api.ts` docstring'i; `e2e/src/db.ts`'in T137a notu — bu **satırın kendisinin de kaçırdığı** bir madde: not "`itemReturned` alanı hikâyenin tamamı" diyordu, oysa o alan da yok, düzeltildi; `docker-compose.e2e.yml` başlığındaki bot seed anlatısı. **DOĞRULAMA TURU DÜZELTMESİ (2026-08-22, B1):** dördüncü madde (`e2e/src/api.ts:165` docstring'i) yapım turunda **kapanmamıştı** ve kapandı sayılmıştı. Madde `itemReturned`'ı değil, bir önceki maddenin alanını — AD1'in `steamAccounts` bloğunu — kastediyordu (`git show origin/main:e2e/src/api.ts | sed -n '165p'` → AD1 docstring'i, `steamAccounts` vaat ediyor) ve satır dalda kelimesi kelimesine duruyordu. Kapatmanın kayıtlı kanıtı olan `grep -rn "itemReturned"` komutu bu maddeyi **yapısal olarak göremez** — satırın kendi dersinin (ölü-referans avı ölü referans bırakır) üçüncü tekrarı. Validator düzeltti; aynı taramada `api.ts`'te dört ölü custody docstring'i daha çıktı (`payViaFake` / `payWrongTokenViaFake` / `paySpamTokenViaFake` "stays ITEM_ESCROWED", `assertStatusStable` "callers pass … TRADE_OFFER_SENT_TO_SELLER / ITEM_ESCROWED" — altı çağıranın hiçbiri onları geçmiyor) ve hepsi P2P karşılıklarıyla değiştirildi. **Yeni kapatma kanıtı, iki komut:** `grep -rn "itemReturned" e2e/ docker-compose.e2e.yml` **ve** `grep -rn "ITEM_ESCROWED\|TRADE_OFFER_SENT\|steamAccounts" e2e/ --include=*.ts` → ikisinde de geriye yalnız **geçmiş zamanda** yazılmış, silmeyi açıklayan yorumlar kalıyor | — |
| ⚪ | T136-NoRouteOrMessageCompileGuard 🆕 | **Ölü bir rota href'i veya eksik bir i18n anahtarı derleme/CI'da SESSİZ kalır.** T136 tamlık eleştirmeninin ölçümü: `next.config.ts` `typedRoutes: false` (ve iki canlı çağrı düz template string olduğu için açık olsa bile kaçardı); next-intl varsayılanı `defaultGetMessageFallback` (`use-intl/…/initializeConfig-B5qJiBCm.js:14-16`) eksik anahtarı **fırlatmadan** düz metin olarak basar, proje bu varsayılanı ezmiyor (`i18n/request.ts` yalnız `{locale, messages}` döndürüyor) ve `IntlMessages` tip augmentation'ı yok. Somut sonuç: yarım kalmış bir silme turu üç ayrı sessiz üretim bozukluğu üretebilir (menüde harfiyen `adminNav.steamAccounts` yazısı, 404'e giden link, dashboard'da ölü blok) ve **lint + prettier + i18n:check + vitest + next build'in hepsi yeşil kalır**. T136'da karşı önlem el disiplini + tam süpürme oldu. Kalıcı kapı: `IntlMessages` augmentation'ı veya kod↔mesaj lint'i | test-debt | T136, T134 |
| 🟡 | T136-ParityGuardsSkippedOnBackendPRs 🆕 | **FE↔C# parity bekçileri, drift'in YARATILDIĞI PR'da koşmuyor.** T136 doğrulamasının ölçümü: `permissionCatalog.parity.test.ts` (T136) ve `enums.parity.test.ts` (T134) backend C# kaynağını okur ama `frontend/` altında yaşar; ikisini de yalnız `3b. JS test (vitest)` job'ı koşturur ve o job'ın koşulu `frontend` / `sidecar-*` filtreleridir (`ci.yml:304`), `frontend` filtresi ise yalnız `frontend/**` + `.github/workflows/**`'dir (`ci.yml:69-71`). **Backend-only bir PR job'ı `skipped` yapar ve `ci-gate` `skipped`'ı geçer** — yalnız `failure`/`cancelled` arar (`ci.yml:740-745`) → bekçi hiç koşmadan CI Gate yeşil. **Kanıt varsayım değil, tarih:** T136'nın kapattığı drift'i yaratan T132'nin squash commit'i (`eb0e49d`) `.claude/`, `Docs/`, `backend/`, `docker-compose*.yml` dosyalarına dokunuyor, `frontend/**`'e **hiç** dokunmuyor — yani yeni bekçi o gün var olsaydı da **koşmazdı**. Backend tarafındaki ikame sinyal kısmî: `AdminRolesEndpointTests.cs:88-96` sayıyı (`Assert.Equal(12, …)`) tutar ve backend-only PR'da koşar, ama FE nüshasının varlığından söz etmez — T132 o assertion'ı **güncelledi** (satır 88-91'deki yorum bunu yazıyor) ve FE nüshası yine de bayatladı. Ucuz kapatma: `frontend` paths-filter'ına bekçilerin okuduğu C# yollarını eklemek (`backend/src/Skinora.Shared/Enums/**`, `backend/src/Modules/Skinora.Admin/Application/Permissions/**`) — job'ın `if`'i değişmez, yalnız tetikleyici genişler. Alternatif (bekçileri backend test suite'ine taşımak) aday değil: parser TS'te yaşıyor ve i18n JSON'larını da okuyor. `T136-NoRouteOrMessageCompileGuard`'dan **ayrı** kalem: o "bekçi yok" diyor, bu "bekçi var ama tetiklenmiyor" diyor. |
| 🟡 | T138-E2ENginxPathFilterGap 🆕 | **Yeni `happy-path UI` E2E leg'i `nginx/nginx.conf`'a bağımlı, ama `e2e-stack` paths-filter'ı `nginx/**`'ı içermiyor.** Ölçüm (T138 doğrulaması): `ci.yml:86-92` filtresi `e2e/**` · `sidecar-fake/**` · `docker-compose.e2e.yml` · `backend/**` · `frontend/**` · `.github/workflows/**` sayıyor; `nginx/**` yok. UI leg'i ise `docker-compose.e2e.yml:200-201` ile `./nginx/nginx.conf`'u `skinora-reverse-proxy`'ye mount ediyor ve Playwright'ı o origin'e (`baseUrl: http://localhost:8080`) bağlıyor — yani `/api/v1` veya `/hubs` proxy kuralını bozan bir PR **hiçbir E2E leg'i tetiklemez** ve ağ sessizce görüş bildirmez. `T136-ParityGuardsSkippedOnBackendPRs` ile **aynı sınıf**: bekçi var, tetikleyicisi eksik. Ucuz kapatma: `e2e-stack` filtresine `nginx/**` eklemek (job'ın `if`'i değişmez). Validator dalda uygulamadı çünkü paths-filter davranışsal bir CI değişikliği ve T138'in kabul kriterlerinde yok | ci-gap | T138, T136-ParityGuardsSkippedOnBackendPRs (sınıf) |
| ⚪ | T138-E2ENoArtifactUpload 🆕 | **On E2E leg'inin hiçbiri playwright artifact'ı yüklemiyor; kırmızı bir turun tanısı 200 satırlık compose log'una kalıyor.** `playwright.config.ts:24` CI'da `[['list'], ['html', { open: 'never' }]]` reporter'ını kuruyor, yani HTML rapor + trace üretiliyor ve leg bitince atılıyor; `ci.yml:750-758`'de yalnız `docker compose logs --tail=200` var, `actions/upload-artifact` adımı yok. T138 öncesinden var olan bir boşluk ama maruziyeti T138 8→10 leg'e çıkardı ve legler advisory olduğu için kırmızı bir leg zaten sessiz düşüyor. Ucuz kapatma: `if: failure()` koşullu `upload-artifact` adımı (`e2e/playwright-report`, `e2e/test-results`) | ci-gap | T138 |

## 8. Operasyonel config

| Önc. | ID | Açıklama | Kaynak |
|---|---|---|---|
| ⚪ | AgeGateDisableValueUnreachable 🆕 | **Yaş kapısının "kapalı" değeri koda yazılı ama API'den yazılamıyor — karar kalemi, kusur iddiası değil.** `SettingsBasedAgeGateCheck.EvaluateAsync` eşiği okuduktan sonra **açıkça** `if (thresholdDays <= 0) return AgeGateDecision.Allowed();` diyor, yani `0` "kapıyı tamamen atla" anlamına geliyor ve değer **her istekte DB'den taze** okunuyor (cache yok). Ama tek desteklenen yazma yolu olan **AD9** (`PUT /admin/settings/:key`) bunu reddediyor: `SystemSettingsValidator`'ın sonundaki **jenerik** `int/decimal` kuralı `d <= 0` için `VALIDATION_ERROR` döndürüyor (`auth.min_steam_account_age_days must be greater than 0 (got 0).`) ve bu anahtar için istisna yok. **Sonuç:** operatör yaş kapısını arayüzden/API'den kapatamıyor; kapatmak için DB'ye doğrudan yazmak gerekiyor — 2026-08-24'te F4 probe'u için tam olarak bu yapıldı ve ölçüldü (0 yazıldı, giriş açıldı, sonra 30'a geri alındı). **İki okuma da savunulabilir ve karar sahibinin:** (a) `<= 0` dalı yalnız bozuk değere karşı savunma, kapıyı kapatmak **bilinçli olarak** desteklenmiyor → o zaman dal ölü ve yorumu yanıltıcı, docstring düzeltilmeli; (b) `0` gerçekten "kapalı" anahtarıydı → o zaman validator'a bu anahtar için istisna gerekiyor. **Kusur olarak açılmadı** çünkü hiçbir şey bozulmuyor ve kullanıcı etkisi yok; kayda geçiyor çünkü kodun belgelediği bir yetenek desteklenen yoldan **erişilemez** durumda. `06_DATA_MODEL.md:1125` yalnız "altındaysa giriş engellenir" diyor, `0`'ın anlamına değinmiyor. | ölçüm 2026-08-24; `SettingsBasedAgeGateCheck.cs`, `SystemSettingsValidator.cs:313-319`, `AdminController.cs:338` |
| ✅ | ForwardedHeadersNotRegistered | **ÇÖZÜLDÜ → F3a ([#270](https://github.com/turkerurganci/Skinora/pull/270))** — `UseForwardedHeaders` pipeline'ın EN BAŞINDA kaydedildi ve güven `ReverseProxySettings.KnownNetworks/KnownProxies` ile SINIRLANDI; yapılandırılmamışsa **hiç kaydedilmez** (yanlış yapılandırmanın bedeli "eksik bilgi", "yanlış güven" değil). **Canlı ölçüm:** IP A kovayı doldurdu → 429, IP B → **401** (ayrı kova), IP A tekrar → 429 — yani rate limit izolasyonu artık istemci başına çalışıyor. **Spoofing savuşturuldu:** nginx üzerinden sahte `X-Forwarded-For` gönderen istemci kendi kovasından kaçamıyor (nginx zincire gerçek istemciyi ekliyor, `ForwardLimit=1` son hop'u alıyor). **Kökeni de kapatıldı:** bulgunun dört faz görülmemesinin sebebi böyle bir sinyalin hiç olmamasıydı — backend artık açılışta `Forwarded headers ENABLED — trusting networks [...]` ya da yapılandırılmamışsa üç kontrolün ETKİSİZ olduğunu söyleyen bir uyarı basıyor; `DEPLOY_RUNBOOK` §B (iki operatör env'i) + §F (doğrulama adımı) + `.env.example` güncellendi. **Eski açıklama:** **Reverse proxy arkasında istemci IP'si hiç görülmüyor — `UseForwardedHeaders` kayıtlı değil.** nginx tarafı **doğru**: `nginx/nginx.conf:53-56` `X-Real-IP` + `X-Forwarded-For` + `X-Forwarded-Proto` gönderiyor. Eksik olan backend: `Program.cs`'te `UseForwardedHeaders` **sıfır eşleşme**, `ASPNETCORE_FORWARDEDHEADERS_ENABLED` ne `docker-compose.yml`'de ne `.env.example`'da ne de çalışan container'ın env'inde var. Dolayısıyla `HttpContext.Connection.RemoteIpAddress` **her istekte proxy'nin IP'sini** döndürüyor. **Kod bunu kendi biliyor ve yorum bayat:** `RateLimitMiddleware.cs:123` — *"…is what UseForwardedHeaders rewrites when the reverse proxy is configured (T11+). **Until then this is the direct socket peer.**"* — proxy F5'te geldi, yorumun "until then"i hiç kapanmadı (`SidecarHealthChecksArePlacebo` ve `T140` ile aynı sahipsiz-yorum ailesi). **Canlı kanıt (2026-08-23):** F7 yükseltmesinden sonra yapılan gerçek Steam girişinin `UserLoginLogs.IpAddress` değeri `::ffff:172.20.0.5` — bu `skinora-reverse-proxy` container'ının kendi IP'si (`docker inspect` ile doğrulandı). **Ölçülen etki, dört kol:** (1) **`auth` rate limit politikası IP-scoped** — `UserScopedPolicies` = {`user-read`, `user-write`, `steam-inventory`, `admin-read`, `admin-write`}, `auth` listede **yok** (`RateLimitMiddleware.cs:32-39`), yani login/brute-force limiti proxy IP'sinde **tek global kova**: kontrol saldırganı izole etmiyor, bir kötüye kullanan herkesin bütçesini tüketebiliyor. (2) **Geo-block sessizce atıl** — özel IP'den ülke çözülemiyor, `SettingsBasedGeoBlockCheck.cs:52` *"Geo-block skipped: country could not be resolved"* yolundan geçiyor. (3) **VPN sinyali (T83 MaxMind GeoIP2) hiç tetiklenmiyor** — aynı sebep. (4) **17 `RemoteIpAddress` çağrı yeri** (admin audit, dispute, maintenance, refresh token…) proxy IP'si kaydediyor; denetim izi kişi ayırt etmiyor. **Neden 🔴:** üç güvenlik kontrolü (auth rate limit izolasyonu · geo-block · VPN sinyali) belgelenen üretim topolojisinde — nginx tek-origin, `§G.3` — sessizce etkisiz; hiçbiri hata vermiyor, hepsi "çalışıyor" görünüyor. **Düzeltmenin tuzağı:** `X-Forwarded-For`'a körü körüne güvenmek spoofing açar — `KnownProxies`/`KnownNetworks` docker ağıyla sınırlandırılmalı, aksi halde istemci kendi IP'sini uydurup rate limit ve geo-block'u birlikte atlar. **Ayrıca `.env.example` + `DEPLOY_RUNBOOK §B`'ye operatör adımı gerekir.** Sahibi: güvenlik/gözlenebilirlik turu — düzeltme küçük ama testi proxy'li bir kurulum gerektirir | ölçüm 2026-08-23 (F6→F7 yükseltme turu), `Program.cs`, `RateLimitMiddleware.cs:32-39,123`, `SettingsBasedGeoBlockCheck.cs:52`, `nginx.conf:53-56`, canlı `UserLoginLogs` satırı |
| 🟡 | SidecarHealthChecksArePlacebo 🆕 | **İki sidecar'ın `/health` ucu hiçbir şey prob etmiyor — sonuç kaynakta sabit.** `sidecar-blockchain/src/health/HealthController.ts:23-24` `tron-node` ve `hot-wallet` için literal `status: 'healthy'` döndürüyor (mesajlar: *"Skeleton — not yet connected"* / *"Skeleton — not yet configured"*); `sidecar-steam/src/health/HealthController.ts:29` aynı şekilde `steam-api` için *"Connectivity probe deferred to T67"*. Altındaki `overallStatus` hesabı ve `503` dalı gerçek mantıktır ama **girdiler sabit olduğu için ölü koddur** — uç 200'den başkasını döndüremez. **Yorumların işaret ettiği görevler kapandı:** blockchain controller'ın JSDoc'u *"Future tasks (T70–T77) will add real Tron node and hot wallet checks"* diyor, T70–T77'nin dokuzu da ✓ Tamamlandı; steam'inki T67'ye erteliyor, T67 de ✓ Tamamlandı. Entegrasyon geldi, health ucuna geri dönülmedi — `T140`'ın *"sahipsiz yorum ucuz değildir"* ailesinden. **Ölçülen sonuç:** platformun kesinti tespiti (`PlatformHealthProbeJob.cs:72` → `SidecarHealthClient.cs:58`) yalnız `IsSuccessStatusCode` okur, dolayısıyla **süreç/ağ down** durumunu görür ama *"süreç ayakta, Tron erişilemez / yapılandırılmamış / API anahtarı reddedildi"* durumunu **hiçbir zaman** göremez; aynı uç compose healthcheck'ini de besler. Telafi eden katman yok — Prometheus `/metrics` scrape ediyor, `/health` üzerinde alarm kuralı tanımlı değil. **Bugün canlı ölçüldü:** blockchain sidecar logda *"HD wallet mnemonic is not configured — /api/wallet/derive will respond 503"* derken `/health` aynı anda `healthy` döndürüyordu ve container 27 saattir `healthy` görünüyordu. **`DEPLOY_RUNBOOK §G.4` kontrol 4 bu ucu kanıt olarak gösteriyor** — göstermemeli; gerçek kanıt kontrol 6'daki log satırlarıdır. Sahibi: launch öncesi gözlenebilirlik turu | ölçüm 2026-08-23 (F6→F7 yükseltme turu), `HealthController.ts` (iki sidecar), `SidecarHealthClient.cs:58`, `PlatformHealthProbeJob.cs:72` |
| ✅ | T55-DormantThresholdMandatoryUnconfigured 🆕 | **ÇÖZÜLDÜ → WP14:** 19 zorunlu ayar (gerçek sayı; "21" stale) `Docs/DEPLOY_RUNBOOK.md §A` + `.env.example`'da belgelendi. Fail-fast (06 §8.9) bilinçli olarak korundu — owner kararı seed-default DEĞİL. | `T55_REPORT.md:37` |
| ⚪ | T136-DeadRolePermissionRows 🆕 | **`AdminRolePermissions` tablosunda ölü yetki string'leri kalmış olabilir.** T132 `VIEW_STEAM_ACCOUNTS` / `MANAGE_STEAM_RECOVERY`'yi koddan sildi ama dev/staging DB'lerdeki rol satırlarını temizleyen bir migration yok. T136 envanteri bunun **tuzağa dönüşmediğini** ölçtü: `PermissionAuthorizationHandler.cs:24` bu anahtarları isteyen bir policy bulamıyor ve `UpdateAsync` (satır 137-144) ilk rol düzenlemesinde `desired` dışındaki satırları soft-delete ediyor; FE gönderilen kümeyi katalogdan türettiği için (`RoleFormModal.tsx:61`) ölü anahtar geri yollanamaz → `INVALID_PERMISSION` 400 tuzağı **doğmuyor**. Kalan etki kozmetik: AD14 dizisinde fazladan eleman + `RolesTable.tsx:48` sayaç rozetinde +1. Ucuz kapatma: tek satırlık idempotent `DELETE FROM AdminRolePermissions WHERE Permission IN (…)`. Prod henüz deploy edilmediği için gerçek veri riski muhtemelen sıfır | data-hygiene | T132, T136 |
| ✅ | T55-DormantThreshold | **ÇÖZÜLDÜ → WP14:** `dormant_account_value_threshold` dahil **19 zorunlu SystemSetting** (plan "21" stale — WP4a `price_deviation_threshold` + WP12 `timeout_warning_ratio` seed-default ile düştü) `Docs/DEPLOY_RUNBOOK.md §A`'da `SKINORA_SETTING_*` env listesi olarak belgelendi + `.env.example`'a eklendi. Owner kararı: seed-default DEĞİL → fail-fast bilinçli güvenlik korundu (06 §8.9 iş-kritik değerler). | — |

---

## 9. F7 — P2P geçişiyle ertelenen işler

P2P pivotu sırasında bilinçli olarak kapsam dışında bırakılan kalemler (T115, 02 §2.1).

| Önc. | ID | Açıklama | Tip | Kaynak |
|---|---|---|---|---|
| 🟡 | P2P-SettlementTiering | **İtibarlı satıcılar için mutabakat süresini kısaltma.** MVP'de herkes 8 gün bekliyor (02 §4.5.1). Geçmişi temiz satıcılar için sürenin kısaltılması (ör. 20+ başarılı işlem → 24 saat) satıcı deneyimini belirgin biçimde iyileştirir. Riski geçmişe göre fiyatlar. **Ön koşul:** itibar verisinin bu karar için yeterince olgun olması | task | 02 §4.5.1, 10 §4.1 |
| 🟡 | P2P-HotWalletPolicyReview | **Sıcak/soğuk cüzdan politikasının 8 günlük mutabakat süresine göre gözden geçirilmesi.** Para artık işlem başına 8 gün platformda duruyor; aynı anda tutulan toplam tutar custodial modele göre çok daha yüksek. `hot_wallet_limit` eşiği, soğuk cüzdana aktarma sıklığı ve `SweepQueueJob` zamanlaması bu yeni profile göre yeniden hesaplanmalı | task | 02 §4.5.1 |
| 🟡 | P2P-DeliveryPollingJob | **Sürekli teslimat taraması.** MVP'de doğrulama üç noktada çalışıyor: alıcı onayı, dispute açılışı, teslimat süresi sonu (02 §9.2). Dakikalık arka plan taraması eklenirse pasif alıcıda teslimat 2-3 dakikada kapanır; şu an satıcı süre sonuna kadar bekleyebiliyor. `DeliveryVerificationService` bu iş için hazır tasarlandı (saf, yan etkisiz) — job ince bir sarmalayıcı olacak. **Maliyet:** Steam envanter okuma bütçesi ve eşzamanlı teslimat tavanı (08 §2.6) | task | 02 §9.2, 11 T125 |
| 🟡 | P2P-FloatVerification | **Aynı sınıf içindeki kalite farkının doğrulanması.** Eşleştirme `(classid, instanceid)` düzeyinde; float/desen farkı otomatik tespit edilmiyor. Satıcı aynı skinin daha kötü kopyasını gönderirse `WRONG_ITEM` dispute'una ve admin incelemesine kalıyor (02 §9.2). **MALİYET DÜŞTÜ — T122 ölçümü (2026-08-13) bu satırın önkoşulunu çürüttü:** kayıtta *"Çözüm CS2 Game Coordinator istemcisi gerektirir — yeni Steam hesabı + yeni servis"* yazıyordu; oysa Steam'in **anonim** envanter yanıtı asset başına `Wear Rating` (float) ve `Pattern Template` alanlarını `asset_properties` içinde **zaten döndürüyor** (runbook §5). Yani ek Steam hesabı, GC istemcisi veya yeni servis **gerekmiyor** — veri teslimat doğrulamasının hâlihazırda yaptığı okumanın içinde geliyor ve önceliği ⚪ → 🟡. Kalan iş veri toplama değil **eşik kararı**: hangi float farkı "yanlış item" sayılır. Post-MVP'de **fraud sinyali** olarak eklenmeli, durum geçiş kapısı olarak değil (02 §9.2 konjonksiyon kuralından zayıf kanıt üretir) | task | 02 §9.2 (v3.1), 10 §4, T122 runbook §5 |
| 🟡 | P2P-InventoryUnauthorizedMapping 🆕 | **Steam `401` kalıcı bir durumu "tekrar deneyin" diye sunuyor.** T122 ölçümü anonim envanter ucunun dört statü döndürdüğünü gösterdi (`200/401/403/429`); `steamcommunity` kütüphanesi yalnız `403 + null`'ı private olarak tanıyor (`users.js:599-606`), `401` generic hataya düşüyor → sidecar `UNAVAILABLE` → 503 → T121 create ucunda **`STEAM_UNAVAILABLE` (retryable)**. Ölçülen vakaların üçünde 401'in sebebi *"This user has not yet set up their Steam Community profile"* — yani **kalıcı** bir durum; kullanıcı profilini kurana kadar hiçbir tekrar denemede düzelmez ve sonsuz retry döngüsüne yönlendiriliyor. Fail-safe yönü doğru (asla "item yok" kanıtı üretmiyor), kırık olan **kullanıcıya verilen talimat**. T121'in öldürdüğü çöktürmenin bir katman yukarısı: ayrılmayan şey artık "item yok / okunamadı" değil, **kalıcı / geçici**. Not: 401 tek sebebe indirgenmedi — bir vakada profil `public` olduğu hâlde 401 geldi; düzeltme yeni bir statü ayrımı yapmadan önce sebebi netleştirmeli | backend-gap | T122 runbook §2, 07 §6.1, 08 §2.3 |
| 🟡 | P2P-SteamRateLimitWindow 🆕 | **Community rate limit penceresi dakikadan uzun — 10/dk varsayılanı sanıldığı kadar rahat değil.** T120 `STEAM_COMMUNITY_REQUESTS_PER_MINUTE=10`'u 08 §2.6'nın "10-20/dk/IP **tahmini**" aralığının muhafazakâr ucu olarak seçmişti. T122 ölçümü (2026-08-13): 6 sn aralıkla (=10/dk) sürdürülen koşuda **~90 sn / ~18 istekte 429** başladı; ~40 sn'de kısmen toparlandı ama **15 sn aralığa (4/dk) düşülmesine rağmen 429 sürdü** — yani limitleyicinin penceresi bir dakikadan uzun ve yalnız dakikalık hıza bakan `RateLimitedQueue` uzun pencereli bütçeyi tüketebiliyor. `Retry-After` başlığı **gözlenmedi**, gövde `null` → backoff tahmini olmak zorunda. Ölçüm **residential TR IP'sinden** yapıldı; üretim datacenter IP'sinden çıkacak ve Steam datacenter aralıklarına tipik olarak daha sert davranır → gerçek marj buradan dar olabilir, geniş olamaz. Kesin eşik ölçülmedi (Steam'e kasıtlı aşırı yük gerekirdi). Gereken: uzun pencereli ikinci bir bütçe katmanı + 429'a özel backoff, ve 10 §4 kapasite tavanının bu profile göre gözden geçirilmesi | task | T122 runbook §3, 08 §2.6, 10 §4, T120 |
| ⚪ | P2P-SellerDebtLedger | **Satıcı borç defteri.** Satıcı kusurlu iptallerde/teslim etmemede gas ücretini alıcı yiyor (02 §4.6 — owner kararı: mevcut formül korundu). Adil olan, kusurlu tarafa yazmak; ancak satıcının platformda parası olmadığı için kesinti yapılamıyor. Borç defteri (sonraki payout'tan kesme) çözerdi ama yeni bir muhasebe katmanı gerektirir ve satıcı bir daha hiç dönmeyebilir | task | 02 §4.6, 10 §4.1 |
| 🟡 | P2P-NonDeliveryAbuseWindow | **02 §14.2'nin teslim etmeme yaptırımı kodda yok.** v3.0 kuralı: yuvarlanan bir zaman penceresi içinde eşiği aşan **ilk** tekrarda hesaba otomatik `ABNORMAL_BEHAVIOR` flag'i, **sonraki** tekrarda otomatik askı. Kodda karşılığı yok — `FraudPreCheckService`'teki `ABNORMAL_BEHAVIOR` yalnız dormant-hesap kuralı; `CancelCooldownEvaluator` teslim etmemeyi sayıyor ama iptal limitiyle aynı kovada ve yaptırımı yalnız cooldown (askı bacağı yok, teslim etmemeye özel eşik yok). F7 listesinde sahibi yok. **Bugünkü caydırıcılık:** itibar düşüşü (06 §3.1, T119 ile testli) + iptal cooldown'u. **Gerekenler:** iki yeni SystemSetting eşiği (02 §16.2), `FraudFlagService`'e pencere sorgusu, askı bacağının `AdminUserService` askı yoluna bağlanması | task | 02 §14.2, T119 denetimi |
| 🟡 | P2P-DeliveryTimeoutWarning | **Teslimat fazı için timeout uyarısı yok.** 03 §4.5 uyarıyı *"**Tüm timeout'lar için**"* başlığı altında tanımlıyor, 02 §3.4 de faz ayrımı yapmıyor; `WarningDispatcher` ise yalnız `SELLER_CONFIRMED` (ödeme) fazını uyarıyor — v2.0'dan devralınan T48 kapsamı. Yani dört fazın üçü (kabul, hazırlık onayı, teslimat) belgelenmiş uyarıyı hiç göndermiyor. P2P'de teslimat penceresinin sahibi **satıcı** ve teslim etmeme birincil abuse vektörü (02 §14.2); satıcı süresi dolmadan önce hiçbir uyarı almıyor. `timeout_warning_ratio` ayarı ve dispatcher deseni mevcut, ikinci bir faz için tekrar kullanılabilir. **Ön koşul KARŞILANDI (T124, 2026-08-14):** `DeliveryDeadline` artık `ConfirmPayment` geçişinde armlanıyor (`TimeoutSchedulingService.ArmDeliveryDeadlineAsync`), yani uyarının hesaplanacağı süre mevcut. Kalem hâlâ açık — uyarıyı gönderen kod yok | task | 02 §3.4, 11 T124, T119 denetimi |
| ✅ | ~~P2P-BotCodeArchive~~ | **KAPANDI (T133, 2026-08-19).** Bot custody kodunun arşiv işaretçisi — üç halkanın üçü de silindi ve hepsinin git işaretçisi aşağıda. **1) Domain — T117 `82bff4d`:** `TradeOffer`/`PlatformSteamBot`/`BotRecoveryItem` entity'leri, bot seçimi, dispatch, recovery, `SteamWebhooksController` (Steam modülü 35→11 dosya). **2) Backend kalıntısı — T132 `eb0e49d`** (PR [#247](https://github.com/turkerurganci/Skinora/pull/247)): 4 ölü `AuditAction` bot değeri + `AuditLogCategoryMap` girdileri, `VIEW_STEAM_ACCOUNTS`/`MANAGE_STEAM_RECOVERY`, `AdminBotStatusChanged`, `/api/v1/webhooks/steam` middleware dalı + `SteamSharedSecret`. **3) Sidecar — T133** (PR [#248](https://github.com/turkerurganci/Skinora/pull/248)): `sidecar-steam/src/bot/`, `TradeOfferService`/`TradeOfferMonitor`/`trade/types.ts`, `src/webhook/` dizininin tamamı, `link-authenticator.cjs`, `02-register-bot.sql` (19 dosya / 4.016 satır) + üç npm bağımlılığı. **İşaretçi olarak PR numarası kullanıldı, squash sha'sı değil:** sha merge anında doğar, yani satır kendi kapanışını hiçbir zaman yazamıyordu (T132'de de aynı sebeple boş kaldı); `git log --grep "(#248)"` squash commit'i sha'dan bağımsız bulur. Geri dönüş senaryosu yok (eski model Steam kuralı nedeniyle çalışmıyor) ama cooldown'ı olmayan bir oyuna genişlerken referans olabilir | doc-drift | 11 T132/T133 |
| 🟡 | T133a-PaymentDetailNulls 🆕 | **07 §7.5 `payment.status` / `txHash` / `confirmedAt` sözleşmede vaat ediliyor, kod üçünü de sabit `null` döndürüyor.** `TransactionDetailService.BuildPaymentAsync` `TransactionPaymentDto`'yu her zaman `Status: null, TxHash: null, ConfirmedAt: null` ile kuruyor; kodun kendi yorumu bunu ödeme bacaklarının bağlanmasına bırakıyor. T133a bu satırları **bilinçli olarak DEĞİŞTİRMEDİ**: sözleşmeyi bugünkü eksik davranışa çekmek hatayı kalıcılaştırırdı — eksik olan doküman değil koddur. FE ödeme onayı bilgisini gösteremiyor. **Gereken:** `BlockchainTransaction` ödeme kaydından (`PAYMENT` tipi) status/txHash/confirmedAt projeksiyonu | backend-gap | 07 §7.5, T133a |
| 🟡 | T133a-DisputeBlockNulls 🆕 | **07 §7.5 `dispute` bloğu sözleşmede tanımlı, `TransactionDetailService` `Dispute: null` döndürüyor.** `DisputeSummaryDto`'nun repo genelinde hiçbir üreticisi yok (yalnız DTO tanımı ve mapping) — T58 açığı. T133a sözleşmeyi **bilinçli olarak korudu**; kapanış kodda yapılacak. Etki: işlem detayında açık dispute bilgisi görünmüyor, alıcı/satıcı itirazın durumunu yalnız bildirimden öğreniyor | backend-gap | 07 §7.5, T58, T133a |
| ✅ | ~~T133a-ActiveCounterRefunded~~ | **ÇÖZÜLDÜ → F7-sonrası parite turu.** İstenen ("iki dışlama listesine `REFUNDED` + regresyon testi") yapıldı ve **kapsam üçe genişledi**: aynı sapma `FraudFlagAdminQueryService`'in iki yükleminde ve `FraudFlagService.ApplyEmergencyHoldCascadeAsync`'te de vardı — AD3 yüklemi kendi yorumunda *"AD19d ile aynı aktif tanımı: **beş** terminal state hariç"* diyordu, oysa AD19d **altı** dışlıyordu; parite ölmüş, parite iddiası yaşamaya devam etmişti. Auto-hold cascade'in sonucu **ölçüldü — çökme değil** (`TimeoutFreezeService` her zaman `TimeoutRemainingSeconds` yazdığı için `CK_Transactions_FreezeActive` tutuyor): etki, kapanmış bir iadeyi dondurup audit satırı yazmak ve iki tarafa `EMERGENCY_HOLD_APPLIED` göndermekti — metodun kendi özeti "already terminal olanları atlar" diyordu. **Yapısal kapanış:** beş yüklem + zaten doğru olan üç `_terminalStates` kopyası tek kaynağa (`TransactionStatusSets`) indirgendi; guard **türetilmiş** (bir statü, state machine ondan hiçbir tetikleyiciye izin vermiyorsa terminaldir) — ikinci bir elle yazılmış liste yok, çünkü kusur tam olarak oydu. Üç regresyon testinin ayırt ediciliği **mutasyonla ölçüldü** | backend-gap | 02 §12.3, 02 §19, T133a → F7-sonrası parite turu |
| ✅ | T133a-FeI18nEmergencyHoldLabel | **ÇÖZÜLDÜ → T134** — dört dilin `adminRoles.permissions.EMERGENCY_HOLD` etiketi kod kataloğuna hizalandı (07 §9.11 / 04 §8.8: "İşlemleri acil dondurma/kaldırma"); EN/ES/ZH nüshaları da aynı anlamı (nesne = işlemler) taşıyacak şekilde yeniden yazıldı. Tarihsel kayıt: S19 ekranı sunucu label’ını değil i18n metnini bastığı için sapma kullanıcıya yansıyordu | doc-drift | 04 §8.8, 07 §9.11, T133a, T134 |
| ✅ | T133a-FePermissionCatalogKeys | **ÇÖZÜLDÜ → T136** — FE `permissionCatalog.ts` 14 → **12** anahtar; iki ölü anahtar (`VIEW_STEAM_ACCOUNTS`, `MANAGE_STEAM_RECOVERY`) 5. ve 6. sıradaydı, çıkarılınca kalan liste backend `PermissionCatalog.All` ile **sıra dahil birebir** oldu (dosyanın kendi "12 admin permissions" yorumu baştan doğruydu — yanlış olan diziydi). Aynı iki ölü etiket dört i18n dosyasından da düştü. **Satırın açık bıraktığı yapısal soru — bekçi yok — T136'da ayrıca kapandı:** `lib/admin/permissionCatalog.parity.test.ts` FE dizisini C# kaynağıyla (sıra dahil) ve dört dilin `adminRoles.permissions` bloğuyla karşılaştırıyor; beş mutasyonla ölçüldü, hayatta kalan mutant yok. Mevcut kapılar bunu tutamıyordu: `enums.parity.test.ts` kataloğu göremez (static class, `public enum` regex'i tutmaz), `check-i18n.mjs` yalnız diller arası parity ölçer | doc-drift | 07 §9.11, T132, T133a, T136 |
| ✅ | T133a-Doc02NotificationCopy | **ÇÖZÜLDÜ → T134** — FE nüshası 28 → **26**: iki v3.0 tipi eklendi (`PAYMENT_WINDOW_OPEN`, `DELIVERY_EXPECTED`), dört emekli tip düştü (`ITEM_ESCROWED`, `TRADE_OFFER_SENT_TO_BUYER`, `ITEM_RETURNED`, `ADMIN_STEAM_BOT_ISSUE`) — `types/enums.ts` + `lib/utils/notification-icons.ts` (ikon kategorileri: `PAYMENT_WINDOW_OPEN` → 💰, `DELIVERY_EXPECTED` → 🔄). **Satırın asıl sorusu — "parity’yi zorlayan bekçi yok" — da kapandı:** `src/types/enums.parity.test.ts` FE nüshasını C# kaynağıyla **değer ve sıra** olarak karşılaştırır, `src/i18n/catalog-parity.test.ts` i18n kataloglarını enum ile karşılaştırır. Kapsam sınırı bekçinin kendi başlığında yazılı (string-union nüshaları hariç → `T134-FeEnumUnionDup`) | doc-drift | 06 §2.13, 07 §8.1, T133a, T134 |
| 🟡 | T133b-LiveRehearsalUnrun 🆕 | **DEPLOY_RUNBOOK §G.4'ün P2P prova reçetesi hiç canlı koşulmadı.** T133b anlatıyı v3.0 akışına çekti ve her adımı **kod** üzerinden doğruladı (uç adları, state machine geçişleri, cron ifadeleri, ayar tabanı) — ama gerçek Steam hesabı çifti + `STEAM_API_KEY` + fonlu Nile testnet cüzdanı gerektiren **canlı prova yapılmadı** (T133b "Neden ayrı task" gerekçesi bunu istiyordu). Kod kanıtı ad doğruluğu için güçlü, **davranış** doğruluğu için zayıftır: sıralama, bekleme süreleri ve Steam'in gerçek hold davranışı ölçülmedi. Ayrıca kontrol 10a'nın prova kısayolu (`PayoutEligibleAt` saat kaydırması) da hiç çalıştırılmadı. **Sahibi: ilk gerçek deploy hazırlığı** — §H (teslimat kanıtı kapısı) ve §I (mutabakat kapısı) zaten aynı ilk-teslimat ölçümünü bekliyor, prova onlarla birlikte koşulmalıdır | doc-drift | DEPLOY_RUNBOOK §G.4/§G.5, §H, §I, T133b |
| 🟡 | T139-ActiveMonitorQuotaAlarm 🆕 | **Aktif izleyici sayısının sağlayıcı rate limit'ine karşı alarmı yok.** T139 doğrulamasının N1 bulgusu ölçtü: D3 pencereyi sweep'e kadar açık tuttuğu ve sweep `payout_settlement_days`'in **7 günlük sert tabanından** önce kuyruklanamadığı için eşzamanlı izleyici sayısı iki saatlik değil **bir haftalık** işlem hacmiyle ölçekleniyor; her izleyici her tick'te iki TronGrid sorgu fazı üretiyor. Bedel 08 §3.4'e yazıldı ve kabul edildi (pencereyi kısaltmak 02 §4.4 fazla-tutar ve 03 §5.5 ikinci-ödeme kollarını öldürür), ama **eşiği gözleyen bir şey yok**: `skinora_blockchain_active_monitors` gauge'u yayınlanıyor ve `integration-metrics` Grafana paneli onu çiziyor, ama üzerinde alarm kuralı tanımlı değil. **Gauge'un kendisi tur 2'de düzeltildi** (bulgu N2 — iki registry etiketsiz gauge'a ayrı ayrı yazdığı için toplam değil son yazan raporlanıyordu); artık aktif + post-cancel izleyicilerin toplamı, yani eşik ikisini birden kapsamalı. Sahibi: ilk kapasite planlaması / launch öncesi gözlenebilirlik turu — TronGrid planının istek bütçesi bilinmeden eşik sayı olarak yazılamaz | ops-config | T139 doğrulaması 2026-08-20, 08 §3.4, `metrics.ts:68`, `activeMonitorGauge.ts` |
| 🟡 | T134-Doc06DeliveryDeferred 🆕 | **06 §2.23 `DeliveryStatus` üç değer sayıyor, kod dört taşıyor.** `DeliveryStatus.DEFERRED` bildirim gönderiminin sessiz-saat/bakım erteleme durumu olarak kodda gerçek ve yazılıyor (`DeferredNotificationDeliveryJob.cs:193`, `NotificationDeliveryJob.cs:184`); dokümanın tablosu `PENDING`/`SENT`/`FAILED` ile kalmış. T134 **FE nüshasını koda** hizaladı (enum bekçisi kodu yetkili sayar) — dolayısıyla bugün FE ile 06 arasında bir satırlık fark var. Kapanış: 06 §2.23 tablosuna `DEFERRED` satırı (doküman turu; GUARDRAILS §3 gereği proje sahibi onayı ile) | doc-drift | 06 §2.23, T134 |
| ⚪ | T134-FeEnumUnionDup 🆕 | **`EmergencyHoldReleaseAction` FE’de iki ayrı string-union nüshası olarak yaşıyor, `enums.ts`’te hiç yok.** `lib/api/admin.ts:527` ve `lib/signalr/events.ts:37` aynı iki değeri (`RESUME`/`CANCEL`) bağımsız olarak tanımlıyor; C# karşılığı `Skinora.Shared/Enums/EmergencyHoldReleaseAction.cs`. Bugün **sapma yok** — açık, T134’ün eklediği enum bekçisinin bu iki nüshayı **görmemesi**: bekçi yalnız `enums.ts`’in `export enum` bloklarını karşılaştırır. Backend’e üçüncü bir değer eklendiğinde iki nüsha da sessizce eskir. Kapanış: nüshaları `enums.ts`’e taşımak (tüketici tip değişikliği: literal atamaları enum üyesine çevrilir) ya da bekçiyi union sözdizimini de okuyacak şekilde genişletmek | fe-drift | T134, 06 §2 |
| 🟡 | T135-FeDisputeTypeChoices 🆕 | **`DisputeForm` üç dispute türünü de koşulsuz sunuyor, sunucunun `availableActions.disputableTypes` alanını hiç okumuyor.** Alan WP5'te (T58-canDisputeEnvelopeBit) tam bu iş için eklendi — 07 §7.5: "per-type dispute eligibility so the FE surfaces only the dispute types currently openable" — ve backend `DisputeEligibility.AllowedStatesByType`'tan doğru üretiyor; FE tipinde (`TransactionDetailAvailableActions`) alan hiç tanımlı değil, `DisputeForm.tsx:208` üç türü sabit dizi olarak çiziyor. Sonuç **kullanıcıya yansıyor**: SELLER_CONFIRMED'da yalnız `PAYMENT` açılabilirken alıcı `DELIVERY`/`WRONG_ITEM` seçebiliyor ve uç reddediyor — yani sözleşmenin önlemek için var olduğu tam senaryo. T135'in ölçtüğü ama kapsamadığı bir açık (matris satırı değil, dispute UI'ı — T92/WP5 bölgesi). Kapanış: FE tipine `disputableTypes` + formun tür listesini ondan türetmesi | fe-drift | 07 §7.5, WP5, T92, T135 |
| ⚪ | T135-FeTimeoutLabelKeyStale 🆕 | **`components/transactions/detail/helpers.ts` `timeoutLabelKey` hem ÖLÜ hem BAYAT.** Repo genelinde çağıranı yok (tek eşleşme kendi tanımı) ve tanıdığı küme custodial dönemin adlarını taşıyor (`buyer_acceptance`, `seller_trade_offer`, `buyer_trade_offer`), oysa backend `TransactionDetailService.BuildTimeout` bugün `accept` / `seller_confirm` / `payment` / `delivery` / `settlement` yayınlıyor (07 §7.5). Bugün **kırık değil** çünkü kimse çağırmıyor — risk, birinin onu "hazır helper" sanıp bağlaması: beş tipin dördü `generic`'e düşerdi. Kapanış: ya silinsin ya beş değere hizalanıp bir bekçiyle bağlansın | fe-drift | 07 §7.5, T117, T135 |
| ⚪ | T135-TimelineHoldPreviousStatus 🆕 | **EMERGENCY_HOLD'da timeline sabit `SELLER_CONFIRMED` çiziyor, `holdInfo.previousStatus`'u okumuyor.** `transactions/[id]/page.tsx` overlay statüsünü timeline'a çevirirken sunucunun gönderdiği gerçek alt statüyü (FE tipinde MEVCUT — `lib/api/transactions.ts` `TransactionDetailHoldInfo.previousStatus`, 07 §7.5) yok sayıyor; dondurulmuş bir `PAYMENT_RECEIVED` işlem bir adım geride görünüyor. **T134 doğrulamasının G2 gözlemi** olarak kaydedilmişti ve sahibi yoktu; T135 matris turu da onu kapsamıyor (timeline, aksiyon paneli değil). Kapanış tek satırlık: sabit yerine `holdInfo?.previousStatus`'u (bilinmeyen değere karşı korumalı) geçirmek + bir render testi | fe-drift | 04 §7.3, 07 §7.5, T134 G2, T135 |
| ⚪ | T135-IntegrationSuiteParallelFlake 🆕 | **Integration suite'i paralel assembly koşumunda kararsız — üç koşum, üç FARKLI düşen küme, hepsi izole olarak temiz.** T135'te Docker açılıp lokal ölçüm yapılırken gözlendi (CI'nın birebir eşdeğeri: tek paylaşılan SQL Server + `INTEGRATION_TEST_SQL_SERVER`). Koşum A (değişken set EDİLMEDEN, her test sınıfı kendi container'ı): 28 düşen — bu koşum CI eşdeğeri değildi, sayılmaz. Koşum B (paylaşılan sunucu): **6 düşen, hepsi `Skinora.Fraud.Tests`** — assembly tek başına 73/73. Koşum C (aynı kod, aynı kurulum): **2 düşen** ve bu kez Fraud temiz — `NotificationDispatcherTests.DispatchAsync_PushesNewNotificationAndUnreadCount_ViaRealtimePublisher` + `SettlementVerificationServiceTests.BuyerInventoryPrivate_IsInconclusive_NotADeparture`; ikisi de izole 8/8 ve 10/10. **GÜÇLÜ ÇELİŞTİRİCİ (over-claim edilmemeli):** ölçüm makinesinde aynı anda projenin tüm compose stack'i (8 konteyner) + Docker Desktop koşuyordu, yani CPU/RAM çekişmesi tek başına açıklayabilir; CI iki koşumda da (`32461001539`, `32462859570`) yeşildi. **Kapanış: temiz bir makinede / CI'da tekrarlı koşumla ölçmek** — gerçekten sıra bağımlılığı mı yoksa yalnız kaynak çekişmesi mi olduğunu söyleyecek tek şey bu. Sıra bağımlılığıysa paylaşılan sunucuda sınıflar arası state sızıntısı aranmalı | test-infra | T135 lokal ölçümü 2026-08-21, `ci.yml` §4, `IntegrationTestBase` |
| 🟡 | F7Gate-OrphanNotificationTypes 🆕 | **06 §2.13'ün dört bildirim tipinin üretim üreticisi yok — katalog gönderilmeyen bildirim vaat ediyor.** `TRANSACTION_FLAGGED` (hedef: Satıcı, "İşlem incelemeye alındı") · `PAYMENT_INCORRECT` (Alıcı) · `PAYMENT_REFUNDED` (Alıcı, "İptal/timeout sonrası ödeme iade edildi") · `FLAG_RESOLVED` (Satıcı, "Flag sonuçlandı"). Ölçüm: dördü de prod kaynakta yalnız `EmailCategoryMap`'te geçiyor, hiçbir `NotificationConsumer` üretmiyor; `FraudFlagApprovedEvent`/`FraudFlagRejectedEvent`'in **yalnız realtime** tüketicisi var (`FraudFlagApproved/RejectedRealtimeConsumer`), bildirim tüketicisi yok. **`T138-DeliveryExpectedNeverPublished` → T140 ile birebir aynı aile** (tüketici/katalog yazılmış, üretici hiç bağlanmamış) ve o satır listenin tek 🔴'sıydı. **F7 regresyonu DEĞİL:** `git grep` `phase/F6-pass` ağacında dördünü de **aynı hâlde** buldu — F7'nin ortaya çıkardığı taban borcu. **Karar açık:** ya (a) üreticileri bağla (06 §2.13'ün hedef kolonu bağlayıcı kabul edilir), ya (b) katalogda `superseded/reserved` işaretle — `PAYMENT_INCORRECT` fiilen T72'nin üç ince tipiyle (`INSUFFICIENT_PAYMENT`/`OVERPAYMENT_REFUNDED`/`WRONG_TOKEN_REFUND`), `PAYMENT_REFUNDED` `TRANSACTION_CANCELLED`+`DISPUTE_RESULT` ile, flag tipleri realtime push'la karşılanıyor **görünüyor** ama bu gate'te hangisinin doğru olduğu **ölçülmedi**. Kapanış turu önce bunu ölçmeli. Karar verilirse 06 §2.13 + 07 §8.1 birlikte güncellenir | backend-gap / doc | F7 Gate Check 2026-08-22 bulgu **F7-N5**, `GATE_CHECK_F7.md` §FK3 |
| ⚪ | F7Gate-EventsWithoutConsumer 🆕 | **İki domain event yayınlanıyor ama tüketicisi yok.** `LatePaymentMonitorRequestedEvent` (`TimeoutSideEffectPublisher.cs:60`, her ödeme-fazı timeout'unda) ve `SellerPayoutIssueResolvedEvent` (`PayoutIssueService.cs:197`). **REFUTE TURU KOŞULDU — bu bir para yolu deliği DEĞİL:** geç-ödeme izlemesi fiilen `PostCancelMonitorStarter` → `PostCancelMonitorStartRequestedEvent` → `PostCancelMonitorStartDispatcher` → sidecar `PostCancelMonitor` (`latePaymentDetected` webhook'unu yayan yer) üzerinden **armlanıyor**, ve `DeadlineScannerJob` + `TimeoutExecutor` bu yolu çağırıyor. Yani `LatePaymentMonitorRequestedEvent` **aşılmış/gereksiz** bir yayın: her ödeme timeout'unda boşa bir outbox satırı ve yanıltıcı bir "bu iş yapılıyor" sinyali. `SellerPayoutIssueResolvedEvent` için karşılık gelen bir ikinci yol ölçülmedi. **F7 regresyonu DEĞİL** — ikisi de `phase/F6-pass` ağacında aynı hâlde. Kapanış: ya tüketici bağla ya yayını kaldır; kaldırma tercih edilirse önce `SellerPayoutIssueResolvedEvent`'in gerçekten karşılıksız olduğu ölçülmeli | backend-hygiene | F7 Gate Check 2026-08-22 bulgu **F7-N6**, `GATE_CHECK_F7.md` §FK4 |
| ✅ | T138-DeliveryExpectedNeverPublished | **ÇÖZÜLDÜ → T140** — `AdvanceStateMachineAsync` artık geçişle **aynı** `SaveChanges` içinde `TransactionStatusChangedEvent(SELLER_CONFIRMED → PAYMENT_RECEIVED)` yayınlıyor; `HappyPathMilestoneNotificationConsumer`'ın ölü `PAYMENT_RECEIVED` kolu canlandı ve satıcı `DELIVERY_EXPECTED` satırını alıyor. **Satır kendisinden geniş kapandı, üç noktada:** (a) **KÖK NEDEN** — açık unutkanlık değildi: olayın XML doc'unun paragraf 1'i ödeme onayını "bu olayı yayınlaMAmalı, yoksa çift-push olur" listesinde **adıyla sayıyordu**, aynı doc'un paragraf 2'si ise üreticiyi "T124 yazar" diyordu. İki paragraf çelişiyordu ve T124 **yazılı talimata uydu** — T123 kendi bacağını bağladı, T124 bağlamadı. Kural custody döneminde doğruydu (o çağda bu bacağın bildirimi `TRADE_OFFER_SENT_TO_BUYER` bot dispatch job'ından geliyordu); v3.0 bildirimi olayın üstüne taşıyıp alıcısını satıcıya çevirdi, kural bayat kaldı. Düzeltilmeseydi açık **yeniden üretilebilir** kalırdı. (b) **ÇİFT-PUSH VARSAYIM DEĞİL, ÖLÇÜLDÜ** — `PaymentReceivedRealtimeConsumer` **zaten** aynı `TransactionStatusChanged` payload'ını hardcoded `FromStatus` ile push ediyordu. Proje sahibi kararıyla (2026-08-22) push'un sahibi generic relay oldu ve hardcoded kaldıraç **silindi**; alternatif — relay'e `ToStatus == PAYMENT_RECEIVED` skip filtresi — reddedildi, çünkü relay'in tek sözleşmesi "producer'ın kararını sorgulamadan verbatim ilet" ve ilk per-status istisna onu kalıcı olarak delerdi. Net etki: bir kaldıraç **eksildi**. (c) **FAZLA ÖDEME KOLU DA KAPSANDI** — yayın "tam tutar" koluna değil `AdvanceStateMachineAsync`'in **içine** kondu: fazla ödeme durumu ilerletir, yani satıcı item'ı aynen borçludur ve teslimat saati ona karşı işler; yalnız temiz kola bağlı bir yayıncı o satıcıyı tam aynı sessiz yolla cezalandırırdı. E2E işareti çevrildi ve **tipten alıcıya yükseltildi**: yeni `getNotificationRecipients` ile alıcı kümesinin `[sellerId]`'e eşitliği iddia ediliyor — tipin varlığı yalnız "bir üretici ateşledi"yi kanıtlar, yanlış tarafa adreslenmiş bir bildirim satıcıyı açık kadar uyarısız bırakır ve tip-bazlı iddia yeşil kalırdı | backend-gap | T138, T133b-B4 (sınıf), T140 |

> **Bot katmanına ait eski kalemler geçersizleşti — ve 2026-08-22'de KAPATILDI.** §1 (T69 — bot health/failover/recovery) ve §2 (T103b — Steam hesapları backend) bölümlerindeki üç açık kalem (`T69-K4`, `T68-K1`, `T64-BotWebhookHandler`) P2P geçişiyle konusuz kalmıştı: platform Steam hesabı işletmediği için bot durumu yayını, bot oturum hatası bildirimi ve bot webhook handler'ı diye bir şey yoktur (02 §15, 05 §3.2). **T116 bunu not etmiş ama satırları ⚪ AKTİF bırakmıştı** — yani dosyanın kendisi "yeni iş üretmezler" derken sayaç onları üç açık iş olarak sayıyordu ve backlog'u sırayla işleyen herkes ilk üç kalemde ölü işe çarpıyordu. Backlog hijyen turunda (2026-08-22) üçü de **ölçülerek** doğrulandı (`AdminBotStatusChanged` → 0 eşleşme · `BOT_SESSION_FAILED` AuditAction yok · `session_failed`/`removed_from_pool` → 0 eşleşme) ve `✅ KONUSUZ KALDI` olarak kapatıldı. **Satırlar tarihsel izlenebilirlik için yerinde duruyor** (GUARDRAILS §4) — yalnız işaretleri gerçeği söylüyor artık.

---

## 10. UI turu (2026-08-23) — gerçek Steam oturumuyla tarayıcı testi bulguları

> Kaynak: [`Docs/TEST_REPORTS/UI_TOUR_2026-08-23.md`](TEST_REPORTS/UI_TOUR_2026-08-23.md) · düzeltme planı: [`UI_TOUR_2026-08-23_FIX_PLAN.md`](TEST_REPORTS/UI_TOUR_2026-08-23_FIX_PLAN.md)
>
> **Turun var oluş nedeni ve dokuz satırın ortak kökeni aynı:** E2E ağı (10 süit) Steam OAuth'u JWT enjeksiyonuyla **atlıyor** (`e2e/src/browser.ts`: *"bypassing Steam OAuth, which cannot be scripted"*), dolayısıyla F6 ve F7 gate'lerine kadar hiçbir ekran **gerçek bir oturumla** görülmemişti. Dokuz bulgunun hiçbiri aranarak bulunmadı; hepsi ürün kullanılmaya çalışılırken düştü — `ForwardedHeadersNotRegistered` dersinin (*"bir alanın dolu olması doğru olduğu anlamına gelmez"*) ölçeklenmiş hâli.

| Önc. | ID | Açıklama | Kaynak |
|---|---|---|---|
| ✅ | UITour-NoUiPathToVerifyMobileAuthenticator | **ÇÖZÜLDÜ → F1 ([#268](https://github.com/turkerurganci/Skinora/pull/268))** — yeni `SteamTradeUrlSection` U17'nin ilk UI çağıranı; hem Ayarlar'a hem de kullanıcının engellendiğinde yönlendirildiği `/auth/mobile-authenticator` sayfasına mount edildi. Backend'e dokunulmadı (turun ayırt edici testi backend yarısının eksiksiz olduğunu göstermişti). U17'nin üç cevabı ayrı gösteriliyor ve "Steam erişilemedi" **sessiz başarı olarak gösterilmiyor**. **Uçtan uca doğrulandı** (kullanıcı MA'sız hâle döndürülüp gerçek arayüzden): engel → Ayarlar'dan kayıt → rozet sayfa yenilenmeden `true` → `/transactions/new` 4 adımlı sihirbazı çizdi. **Eski açıklama:** **Hiçbir kullanıcı UI üzerinden işlem oluşturamıyor — kapalı döngü.** `/transactions/new` → *"MA aktif değil"* engeli (ölçüldü: sayfadaki form alanı sayısı **0**, `GET /transactions/eligibility` → `reasons:["MOBILE_AUTHENTICATOR_REQUIRED",…]`) → tek çıkış **"MA'yı aktifleştir"** → `/auth/mobile-authenticator` → o sayfanın tek eylemi "yeniden kontrol et" ve **Steam'e hiç sormuyor**; kendi yorumu: *"recheck just re-reads the latest verified state from /auth/me (WP11 decision)"* → `User.MobileAuthenticatorVerified`'ı yazan **tek** kod `SteamTradeUrlService.cs:46` = **U17** (`PUT /users/me/settings/steam/trade-url`) → **U17'yi çağıran hiçbir frontend kodu yok** (`steam/trade-url` dizgisi frontend'de yalnız `lib/api/users.ts:50`'deki bir **doc yorumunda** geçiyor; Ayarlar/Profil'de alan yok, kabul formundaki alan `defaultSteamTradeUrl` ile sadece **ön-doldurma**) → kabul akışı da bayrağı yazmıyor (`TransactionAcceptanceService` URL'i body'den okuyup canlı probe yapıyor, alanlara dokunmuyor). Bayrak `false` doğuyor (`UserConfiguration.cs:125`) ve arayüzde `true` yapmanın yolu yok. **Kullanıcının MA'sı gerçekten açıkken bile reddediliyor** — Steam'den ölçüldü: `{"active":true,"escrowEndDurationSeconds":0}`. **Ayırt edici test — backend mi bozuk, UI mı eksik:** U17 doğrudan çağrıldı → `200` + `mobileAuthenticatorActive:true` → `/auth/me` true → eligibility'den `MOBILE_AUTHENTICATOR_REQUIRED` **düştü** → `/transactions/new` **açıldı** ve 4 adımlı sihirbaz çizildi. **Backend yarısı eksiksiz; eksik olan yalnız UI çağrısı.** **Neden dört faz görmedi:** E2E süitleri MA'yı JWT enjeksiyonu veya doğrudan API ile geçiyor; hiçbiri *"yeni kullanıcı arayüzden nasıl uygun hâle gelir"* sorusunu sormuyor. Düzeltme: plan §F1 | ölçüm 2026-08-23 UI turu; `SteamTradeUrlService.cs:46`, `lib/api/users.ts:50`, `auth/mobile-authenticator/page.tsx`, `TransactionAcceptanceService.cs` |
| ✅ | UITour-InventoryClientRefusedBySteam | **ÇÖZÜLDÜ → F2 ([#269](https://github.com/turkerurganci/Skinora/pull/269))** — `steamcommunity` paketinin eski `request` yığını yerine doğrudan `fetch` (`HttpInventoryFetcher`). Kütüphanenin gözlenebilir davranışı birebir korundu (sayfalama · `classid_instanceid` birleştirme · `asset_properties` · `total_inventory_count` son sayfadan · gizli envanter TAM olarak `This profile is private.` dizgesiyle). `getImageURL()` bilerek **metot** olarak eklendi — düz JSON döndürseydik her item'ın görseli sessizce `null` olurdu. 429/5xx için üstel backoff + 50 sayfalık döngü sınırı eklendi. **Canlı doğrulama:** sidecar `PUBLIC` 1/1 item, `/transactions/new` gerçek envanteri çizdi, item görseli `naturalWidth=512`. **Eski açıklama:** **Sidecar'ın envanter istemcisi Steam tarafından reddediliyor; üretimde "Steam çöktü" gibi görünür.** MA engeli aşıldıktan sonra sihirbazın 1. adımı **"Envanter okunamadı"** diyor. **Aynı konteynerin içinden, aynı dakikada ölçüldü:** `steamcommunity` paketi (`getUserInventoryContents`) → **429**, her denemede · düz `fetch`, **birebir aynı URL + `Referer` + `count=1000`** → **200**, her denemede. Yani Steam bizi engellemiyor, **paketin gönderdiği isteğin kendisi** reddediliyor. **Ölçümle elenen hipotezler:** User-Agent (paketin gönderdiği Chrome UA ile de 200) · URL biçimi (`start_assetid` boş, sondaki eğik çizgi — üç varyant da 200) · profil gizliliği (açıldı, doğrudan istek 200) · `STEAM_API_KEY` (envanter yolu anahtar kullanmıyor — sidecar'ın kendi yorumu: *"a missing/empty STEAM_API_KEY degrades a single route (trade-hold → 503)"*). Geriye paketin kullandığı **eski `request` HTTP yığını** kalıyor. Ucuz düzeltme yönü: `getUserInventoryContents` yerine doğrudan `fetch` ile sayfalama — `InventoryService` zaten bir port arkasında ayrılmış (*"tests inject a stub without monkey-patching steamcommunity"*), yani değişim tek adaptörle sınırlı. Korunması gerekenler: sayfalama (`start_assetid`), `403`+`null` → **private** ayrımı, gerçek `429` → backoff, Redis cache (120 sn), `RateLimitedQueue` bütçesi. **Önemi:** envanter okunamazsa satıcı item seçemez; `UITour-NoUiPathToVerifyMobileAuthenticator` düzeltilse **bile** işlem oluşturulamaz — ikisi aynı sürümde çıkmalı. Düzeltme: plan §F2 | ölçüm 2026-08-23 UI turu; `sidecar-steam/src/trade/InventoryService.ts`, `steamcommunity@3.x` |
| ✅ | UITour-AuthBucketIncludesSessionReads | **ÇÖZÜLDÜ → F3b + F3c ([#270](https://github.com/turkerurganci/Skinora/pull/270))** — `/auth/me` → `user-read` (kullanıcı bazlı), `/refresh` + `/logout` → yeni `session` kovası (30/60sn); sınıf düzeyi `auth` kovası (10/60sn) yalnız brute-force yüzeyinde kaldı. **F3c:** `AdminGuard` artık "cevap alamadım" ile "yetkin yok"u ayırıyor — 401 → oturum yok, diğer hatalar → yeniden denenebilir hata, yönlendirme YOK; mutasyon turu koşuldu (eski davranışa dönünce tam olarak yeni davranışı iddia eden iki test kırıldı). Not: `RateLimitTests`'in refresh testi sabit "10 istek" varsayımından kurtarıldı — ölçtüğü şey limitin DEĞERİ değil, aşıldığında dönen cevabın BİÇİMİ. **Eski açıklama:** **Kimliksiz bir istemci 10 istekle tüm platformun oturum katmanını 60 saniyeliğine düşürebiliyor.** Ölçüm (`:8080` üzerinden, **token olmadan**): `1→401 … 10→401, 11→429 … 14→429`, `X-RateLimit-Limit: 10 · Retry-After: 60 · RATE_LIMIT_EXCEEDED`. **Mekanizma:** `auth` politikası **10/60 sn** (`appsettings.json:145`) ve `RateLimitMiddleware.UserScopedPolicies`'te **yok** → IP bazlı; `AuthController` **sınıf düzeyinde** `[RateLimit("auth")]` taşıyor ve `[HttpGet("me")]` için istisna yok → **her sayfa yüklemesinin yaptığı `/auth/me`**, ayrıca `/refresh` ve `/logout` giriş denemeleriyle **aynı kovada**. Kovayı tüketen 10 istek **401**'di, yani token gerekmiyor. **Kullanıcının gördüğü hata değil, sessiz bozulma:** `/auth/me` 429 dönünce `AdminGuard` rolü çözemiyor ve süper admini `/tr/dashboard`'a atıyor — *"admin değilsin"* gibi; turun ilk koşusunda dört admin rotası tam olarak böyle düştü ve ilk teşhis "yetki sorunu" oldu, kova dolunca aynı dokuz rota **9/9 açıldı**. **Ölçülen pratik bütçe:** tam sayfa yüklemesi başına **2** `/auth/me` → platform genelinde **~5 sayfa yüklemesi/dakika**. **`ForwardedHeadersNotRegistered` ile ilişkisi ayırt edildi, tek sebebe bağlanmadı:** o satır **yayılma yarıçapını** açıklıyor (proxy IP'si → kova tüm platform için tek) ve düzeltilirse etki tek IP'ye iner; **bu satır kova tasarımıdır** ve forwarded headers düzelse **bile** NAT/CGNAT arkasındaki bir ofisi veya iki sekme açan tek kullanıcıyı kırar. Üçüncü parça: 429 ile "yetkisiz"in ayrılmaması (`AdminGuard`). Düzeltme: plan §F3b + §F3c (§F3a = mevcut 🔴) | ölçüm 2026-08-23 UI turu; `appsettings.json:145`, `RateLimitMiddleware.cs:32-39`, `AuthController.cs:22,232`, `lib/auth/AdminGuard.tsx` |
| ✅ | UITour-SignupLanguageHardcodedEn | **ÇÖZÜLDÜ → F4 ([#272](https://github.com/turkerurganci/Skinora/pull/272)) + F4a [#276](https://github.com/turkerurganci/Skinora/pull/276).** **F4 TEK BAŞINA YETMEDİ ve bu ölçülerek bulundu:** F4'ün canlı probe'u hazırlanırken zincir simüle edildi ve ürünün **birincil kayıt yolunda** dilin hâlâ `en`'e düştüğü görüldü — `/tr` açılış sayfasının kayıt CTA'sı `returnUrl` hiç geçirmiyor, login sayfası da locale'siz `"/dashboard"`a düşüyordu, dolayısıyla A1'e giden istek `?returnUrl=%2Fdashboard` oluyordu ve `FromPathPrefix("/dashboard")` → `null` → `en`. Kontrol turu (elle `?returnUrl=%2Ftr%2Fdashboard`) doğru URL'i üretti: **mekanizma çalışıyordu, ona doğru veri gelmiyordu.** **F4a** `sanitizeReturnUrl`'ü locale-farkında yaptı (varsayılan `/{locale}/dashboard`; locale'siz returnUrl'e önek; zaten locale taşıyana dokunmaz; uygulama-dışı değer atılır) ve **ikinci semptomu da aynı kökten kapattı** — `/dashboard`'a dönen Türkçe ziyaretçi `NEXT_LOCALE` cookie'si yoksa `/en/dashboard`'a düşüyordu. **Canlı ölçüm** (image `main`'den kurulup gerçek tarayıcıyla): `/tr/auth/login` → önce `returnUrl=%2Fdashboard`, sonra `%2Ftr%2Fdashboard`; dört dilde kendi öneki (`en/tr/es/zh`); A1'in cookie'yi bu değerden kurduğu da ölçüldü (`Set-Cookie: skinora_oid_rt=%2Ftr%2Fdashboard`). 6 yeni test **mutasyonla doğrulandı** (varsayılan eski hâline çevrilince 4 test kırıldı) ve testler fonksiyonu doğrudan çağırmıyor, **butona tıklayıp gidilen A1 URL'ini** okuyor — kusur "fonksiyon doğru ama bileşen onu locale ile beslemiyor" katmanındaydı. **CANLI PROBE ✓ GEÇTİ (2026-08-24, gerçek Steam hesabıyla).** Zincirin son halkası da ölçüldü: kullanıcı `/tr` arayüzünden **yeni** bir Steam hesabıyla (`76561198652999063`) kayıt oldu → `Users.PreferredLanguage = **tr**` ve `/auth/me` → `"language":"tr"`. **Negatif kontrol en az onun kadar değerli:** F4 öncesi kayıtlı kullanıcı (`turkerurganci`) aynı gün defalarca giriş yaptığı hâlde **`en` kaldı** — dil yalnız kayıt anında yazılıyor, girişte mevcut tercihi ezmiyor. Böylece zincirin tamamı uçtan uca ölçülmüş oldu: `/tr` CTA → `returnUrl=/tr/dashboard` → `skinora_oid_rt` cookie → Steam OpenID → callback → `FromPathPrefix` → provisioning → `tr`. Probe'un koşulabilmesi için `auth.min_steam_account_age_days` geçici olarak `0` yapıldı (hesap 0 günlüktü) ve ölçümden sonra AD9 ile **30'a geri alındı**, DB'den teyit edildi. **F4'ün orijinal açıklaması:** — sabit `PreferredLanguage = "en"` kalktı; dil, A1'de **zaten saklanan** return URL'inden türetiliyor (`/tr/dashboard` → `tr`). Yeni cookie veya OpenID state alanı **eklenmedi** ve state/CSRF doğrulaması hiç değişmedi. Dil **yalnız kayıt anında** yazılıyor: mevcut kullanıcının Ayarlar'dan yaptığı bilinçli seçim her oturum açışta sessizce ezilmesin diye, ayrı bir testle sabitlendi. Desteklenen diller listesi `LanguageService` içinde private duruyordu ve Auth modülü erişemiyordu; kopyalamak ikinci bir kaynak yaratacağı için `Shared`a taşındı (`SupportedLanguages`). **Yan bulgu:** `AuthSteamEndpointTests` fixture'ı sınıf düzeyi `auth` kovasının (10/60sn, IP bazlı) tam dibindeydi — yani sınıf **yeni test kabul edemez** hâldeydi ve bunu kimse bilmiyordu; fixture'da rate limit kapatıldı. **Bu satır F4 merge edilirken çevrilmemişti,** ertesi gün F6 turunda ([#273](https://github.com/turkerurganci/Skinora/pull/273)) yakalandı — dosyanın kendi başlığındaki kusur ailesi. **Eski açıklama:** **Her yeni kullanıcının bildirim dili İngilizce'ye sabitleniyor.** `UserProvisioningService.cs:32` kullanıcıyı yaratırken `PreferredLanguage = "en"` **sabit** yazıyor; metot `steamId64` ve Steam profilini alıyor, **arayüz dili hiç aktarılmıyor**. **Nasıl görüldü:** mobil turda `/tr/settings` ekranı Türkçe, header `TR`, ama "Dil Tercihi" kutusunda **"English (EN)"**; `/auth/me` teyit etti (`"language":"en"`). **Neden kozmetik değil:** bu alan **giden her mesajın dilini** belirliyor — `NotificationDispatcher.cs:174` (platform içi + e-posta + Telegram + Discord), `DisputeService.cs:453` (itiraz yazışmaları), `MisdeliveryDisputeEscalator.cs:293` (yanlış teslimat tırmandırması). Türkçe arayüzde kayıt olan satıcı **teslimat süresi uyarısını İngilizce** alıyor ve P2P'de kusur kaçırılan süreye göre atanıyor. Bozukluk sessiz: kullanıcı Ayarlar'daki kutuyu açıp fark etmedikçe görünmüyor. Düzeltme: plan §F4 — giriş başlatılırken arayüz dili OpenID state'i üzerinden taşınır; **state/CSRF doğrulaması bozulmamalı**, mevcut kullanıcıların tercihi değişmemeli | ölçüm 2026-08-23 UI turu; `UserProvisioningService.cs:32`, `NotificationDispatcher.cs:174` |
| ✅ | UITour-AdminUsersPageIsStub | **ÇÖZÜLDÜ → F5 ([#274](https://github.com/turkerurganci/Skinora/pull/274))** — `/admin/users` artık gerçek bir kullanıcı dizini: AD15 araması + rol filtresi + sayfalama, URL'e senkron (WP13 sözleşmesi, S21 ile birebir). **Asıl düzelen şey liste değil erişim** — S20 detay sayfası T39'dan beri çalışıyordu ama oraya ancak SteamID ezbere bilinerek gidiliyordu; bu yüzden testlerin yük taşıyan iddiası `href`, çünkü adı çizip bağlantıyı unutan bir satır ekran görüntüsünde kusursuz görünür. **Kapsam gizlenmedi, yazıldı:** `AdminUserService.ListAsync` arama terimi **yokken** yalnız aktif admin rolü atanmış kullanıcıları, **varken** devre dışı olmayan tüm kullanıcıları döndürüyor; bunu bilmeyen bir admin varsayılan listeyi *"platformda dört kullanıcı var"* diye okurdu — filtre çubuğunun altındaki ipucu hangi kipte olunduğunu söylüyor, boş sonucun gerekçesi de kipe göre ayrılıyor. **Rol atama bilerek taşınmadı:** S19'da (`/admin/roles` → `UserRoleAssignment`) zaten AD15 listesi + satır içi AD17 dropdown'ı var; ikinci bir editör tek iş akışını iki ekrana çatallardı — rol burada salt-okunur rozet, başlıkta S19'a bağlantı var. **`UserCard` (C04) planda adı geçtiği hâlde elendi:** `reputationScore`/`completedTransactions`/`accountAgeText` istiyor, AD15 üçünü de döndürmüyor — kullanmak sıfır uydurmak olurdu; yerine `ResponsiveTable`. **14 yeni test, iki mutasyonla doğrulandı** (kapsam ipucu sabitlenince 1, detay bağlantısı `steamId` yerine iç `id` kullanınca 2 test kırıldı). Ölçüm: vitest 164 → **178/178** · eslint 0 · i18n 4×**1325** parite OK (yeni advisory uyarı yok) · `next build` 0 hata. **CANLI DOĞRULAMA ✓ (2026-08-24, merge sonrası).** Docker ayağa kaldırılıp frontend image'ı `main` (`64e95d4`) üzerinden yeniden kuruldu; e2e harness'ının JWT-enjeksiyon yolu (`e2e/src/jwt.ts` deseni) ile süper admin oturumu açılıp Playwright'la ölçüldü. **Önce/sonra:** eski (2026-08-23) image'da `/en/admin/users` görünür metin **111** karakter, tablo satırı **0**, bağlantı **0**, metin *"… Audit Log Admin Users"* — turun *"metin uzunluğu 120"* ölçümü birebir yeniden üretildi. Yeni image'da: browse kipi metin **412**, satır **1**, satır bağlantısı `/en/admin/users/76561199053273410`; `?search=7656` kipinde metin **408**, satır **2**, ikinci satır rolsüz kullanıcı için **"No role"**. **AC2 tıklanarak ölçüldü:** satır bağlantısı S20 detayına götürdü (varılan URL `/en/admin/users/76561199053273410`, detay metni 728 karakter, kullanıcı adı görünür). **Kapsam ipucu ayırt edici ölçümle doğrulandı:** DB'ye geçici rolsüz bir sonda kullanıcısı eklendi → AD15 arama **yokken** `totalCount 1` (yalnız rol sahibi), `search=7656` ile `totalCount 2`; UI de tam olarak bunu gösterdi ve doğru ipucunu çizdi. Sonda kullanıcısı ölçümden sonra silindi. **Turdan gelen iki tuzağa da düşüldü ve ikisi de zaten `DEPLOY_RUNBOOK`'ta kayıtlıydı:** çıplak `docker compose up` dev override'ını katmanlayıp host kaynağını production image'ın üzerine bind-mount ediyor (frontend `node server.js`'i bulamayıp crash-loop'a giriyor — runbook §G) ve container yeniden yaratılınca nginx bayat upstream IP'sini tutuyor (runbook §G, `resolver` direktifi yok). **Eski açıklama:** **`/admin/users` hiç yapılmamış, ama admin menüsünde duruyor.** Dosyanın tamamı: `export default function AdminUsersPage() { return <div>Admin Users</div>; }` (3 satır). `AdminSidebar.tsx:28` MENU'sünde `users` maddesi var → admin "Kullanıcılar"a tıklıyor ve boş bir sayfada **İngilizce** `Admin Users` yazısı görüyor; listeleme/arama yok. Detay sayfası (`/admin/users/[steamId]`, 33 satır) **çalışıyor** ama oraya ancak SteamID ezbere bilinerek gidiliyor. **Ölçüldü: backend hazır, iş tamamen frontend** — `AdminController.cs:158` **AD15** `GET /admin/users`, imzası eksiksiz (`search`, `roleId`, `page`, `pageSize` → `PagedResult<AdminUserListItemDto>`), yetkilendirmesi bağlı (`PolicyManageRoles`, `admin-read`). Yani uç planlanmış, yazılmış ve test edilmiş; atlanan tek şey UI — bu yüzden "menüden kaldıralım" seçeneği **elendi**, kaldırmak çalışan bir backend yeteneğini gömmek olurdu. Gerekli ortak bileşenler galeride hazır (`FilterBar` C17 · `Pagination` C16 · `UserCard` C04 · `EmptyState` C13 · `Skeleton` C14). Düzeltme: plan §F5 | ölçüm 2026-08-23 UI turu; `admin/users/page.tsx`, `AdminSidebar.tsx:28`, `AdminController.cs:158` |
| ✅ | UITour-TransactionsListPageIsStub | **ÇÖZÜLDÜ → F6 ([#273](https://github.com/turkerurganci/Skinora/pull/273))** — `/transactions` artık `/{locale}/dashboard`'a **307 ile yönleniyor** (`next-intl` `redirect`'i, locale korunuyor). **İki seçenek ölçülerek elendi:** (a) *listeyi buraya da çiz* — işlem listesi zaten dashboard'da yaşıyor (`TransactionList` + `TransactionTabs` + WP13'ün URL'e senkron tab/page durumu) ve ikinci bir kopya o durumu iki sayfaya çatallardı, yani bu projenin kayıtlı **"iki kaynak"** kusur ailesini yeniden üretirdi; (b) *dosyayı sil* — iki canlı çocuk rotası (`/transactions/new`, `/transactions/{id}`) olan bir üst yolun 404 vermesi demekti ve o 404 **çevrilmemiş** global `not-found.tsx`'e düşüyor. **Ölçüm:** dört locale'de de 307 → `/{locale}/dashboard`; `/transactions/new` 200 (bozulmadı). **Eski açıklama:** **`/transactions` hiç yapılmamış (yetim rota).** Dosyanın tamamı: `export default function TransactionsPage() { return <div>Transactions</div>; }`. `UITour-AdminUsersPageIsStub`'dan **hafif**, çünkü ana menüde bu rotaya link yok (`Header.tsx` yalnız dashboard/notifications/profile/settings veriyor) ve gerçek işlem listesi dashboard'da yaşıyor (`TransactionList` + `TransactionTabs` çalışıyor). Yine de rota canlı ve URL'i yazan biri çıplak bir kelimeyle karşılaşıyor. **Bu satırın ve kardeşinin ortak kaydı:** iki dosyanın da git geçmişinde **tek commit** var — `c01e790` *"T13: Next.js Frontend iskeleti"*; T14→T140 arası 100+ görev, F6 (MVP kapanışı) ve F7 gate check'leri, 10 E2E süiti hiçbiri dokunmadı. **E2E'nin görememesi yapısal:** UI süiti işlem **detay**ına id ile doğrudan gidiyor, listeye hiç uğramıyor. Karar kalemi: dashboard'daki `TransactionList`'i bu rotaya bağla **veya** rotayı kaldır. Düzeltme: plan §F6 | ölçüm 2026-08-23 UI turu; `(main)/transactions/page.tsx`, `c01e790` |
| ✅ | UITour-NoFavicon | **ÇÖZÜLDÜ → F6 ([#273](https://github.com/turkerurganci/Skinora/pull/273))** — Next.js App Router kuralıyla üç dosya: `src/app/favicon.ico` (16+32+48, PNG gömülü ICO) · `icon.png` (512) · `apple-icon.png` (180); üçünün `<link>`'i de `<head>`'e enjekte oluyor. Marka varlığı olmadığı için tek gerçek marka sinyali kullanıldı: Header'daki **"Skinora"** kelime işareti ve arayüzün tek vurgu rengi **blue-600 (#2563eb)** → mavi yuvarlatılmış karede beyaz **"S"**. **Ölçüm:** `/favicon.ico` 404 → **200** (`image/x-icon`), `/icon.png` ve `/apple-icon.png` **200**. **`/apple-touch-icon.png` bilerek 404 bırakıldı:** Next apple ikonunu `/apple-icon.png`'de servis edip `<link rel="apple-touch-icon">` ile bildiriyor; çıplak yolu doldurmak aynı görselin `public/`'te ikinci bir kopyası olurdu. Link etiketini okumayan istemciler için o yol boş kalır. **Eski açıklama:** **Favicon yok** — `/favicon.ico` ve `/apple-touch-icon.png` → **404**. Her sayfa yüklemesinde tarayıcı console'una bir 404 düşüyor (turun ilk ekranındaki console hatası buydu) ve sekmede jenerik ikon görünüyor. Ucuz kapatma: `app/icon.png` + `apple-icon.png` (Next.js App Router kuralı). Para taşıyan bir üründe lansman öncesi cila kalemi. Düzeltme: plan §F6 | ölçüm 2026-08-23 UI turu |
| ✅ | UITour-NextStarterAssetsShipped | **ÇÖZÜLDÜ → F6 ([#273](https://github.com/turkerurganci/Skinora/pull/273))** — beş şablon SVG'si (`next` · `vercel` · `file` · `globe` · `window`) silindi; hiçbiri koddan referans edilmiyordu (tarandı) ve beşi de 200'den **404**'e döndü. **`public/` dizini boş ama duruyor:** `frontend/Dockerfile:24` runner katmanına `COPY --from=builder /app/public ./public` yapıyor ve dizin yoksa **image build'i kırılır**; git boş dizin takip edemediği için sebebini anlatan bir `.gitkeep` bırakıldı. Uygulamanın ikonları `public/`'te değil, `src/app/` altında (App Router kuralı). **Eski açıklama:** **Next.js starter artıkları production image'ında.** `frontend/public/` içeriğinin tamamı şablondan geliyor: `next.svg` · `vercel.svg` · `file.svg` · `globe.svg` · `window.svg`; projeye ait tek görsel yok. `vercel.svg` ve `next.svg`'nin ürünle hiçbir ilgisi yok ve image'a giriyorlar. Düzeltme: plan §F6 | ölçüm 2026-08-23 UI turu; `frontend/public/` |
| ✅ | UITour-StateScreensReachableAnonymously | **ÇÖZÜLDÜ → WP2a (owner kararı 2026-08-24): anonim erişim KALDI, indekslenme kapandı.** Bulgunun ürün yarısı bilinçli bir karardı ve korundu — bilgilendirme ekranlarına ihtiyacı olan kişi tanımı gereği giriş yapamıyor. Kayıtta **olmayan** sonuç ölçümle çıktı: `frontend/src` genelinde `noindex`/`robots` **hiç geçmiyordu**, yani üç ekran arama motorlarına tamamen açıktı — "Skinora — hesabınız askıya alındı" bir Google sonucu olabilirdi. `auth/layout.tsx`'e `robots: { index: false, follow: false }` eklendi. **Yerleşim bilinçli** (GUARDRAILS §7): sayfalar `"use client"` olduğu için `metadata` export edemiyorlar, ve `/auth/*` altındaki hiçbir yüzey (login · callback · age-gate · mobile-authenticator) indekslenmekten fayda görmüyor — üç ayrı layout dosyası yazmak hem tekrar olurdu hem login'i açıkta bırakırdı. **Derlenmesi değil, ULAŞTIĞI ölçüldü:** build alınıp sunucu ayağa kaldırıldı, üç ekran da `<meta name="robots" content="noindex, nofollow"/>` döndürdü ve negatif kontrol (`/en/privacy`) robots meta'sı **döndürmedi** — yani tüm site yanlışlıkla noindex edilmedi | ölçüm 2026-08-23 UI turu → WP2a |

---

## Ek — Zaten çözülmüş / non-actionable (yalnız izlenebilirlik)

Aşağıdakiler tarama sırasında "erteleme" olarak görünebilir ama **aktif borç değil**:

| ID | Durum |
|---|---|
| AD16 user-detail boşlukları (stats/wallet history/reputation/flag-dispute-counterparty) | ✓ T105 + T105b'de çözüldü |
| SignalR push'lar (tx-detail/notifications/dispute/profile/header) | ✓ T96'da çözüldü |
| Hesap suspend + flag-content projeksiyon (K2/K9/K10) | ✓ T105a + T100a'da çözüldü |
| F0/F1/F2 bootstrap forward-defer'leri (rate-limit/auth/Loki/migration/stub'lar) | ✓ Hedef successor task'larında çözüldü |
| T105b öncesi wallet adres backfill | ✗ Non-actionable — tarihsel veri hiç tutulmadı, imkânsız |
| GitHub branch protection | ✗ Paid feature (403); owner kararı bekliyor — implementasyon borcu değil |
| Çeşitli by-design MVP stub'ları (loose tx-hash, rate-limiter, price-cache retention) | ✗ By-design |
| 08-Telegram webhook 07'de eksik | ✓ Aslında `07_API_DESIGN.md:767` §5.11b'de **var** (tarama yanlış alarmı; doğrulandı) |
| T37 kanal-handler PII log maskeleme | ✓ `TargetExternalIdMasker.Mask()` ile T78/T79/T80'de yerine getirilmiş |

---

> **Doğrulama:** İki-turlu çıkarımın tüm net-yeni adayları refute-default doğrulayıcıdan geçirildi (kod/rapor kanıtıyla). Doğrulayıcı verdict'i: "kalan üst-düzey erteleme tespit edilmedi; iki geçiş ertelenen iş havuzunu yüksek güvenle kapsıyor." Kritik kod iddiaları (escrow→bot bağı yok, `NullMarketPriceProvider`/`StubPayoutVerifier`, sidecar round-robin) bağımsız doğrulandı.
