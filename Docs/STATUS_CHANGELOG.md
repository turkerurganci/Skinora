# Skinora — Implementation Status · Güncelleme Geçmişi (Arşiv)

> `IMPLEMENTATION_STATUS.md`'nin "Son güncelleme" başlığından taşınan tarihsel changelog (en yeni en üstte). Güncel durum + görev tablosu için → [`IMPLEMENTATION_STATUS.md`](IMPLEMENTATION_STATUS.md).

## Güncelleme geçmişi

**Önceki güncelleme:** 2026-06-06 (T101 ✓ **Tamamlandı — bağımsız validator PASS** (4/4 kabul ✓; doğrulama kontrol listesi ~Kısmi; 9 bloke-edici-olmayan bulgu K-notes — 4×S3 [kolon-sıralama UI yok ama en-yeni-üstte backend default'u ile var / iptal-modal iade bilgisi / hold süresi / taraf skoru-kontratta yok] + 5×advisory; K3/K4/notification = T63 AD6/AD7 kontrat daralması; task CI [`27071230219`](https://github.com/turkerurganci/Skinora/actions/runs/27071230219) 11/11; proje sahibi "merge + K-notes" onayı). **Sırada T102** (Parametre S17). Yapım özeti — Admin İşlem listesi + detay (S15, S16). **Frontend + minimal AD6 backend eklentisi** (proje sahibi onayı 2026-06-06, Seçenek A — T100 precedent'i): 04 §8.4 gruplu `Durum` filtresi için AD6'ya `statusGroup` query param (`AdminTransactionStatusGroup` enum ACTIVE/COMPLETED/CANCELLED/FLAGGED; **ACTIVE = terminal-olmayan**, `AdminDashboardService._terminalStates` ile birebir → S12 `?tab=active` deep-link `activeTransactions` sayacıyla tutarlı, FLAGGED dahil) + `BuildFilteredQuery` eşlemesi + controller `[FromQuery]` + 07 §9.6 doc; **migration yok, yeni domain enum yok** (query-param enum). 3 yeni `AdminT63EndpointTests` (ACTIVE/CANCELLED/FLAGGED) → **24/24 PASS** (SQLite). Frontend: `lib/api/admin.ts` işlem katmanı (list/detail tipleri + statusGroup query + cancel/emergency-hold/release-hold) + 3 React Query hook + `TransactionListTable` (04 §8.4 — 8 kolon, ID→S16, taraflar→S20 forward-link, ResponsiveTable mobil) + `TransactionDetailView` (04 §8.5 — 9 AD7 bölümü: İşlem Bilgileri/Durum Geçmişi timeline/Taraflar/Ödeme/Satıcıya Ödeme/İade/Bildirim/Dispute/Flag + state-aware aksiyon rayı: FLAGGED→İşleme Devam Et/İptal Et AD4/AD5; aktif→İşlemi İptal Et AD19 + Emergency Hold AD19b; EMERGENCY_HOLD→Hold Kaldır Devam/İptal AD19c + Auto-Hold sanctions etiketi; ITEM_DELIVERED→Exceptional Resolution deferred; terminal→salt okunur; `FlagActionModal` reuse) + 2 sayfa (`admin/transactions` stub→tam URL-senkron filtre + S12 dashboard `?tab=active`/`?range=` deep-link uyumu, `[id]` detay) + `adminTransactions` 4-locale namespace (100 leaf × 4, **parity 859×4**). Lokal: backend Release 0W/0E + `dotnet format --verify-no-changes` exit=0 + AdminT63EndpointTests 24/24 (SQLite); frontend `tsc` 0 + `eslint` 0/0 + `prettier --check` clean + `next build` (`/admin/transactions` + `/admin/transactions/[id]` ƒ). **4/4 kabul self-check ✓; test beklentisi: Yok** (plan; frontend test runner yok). K1 Exceptional Resolution (ITEM_DELIVERED cancel + release→CANCEL@delivered) backend endpoint yok → disabled "Yakında" forward / K2 S20 taraf linkleri + `/admin/users/{steamId}` route T105 forward (404) / K3 S16 taraf skoru AD7 party DTO'da yok / K4 list cancel tarihi AD6 DTO'da yok (S16'da) / K5 `?range=daily|weekly`→COMPLETED, tam pencere (CompletedAt) deferred / K6 dispute/flag/notification geçmişi ham enum / K7 Auto-Hold sanctions heuristic / K9 frontend test runner yok. PR [#152](https://github.com/turkerurganci/Skinora/pull/152). **T101 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** **(Tam güncelleme geçmişi → [`STATUS_CHANGELOG.md`](STATUS_CHANGELOG.md).)**

**Önceki güncelleme:** 2026-06-06 (T105a ✓ Tamamlandı — Hesap askıya alma + kısıtlı oturum (S03d), PR [#149](https://github.com/turkerurganci/Skinora/pull/149) merged. Bu oturumda **T100 (#148) + T100a (#150) + T105a (#149)** main'e squash merge edildi; üçü de bağımsız validator PASS (T105a reconciled CI [`27067117970`](https://github.com/turkerurganci/Skinora/actions/runs/27067117970) 11/11). **Sırada T101** (Admin İşlem listesi + detay, S15/S16). 

**Önceki güncelleme:** 2026-06-05 (T100 ⏳ yapım bitti — Admin Flag Kuyruğu + Detay (S13, S14). Frontend: S13 kuyruk (URL-senkron `FilterBar` kategori/tür/durum/tarih + `ResponsiveTable` T98 + `Pagination` + `pendingCount` başlık rozeti) `app/[locale]/admin/flags/page.tsx` stub→tam; S14 detay yeni route `[id]/page.tsx` (işlem-flag varyantı fiyat sapması/yüksek hacim/anormal davranış `flagDetail` type-switch + hesap-flag varyantı çoklu hesap sinyali/ilişkili hesaplar; `UserCard` detailed + `StatusBadge` + `formatStablecoin/formatDateTime`). Aksiyon alanı: page-level Admin Notu (opsiyonel) + `FlagActionModal` (approve/reject "emin misiniz?" + Hold zorunlu-sebep ≥10). İşlem flag: İşleme Devam Et (AD4 approve) / İptal Et (AD5 reject); hesap flag: Flag Kaldır (AD4) + **Aktif İşlemleri Hold'a Al (AD19d)**; **Askıya Al deferred** (disabled "Yakında" → ayrı suspend task/PR, S20=T105). Yeni `lib/api/admin.ts` flag katmanı (`listAdminFlags`/`getAdminFlag`/`approve`/`reject`/`holdUserTransactions`) + 3 React Query hook (`useAdminFlagList`/`Detail`/`Mutations`) + 4 component (`FlagQueueTable`/`FlagDetailView`/`FlagActionModal`/`FlagReviewStatusBadge`) + `adminFlags` 4-locale namespace (82 leaf × 4, parity 747×4). **Backend (T100 kendi gereksinimi):** AD2 `scope` query param (07 §9.2 — `FraudFlagListQuery.Scope` + ListAsync where + controller `[FromQuery] FraudFlagScope?`; server-side kategori filtresi, pagination/`totalCount` tutarlı); AD3 `userId` (07 §9.3 — `FraudFlagDetailDto.UserId` flag'lenmiş kullanıcı Guid'i, Hold hedefi); **AD19d** `POST /admin/transactions/hold-by-user/:userId` (07 §9.22a — `AdminTransactionService.HoldAllUserTransactionsAsync` AD19b per-tx freeze+`ApplyEmergencyHold`+`EmergencyHoldAppliedEvent`+`EMERGENCY_HOLD_APPLIED` audit sırasını kullanıcının tüm aktif işlemlerinde reuse, `EMERGENCY_HOLD` yetkisi, `!IsOnHold` idempotent, tek SaveChanges; **yeni enum/event/migration yok**). Lokal: frontend tsc 0 + eslint 0/0 + prettier clean + `next build` 26 route (`/admin/flags` + `/admin/flags/[id]`); backend Release 0W/0E + dotnet format Δ=0 (11 dosya) + `AdminFlagsEndpointTests` 10/10 PASS (SQLite, scope-binding testi dahil); SQL-Server integration (FraudFlagAdminQueryServiceTests scope filter + UserId, AdminTransactionServiceTests hold-all 3 test) → CI shared mssql (lokal Windows Docker yok, T11.3). 5/5 kabul + 1/1 kontrol self-check ✓. K1 Askıya Al→T105 / K2 hesap-flag liste kolonları (Sinyal/İlişkili/Aktif) AD2'de yok S14'te / K3 SANCTIONS_MATCH flagDetail null / K4 pendingCount global / K5 frontend test runner yok / K6 dateTo gün-sonu. Proje sahibi scope onayı 2026-06-05 (Seçenek C: T100 = frontend+scope+Hold; suspend ayrı task). PR pending. **T100 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-24 (T99 ⏳ yapım bitti — Admin Dashboard (S12). T63 PR #100 AD1 backend kontratı (`GET /admin/dashboard` — `summaryCards{activeTransactions,pendingFlags,dailyCompleted,weeklyCompleted}` + `steamAccounts[]` AdminSteamAccountDto + `recentFlags[]` server limit 5) frontend tarafa bağlan T63 PR #100 AD1 backend kontratı (`GET /admin/dashboard` — `summaryCards{activeTransactions,pendingFlags,dailyCompleted,weeklyCompleted}` + `steamAccounts[]` AdminSteamAccountDto + `recentFlags[]` server limit 5) frontend tarafa bağlandı. Yeni `lib/api/admin.ts` typed envelope (`AdminDashboardResponse` + 3 sub-record + 3 string-enum union: `AdminSteamAccountStatus` ACTIVE/RESTRICTED/BANNED/OFFLINE 06 §2.15, `AdminFlagType` 5 değer 04 §8.2, `AdminFlagReviewStatus` PENDING/APPROVED/REJECTED) + `useAdminDashboard()` React Query hook 30s staleTime. Page rewrite: `app/[locale]/admin/dashboard/page.tsx` placeholder div → tam sayfa (heading + SummaryCards row + 2-col grid SteamAccountsStatus + RecentFlagsTable). 3 yeni component `components/admin/`: (1) `SummaryCards` 4 kart `<Link>` (Aktif→`/admin/transactions?tab=active`, Bekleyen Flag'ler→`/admin/flags?status=PENDING` urgent variant kırmızı badge 04 §8.1 "acil" şartı, Günlük→`?range=daily`, Haftalık→`?range=weekly`); (2) `SteamAccountsStatus` sm:2col/lg:3col grid kart, STATUS_ICON map (✓/⚠/✕/○), statusTone helper (ACTIVE=emerald, OFFLINE=gray, RESTRICTED+BANNED=red border) — 04 §8.1 "Kısıtlı/banned bot uyarısı" iki kanaldan: per-card kırmızı border + header `role="alert"` banner (client-side `degraded = filter(s ∈ {RESTRICTED,BANNED})`, AD1 response'unda warningMessage yok AD10'a özel); (3) `RecentFlagsTable` T98 ResponsiveTable<T> ilk tüketici, 4 kolon ID/Tür/Tarih/Durum, mobil <md card list 04 §9.4 dt/dd paterni internal, "Tümünü Gör"→`/admin/flags`. 4-locale `adminDashboard.*` namespace ~32 anahtar tr/en/es/zh (1009 → 1055 = +46 satır 4 dilde, leaf parity 1055×4 korundu); ICU `{count}` warning + `{name}/{status}` cardAriaLabel; flag tür/status verbatim 04 §8.1+§8.2. Build: `next build` 26 route PASS, `tsc --noEmit` 0, `eslint <T99 files>` 0, `prettier --check <T99 files>` PASS (autofix sonrası 7 dosya). Migration yok, yeni dep yok. **5/5 kabul kriteri ✓** (özet kartları 4 lokalize + son 5 flag tablo + Steam durum kart grid + kısıtlı/banned uyarı iki kanal + GET /admin/dashboard). **1/1 doğrulama listesi ✓** (04 §8.1 tüm bileşenler). K1 link target query param T100/T101 honor forward / K2 `/admin/steam-accounts` route T103 forward (link şu an 404) / K3 `/admin/flags/{id}` detay route T100 forward / K4 frontend test runner yok F5 plan-onaylı / K5 admin permission frontend guard T-future (backend AuthPolicies.AdminAccess enforce) / K6 AD1 warningMessage AD10'a özel client-türetimi / K7 152 pre-existing prettier drift T80 K7 havuzu. **T99 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-24 (T98 ✓ Validator PASS — Responsive tasarım. Tailwind v4 default screens (md=768, lg=1024) 04 §9.1 ile birebir, config Δ=0. AdminShell client drawer (ESC keydown + overlay click + nav-onClick 3-yol kapanış), AdminSidebar `hidden md:block w-56` desktop + `fixed inset-y-0 z-50 md:hidden` mobile overlay (`role="dialog" aria-modal`), AdminHeader `md:hidden` hamburger inline SVG. Yeni reusable `ResponsiveTable<T>` (`hidden md:block` semantic `<table>` + `flex md:hidden` `<ul role="list">` + `<dl>` dt/dd 04 §9.4 label/value paterni; T100/T101 tüketici). StatsCards mobile fix `grid-cols-1 sm:grid-cols-3 lg:grid-cols-1` (~360px viewport 3-up crush önlendi). Dashboard `grid-cols-1 lg:grid-cols-[1fr_18rem]` (mobile/tablet stats üst-slot, desktop right rail) — 3 layout 04 §9.2 birebir. TxCreate `mx-auto max-w-3xl px-4`, TxDetail `grid-cols-1 md:grid-cols-[2fr_1fr]`, Timeline `flex flex-col md:flex-row` — bağımsız doğrulandı. 4-locale `adminNav.openMenu/closeMenu` 632×4 → 634×4 parity. Lokal `next build` 3.5s PASS + `tsc --noEmit` 0 + `eslint` 0 + `prettier --check` 12/12 clean. Task branch CI HEAD `d2a01a2` [`26365241599`](https://github.com/turkerurganci/Skinora/actions/runs/26365241599) **9/9 SUCCESS**. 0 S-bulgu, 1 S1 minor same-PR fix (AdminShell.tsx `<AdminSidebar>` JSX 4 satır → tek satır prettier-clean; yapım raporu "12/12 clean" iddiası validator chat'inde düzeltildi). K1 pathname effect alternatifi nav-onClick bubble-up / K2 ResponsiveTable T100/T101 tüketici / K3 152 pre-existing prettier drift T80 K7 havuzu / K4 lucide-react reddedildi / K5 frontend test framework yok. **T98 satırı `⏳ Devam ediyor` → `✓ Tamamlandı`.** 

**Önceki güncelleme:** 2026-05-24 (T97 ✓ Validator PASS — i18n (4 dil desteği). T84 next-intl iskeleti + T84–T95 632×4 locale parity sonrası runtime format katmanı 04 §10'a uydurulduk. Yeni `lib/utils/format.ts` merkezi helper'lar: `formatDate/Time/DateTime/DateLong` (Intl.DateTimeFormat — `dateStyle:"medium"` + `timeStyle:"short"` 04 §10.2 4 locale tablosu birebir), `formatNumber`/`formatPercent` (Intl.NumberFormat locale-aware — en/zh `1,234.56`, tr/es `1.234,56`), `formatStablecoin` (locale-invariant — `Intl.NumberFormat("en-US",{useGrouping:false})` veya string passthrough — 04 §10.3 stablecoin dot decimal). Yeni `src/i18n/untranslatable.ts` 10 sabit terim (USDT/USDC/TRC-20/Tron/Steam/Steam ID/Mobile Authenticator/Trade offer/CS2/Gas fee — 04 §10.4 birebir) + `isUntranslatable` case-insensitive checker. **15 dosya migrate:** 7 date call site (CancelInfoBlock/DisputeBlock/FlagHoldBanner/SellerPayoutSummary/TransactionInfoPanel/TransactionRow/MaintenanceGate), 9 stablecoin display, 4 non-stablecoin (StatsCards/TrustSignals/ReputationCard/Step2Details). `profile/helpers.ts` `formatPercent`/`formatScore` locale parametresi alır oldu. Text-length 1.5x audit (04 §10.1) PASS — StatusBadge `inline-flex` auto-expand + button'lar `px-4 py-2`, fixed `w-XX`+truncate kombi yok, ek CSS gerekmiyor. 6/6 kabul ✓ + 1/1 kontrol ✓. ESLint 0/0 + Next build ✓ + Prettier T97 dosyaları clean + 4-locale parity 632/632/632/632 korundu. **Validator PASS bağımsız 2026-05-24** (task branch CI [`26359346213`](https://github.com/turkerurganci/Skinora/actions/runs/26359346213) success + runtime Intl 4-locale ölçüm § 10.2 birebir + 0 S-bulgu; M1 advisory ES CLDR 4-haneli sayılarda binlik ayracı yok — Intl standardı, finding değil). [PR #145](https://github.com/turkerurganci/Skinora/pull/145). K1 i18n lint CI yok / K2 formatDateLong call site yok / K3 formatAmount deprecated alias / K4 AdminSidebar truncate yok / K5 LanguageSelector localStorage path-based / K6 149 pre-existing prettier drift T80 K7 havuzu. **T97 satırı `⬚ Bekliyor` → `✓ Tamamlandı`.** 

**Önceki güncelleme:** 2026-05-05 (T59 ⏳ yapım bitti — Emergency hold. T44 state machine + T50 freeze service + T42 audit logger + T37 notification dispatcher kompozisyonu ile admin işlem-yaşam-döngüsü orchestrator katmanı kuruldu (07 §9.20–§9.22, 02 §7, 03 §8.8). Yeni `Skinora.Transactions/Application/Admin/` paketi: `IAdminTransactionService`/`AdminTransactionService` 3 metot orchestrator (~480 satır, her metot 5-7 stage pipeline + tek `SaveChangesAsync` ile atomik commit 09 §13.3) — CancelAsync (AD19: state guard ITEM_DELIVERED 422 / IsOnHold 409 / terminal 409 + Fire(AdminCancel) + ItemRefund/PaymentRefund/TransactionCancelledEvent ADMIN + audit `TRANSACTION_CANCELLED_ADMIN`), ApplyEmergencyHoldAsync (AD19b: terminal 409 / AlreadyOnHold 409 + state machine.ApplyEmergencyHold + freeze service Hangfire iptal + EmergencyHoldAppliedEvent + audit `EMERGENCY_HOLD_APPLIED`, response `status:"EMERGENCY_HOLD"` overlay projection), ReleaseEmergencyHoldAsync (AD19c: RESUME → ResumeAsync+ReleaseEmergencyHold+EmergencyHoldReleasedEvent+audit; CANCEL → ReleaseEmergencyHold+TimeoutRemainingSeconds=null (CK_Transactions_FreezePassive)+Fire(AdminCancel)+refunds+TransactionCancelledEvent ADMIN+2 audit row; ITEM_DELIVERED CANCEL guard 422 `CANNOT_CANCEL_DELIVERED_HOLD`). Yeni `AdminTransactionsController` 3 endpoint (`/admin/transactions/{id}/cancel|emergency-hold|release-hold`) ayrı yetki policy'leri (`Permission:CANCEL_TRANSACTIONS` AD19, `Permission:EMERGENCY_HOLD` AD19b/c — 02 §7 not). Enum genişlemeleri: `AuditAction` (17→20: TRANSACTION_CANCELLED_ADMIN/EMERGENCY_HOLD_APPLIED/EMERGENCY_HOLD_RELEASED), `ItemRefundTrigger` (4→5: AdminCancel), `NotificationType` (20→22: EMERGENCY_HOLD_APPLIED/RELEASED), yeni `EmergencyHoldReleaseAction` (RESUME/CANCEL). 2 yeni outbox event (`EmergencyHoldAppliedEvent`/`EmergencyHoldReleasedEvent`) + 2 yeni notification consumer (RESUME branch only — CANCEL ileri; CANCEL fan-out `TransactionCancelledNotificationConsumer` ADMIN dalı extension üzerinden, her iki tarafa "İşlem yönetici tarafından iptal edildi"). 4 yeni resx anahtarı (Title+Body × 2 EN+TR). `AuditLogCategoryMap` 3 yeni AuditAction → ADMIN_ACTION. 06 §2.19 doc 3 satır eklendi (kod-doc senkron). DI 1 satır (`AdminTransactionService` Scoped). Migration yok (kolonlar T19/T44, CK_Transactions_Freeze*/Hold T30). **8/8 plan kabul kriteri ✓** (3 endpoint + 2 ayrı yetki + ITEM_DELIVERED RESUME-only + timeout durur + audit + bildirim). 2/2 doğrulama kontrol listesi ✓ (02 §7 + 07 §9.20–§9.22). Lokal Release 0W/0E + format `--verify-no-changes` exit=0 + 7 unit assembly **734/734 PASS** (Shared 180 + Auth 57 + Notifications 49 + Users 16 + Fraud 14 + Platform 85 + Transactions(unit) 333). Integration testler (17 yeni `AdminTransactionServiceTests`) lokal Windows Docker engine yok → CI shared services:mssql üzerinde doğrulanacak (T11.3 paterni). PR pending. **Mimari kararlar (Notlar):** (1) Tek service 3 metot tek sınıfta — T58 DisputeService paterni; (2) `CancelledByType.ADMIN` reuse — T51 `TransactionCancelledEvent` consumer'ında ADMIN dalı eklendi (yeni event/template gerekmedi); (3) CANCEL release dual-event — `EmergencyHoldReleasedEvent`(CANCEL) consumer skip + `TransactionCancelledEvent`(ADMIN) consumer fan-out (SignalR T61 forward-aware + duplicate-free); (4) Idempotent ApplyEmergencyHold + FreezeAsync sırası kritik — state machine önce damgalar, freeze service sonra Hangfire iptali (idempotent guard); (5) CANCEL release path'inde `TimeoutRemainingSeconds = null` manuel clear (CK_Transactions_FreezePassive zorunluluğu, T47 path tetiklenmediği için T50 ResumeAsync atlanır). Forward-deferred: SignalR RT1 push T61, sanctions auto-trigger T82, i18n locale T97 (ES/ZH). **T59 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-05 (chore PR #91 squash `fc9682c` — T50 status row drift fix. PR #81 squash sonrası `cb71f74` "validator ✓ PASS" commit'i sadece "Son güncelleme" header'ını yenilemiş, 125. satırdaki T50 row'u atlamış. Functionally complete (PR #81), sadece tablo görünüm drift'i. Ayrı chore PR ile düzeltildi (bundled-PR yasağı). T50 satırı `⏳ Devam ediyor / ⏳` → `✓ Tamamlandı / ✓ PASS bağımsız validator (...) / 0310a81 (PR #81)`. CI 25398051296 success — sadece Detect+Lint+CI Gate (path filter docs-only). 

**Önceki güncelleme:** 2026-05-03 (T53 ✓ PASS bağımsız validator + ✓ Tamamlandı PR #84 — Gas fee yönetimi. **Validator bulgu (S1 minor — same-PR fix):** `AuditAction.REFUND_BLOCKED` enum kodda 12→13 büyütülmüştü ama [Docs/06_DATA_MODEL.md §2.19](06_DATA_MODEL.md) AuditAction tablosu güncellenmemişti — INSTRUCTIONS.md §4 enum 1:1 doc tutarlılığı ihlali. Same-PR fix uygulandı: `| REFUND_BLOCKED | Admin | Min iade eşiğinin altında kalan iade bloklandı — admin alert (09 §14.4, T53) |` satırı 06 §2.19'a eklendi (group: Admin — 06 §2.19'da Sistem group'u tanımlı değil, AuditLogCategoryMap REFUND_BLOCKED→AdminAction map'iyle tutarlı). 4/4 kabul kriteri ✓; 1/1 doğrulama listesi ✓ (02 §4.7); lokal Release 0W/0E + GasFee 40/40 unit + Shared audit/enum 14/14 + task branch CI 25288650567 7/7 job ✓; main CI startup ardışık 3 ✓. Yapım: T52 `FinancialCalculator` saf statik formüllerinden sonra, live SystemSetting okuma + admin alert mekanizması production glue layer'ı kuruldu (02 §4.6, §4.7, 09 §14.4). Yeni `Skinora.Transactions/Application/GasFee/` paketi: `IGasFeeSettingsProvider` + `GasFeeSettingsProvider` (`gas_fee_protection_ratio` + `min_refund_threshold_ratio` AsNoTracking + IsConfigured filter, validator envelope mirror — out-of-range/malformed → `FinancialCalculator.Default*` fallback; `TransactionLimitsProvider`/`ReputationThresholdsProvider` paterni); `IRefundDecisionService` + `RefundDecisionService` (3 method: `ResolveBuyerRefundAsync` tam iptal, `ResolveOverpaymentRefundAsync` fazla ödeme, `ResolveSellerPayoutAsync` gas-fee-protection; `RefundDecision { Outcome (Refund/Block), NetRefund, Threshold, GasFee, TotalPaid, Reason }` — pure decision tier, audit/outbox/SaveChanges yapmaz); `IRefundBlockedAlertService` + `RefundBlockedAlertService` (side-effect tier — `IAuditLogger.LogAsync(REFUND_BLOCKED)` + `IOutboxService.PublishAsync(RefundBlockedAdminAlertEvent)` change-tracker stage, caller atomik SaveChanges 09 §13.3). Yeni `Skinora.Shared/Events/RefundBlockedAdminAlertEvent.cs` (8 alan + `RefundBlockedReason` enum: BelowMinimumThreshold/NegativeAmount; consumer T78–T80 forward-devir T49 paterni). Yeni `AuditAction.REFUND_BLOCKED` (12→13 enum string-stored migration yok); `AuditLogCategoryMap` REFUND_BLOCKED → ADMIN_ACTION (07 §9.19 — MANUAL_REFUND ile aynı admin kuyruğunda görünmesi UX gereği, FUND_MOVEMENT yüksek hacim arasında alert'i gizlerdi). DI: `TransactionsModule.cs` 3 yeni Scoped (provider + decision + alert). Migration yok, SystemSetting yok (T26 seed'inde mevcut). **4/4 plan kabul kriteri ✓** (1 satıcı gas fee komisyondan, 2 koruma eşiği aşımı satıcıdan kesilir, 3 iadeden gas düşülür, 4 minimum eşik altı block + admin alert). 1/1 doğrulama listesi ✓ (02 §4.7 gas fee kuralları eksiksiz). Lokal Release 0W/0E + format `--verify-no-changes` exit=0 + tüm 8 unit test assembly **1149/1149 PASS** (Users 16 + Auth 57 + Notifications 40 + Shared 178 + Fraud 5 + Platform 87 + Transactions 495 + API 271; Admin/Disputes/Payments/Steam integration-only). Yeni T53 testleri: 17 unit `RefundDecisionServiceTests` + 7 unit `RefundBlockedAlertServiceTests` + 6 integration `GasFeeSettingsProviderTests` (CI MSSQL T11.3 paralel) + 1 InlineData EnumTests REFUND_BLOCKED + 1 InlineData AuditLogCategoryMapTests + count assertion 12→13 ve ADMIN_ACTION 6→7. PR pending. **Mimari kararlar (Notlar):** (1) Pure decision/alert split — eligibility/preview path "iade kabul edilir mi?" sorusunu audit row üretmeden cevaplayabilir; (2) JSON serialize edilen audit `NewValue` decimal'leri InvariantCulture stringe — System.Text.Json default invariant ama defensive; (3) `RefundBlockedReason` enum (BelowMinimumThreshold/NegativeAmount) — net < 0 ile net < threshold ayrımı admin observability için anlamlı; (4) Caller wiring forward-devir T49 paterni — T57+ refund orchestrator + T73 blockchain sidecar consumer tüketici; (5) No caching layer — admin update'lerin anında etkili olması cache window içinde block-after-unblock'tan daha kritik (T43/T45 paralel). **T53 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-03 (T52 ✓ PASS bağımsız validator + ✓ Tamamlandı PR #83 — Komisyon ve finansal hesaplamalar. T51 ✓ PASS bağımsız validator — İptal akışı. 0 S-bulgu, 0 minor advisory; 9/9 plan kabul kriteri ✓ (8 tam + 1 ~ kısmi #5 "Ödeme alınmışsa → alıcıya iade tetikleme" — T51 user-cancel post-payment kapalı 02 §7 gereği, ödeme iadeli cancel yolları timeout T49 ✓ + admin T59 forward + dispute T58 forward, `paymentRefunded` her zaman false reserved slot 07 §7.7 zorunlu alan); 2/2 doğrulama kontrol listesi ✓ (02 §7 tüm iptal kuralları + 07 §7.7 sözleşmesi); validator lokal Release `0W/0E` + format `--verify-no-changes` exit=0 + 12 test assembly **1300/1300 PASS** (Users 16 + Payments 6 + Admin 20 + Auth 93 + Disputes 11 + Fraud 17 + Notifications 77 + Steam 21 + Shared 187 + Platform 136 + Transactions 445 + API 271); task branch CI run [`25277595776`](https://github.com/turkerurganci/Skinora/actions/runs/25277595776) (HEAD `55905c2` docs) 9/9 + Guard skipped ✓ + ardışık [`25277455577`](https://github.com/turkerurganci/Skinora/actions/runs/25277455577) (`fb301c8` ana commit) 9/9 ✓; main CI startup ardışık 3 ✓ (`25275894773`+`25275894766` T50 #81 + `25259885322` chore #80); doc uyumu tam (07 §7.7 request/response + tüm 4 hata kodu + auth/rate-limit; 02 §7 post-payment yasak + reason + cooldown + admin/hold T59 forward; 03 §2.5 step 9 + §3.3 step 8 Türkçe metin verbatim consumer'da; 09 §13.3 atomicity boundary `BeginTransactionAsync` two-save; 05 §4.2 role × state → trigger matrisi state machine permit ile 1:1); 0 yeni dış bağımlılık + 0 migration; mini güvenlik (secret yok, auth/authorization Authenticated + party guard + 4-katmanlı state guards, RowVersion EF Core optimistic concurrency aktif explicit guard T59 forward) temiz. Yapım raporu karşılaştırması tam uyumlu — 0 uyuşmazlık. Sırada **T52** (Komisyon ve finansal hesaplamalar; bağımlılık T19 ✓; doc 02 §5 + §4.6–§4.7 + 06 §8.3 + 09 §14). **T51 satırı `⏳ Devam ediyor` → `✓ Tamamlandı`.** 

**Önceki güncelleme:** 2026-05-03 (T51 ⏳ yapım bitti — İptal akışı. T44 state machine + T46 acceptance + T47–T50 timeout altyapısı sonrası kullanıcı kaynaklı iptal akışı bağlandı (07 §7.7, 02 §7, 03 §2.5/§3.3, 09 §13.3). **`ItemRefundToSellerRequestedEvent` generalize edildi** — `Trigger` alanının tipi `TimeoutPhase` → yeni `Skinora.Shared.Enums.ItemRefundTrigger` (`TimeoutPayment/TimeoutDelivery/SellerCancel/BuyerCancel`); T49 `TimeoutSideEffectPublisher` mapping (2 satır) + 3 T49 test dosyası assertion update. Yeni `Skinora.Shared/Events/TransactionCancelledEvent.cs` (notification fan-out trigger) + `Skinora.Transactions/Application/Lifecycle/`: `ITransactionCancellationService` + `TransactionCancellationService` ~200 satır 7-aşamalı pipeline (load → party guard → reason min 10 char trim → state guard post-payment 422 + role×state→trigger map → state machine `Fire(SellerCancel|SellerDecline|BuyerCancel, CancellationContext)` → side effects (Hangfire job cancel + `ItemRefundToSellerRequestedEvent` if `EscrowBotAssetId` set + `TransactionCancelledEvent` always) → atomic commit + denormalized projection BeginTransactionAsync ile state save → ReputationAggregator/CancelCooldownEvaluator → user updates save → tek transaction commit). Yeni `Skinora.Notifications/Application/EventHandlers/TransactionCancelledNotificationConsumer` role-aware counter-party fan-out (SELLER→buyer "İşlem satıcı tarafından iptal edildi" 03 §2.5/9; BUYER→seller "İşlem alıcı tarafından iptal edildi" 03 §3.3/8; null buyer no-op + processed-event marker — pre-accept seller cancel). Endpoint `POST /transactions/{id:guid}/cancel` `Authorize(Authenticated)` + `RateLimit("user-write")`; outcome→HTTP eşleme (200/404/403/422/409/400). DI 1 satır `ITransactionCancellationService` Scoped. 2 yeni hata kodu (`PAYMENT_ALREADY_SENT`, `CANCEL_REASON_REQUIRED`). DTO + outcome + 6-value status enum. Migration yok, SystemSetting yok (cancel cooldown thresholds T43 seed'inde), yeni paket yok. **Role × State → Trigger haritası:** Seller × {CREATED, ACCEPTED, ITEM_ESCROWED} → SellerCancel; Seller × TRADE_OFFER_SENT_TO_SELLER → SellerDecline (state machine SellerCancel'i bu state'te permit etmiyor; aynı CANCELLED_SELLER sonu); Buyer × {CREATED, ACCEPTED, TRADE_OFFER_SENT_TO_SELLER, ITEM_ESCROWED} → BuyerCancel; diğer → null → INVALID_STATE_TRANSITION 409. **Post-payment guard:** PAYMENT_RECEIVED + TRADE_OFFER_SENT_TO_BUYER + ITEM_DELIVERED → 422 PAYMENT_ALREADY_SENT (state machine `BuyerDecline` permit etse de T51 endpoint'i kapalı; T58 dispute / T59 admin orchestrator forward-devir). **9/9 plan kabul kriteri ✓** (8 tam + 1 ~ kısmi #5 "Ödeme alınmışsa → alıcıya iade tetikleme" — T51 user-cancel post-payment kapalı, ödeme iadeli cancel yolları timeout T49 ✓ + admin T59 forward + dispute T58 forward; `paymentRefunded` her zaman false reserved slot). **2/2 doğrulama listesi ✓** (02 §7 tüm iptal kuralları + 07 §7.7 sözleşmesi). Lokal Release 0W/0E + format `--verify-no-changes` exit=0 + 12 test assembly **1300/1300 PASS** isolated (T50: 1264 → T51: 1300, +36 net = 21 yeni TransactionCancellationService integration + 6 yeni endpoint + 4 yeni consumer unit + 5 yeni Shared.Tests ItemRefundTrigger enum). Skinora.Transactions.Tests 424/424 → 445/445 (+21), Skinora.API.Tests 265/265 → 271/271 (+6), Skinora.Notifications.Tests 73/73 → 77/77 (+4), Skinora.Shared.Tests 182/182 → 187/187 (+5). Solution-paralel `dotnet test Skinora.sln` 6 modülde 17 fail (Docker container saturation Windows env'da; CI shared mssql + per-class DB izolasyonu T11.3 sorun değil) — isolated re-run ile 12 modül 100% PASS doğrulandı. PR pending. **Mimari kararlar (Notlar):** (1) `ItemRefundToSellerRequestedEvent.Trigger` enum'u `TimeoutPhase` → `ItemRefundTrigger` generalize: T49 callers + 3 test dosyası assertion mekanik update; alternatif duplicate cancel-event ayrı consumer/extra event her yeni cancel kaynağında çoğalır; tek event/tek consumer modeli korundu (Steam sidecar T64–T68 forward); (2) `paymentRefunded` her zaman false: 02 §7 nedeniyle T51 user-cancel post-payment kapalı, ödeme iadeli cancel yolları forward-devir; field 07 §7.7 contract'ında zorunlu — false ile reserved slot; (3) Atomic commit BeginTransactionAsync ile iki SaveChanges: `IReputationAggregator.RecomputeAsync` + `IUserCancelCooldownEvaluator.EvaluateAsync` AsNoTracking sorgularda in-flight cancel state'i göremez (T43'te canonical state read pattern), tek-tx commit ile state flip + recompute atomik — alternatif tracked-aware aggregator T43'te seçilmemişti; (4) Pre-cancel state capture (`previousStatus`/`itemWasOnPlatform`) `Fire`'dan önce: state machine OnEntry `CancelledAt` stamp'liyor, ITEM_ESCROWED → ItemRefundToSeller refund event `EscrowBotAssetId` snapshot ile yönetilir; (5) State machine `BuyerDecline` (TRADE_OFFER_SENT_TO_BUYER) permit ediyor ama T51 endpoint'i 02 §7 nedeniyle 422 ile reddediyor — T58 dispute / T59 admin akışları orchestrator'larında tüketilecek; (6) Cooldown evaluator yalnız caller-side: aggregator zaten responsibility map ile non-responsible counter-party'yi filtreler; (7) Türkçe bildirim metni inline literal — T97 i18n full coverage'da resx'e taşınacak (T49 timeout reason metinleriyle birlikte). **T51 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-03 (T50 ✓ PASS bağımsız validator — Timeout freeze/resume. 0 S-bulgu, 0 minor advisory; 6/6 kabul kriteri ✓ (4 reason senaryosu + freeze trio set + resume reschedule) + 2/2 doğrulama kontrol listesi ✓ (02 §3.3 4 freeze senaryosu + 05 §4.5 emergency hold mekanizması karşılıklı invariant); lokal Release `0W/0E` + format `--verify-no-changes` exit=0 + Skinora.Transactions.Tests 424/424 (T50 odak 21/21: 5 unit `TimeoutFreezeReasonScopesTests` + 16 integration `TimeoutFreezeServiceTests` Freeze/Resume/FreezeMany/ResumeMany sıkı senaryolar); task branch CI run [`25274389994`](https://github.com/turkerurganci/Skinora/actions/runs/25274389994) (HEAD `3143676`) 9/9 + Guard skipped ✓ + önceki `25274239015` 9/9 ✓ ardışık; main CI startup ardışık 3 ✓ (`25259885322`+`25259885329` chore #80 + `25259571348` T49 #79); doc uyumu tam (`TimeoutFreezeReason` enum 4 değer ↔ `TimeoutFreezeReasonScopes` map ↔ DB CK_Transactions_FreezeActive/Passive/Hold_Forward/Reverse trio ↔ `ITimeoutSchedulingService.ReschedulePaymentTimeoutAsync` API); 0 dış bağımlılık + 0 migration; mini güvenlik (secret yok, auth/authorization etkisi yok service-only katman, input validation defense-in-depth, time clamping `Math.Floor` + non-negative clamp) temiz. Sırada **T51** (İptal akışı; bağımlılık T44 ✓, T37 ✓; doc 02 §7 + 03 §2.5 + §3.3 + 07 §7.7). **T50 satırı `⏳ Devam ediyor` → `✓ Tamamlandı`.** 

**Önceki güncelleme:** 2026-05-03 (T50 ⏳ yapım bitti — Timeout freeze/resume. T44 state machine + T47 scheduling + T48/T49 side effect altyapısı sonrası bir transaction'ın geçici dondurulup kaldığı yerden devam ettirilmesi mekaniği bağlandı (02 §3.3, 05 §4.4–§4.5, 09 §13.3, 06 §3.5 + §8.1). Yeni `Skinora.Transactions/Application/Timeouts/`: `TimeoutFreezeReasonScopes` (public static helper — `MAINTENANCE`→8 aktif state, `STEAM_OUTAGE`→TRADE_OFFER_SENT_TO_SELLER+TRADE_OFFER_SENT_TO_BUYER, `BLOCKCHAIN_DEGRADATION`→ITEM_ESCROWED, `EMERGENCY_HOLD`→ArgumentException) + `ITimeoutFreezeService`/`TimeoutFreezeService` 4 method (FreezeAsync per-tx idempotent stamp+inline Hangfire delete, ResumeAsync per-tx newDeadline=now+TimeoutRemainingSeconds 06 §8.1 otoritesi+`ITimeoutSchedulingService.ReschedulePaymentTimeoutAsync` ITEM_ESCROWED için+freeze trio temizle, FreezeManyAsync bulk filter+tek SaveChanges, ResumeManyAsync simetrik). Internal helper'lar 06 §3.5 state→active deadline matrix'ini yansıtır (GetActiveDeadline/SetActiveDeadline/ComputeRemainingSeconds). DI: `TransactionsModule.cs` `ITimeoutFreezeService` Scoped 1 satır. Migration yok (CK_Transactions_FreezeActive/FreezePassive/FreezeHold_Forward/FreezeHold_Reverse T30'da kuruldu; freeze trio kolonları T19/T44'ten beri var). SystemSetting yok. Yeni dış bağımlılık yok. Caller wiring forward-devir: `FreezeAsync`/`ResumeAsync` → T59 emergency hold orchestrator (T44 state machine + T50 freeze service kompozisyonu); `FreezeManyAsync(MAINTENANCE)` → admin maintenance start endpoint (henüz 11'de explicit task yok — T63a P2 endpoint adjacent veya ayrı admin task); `STEAM_OUTAGE` → T64–T68 sidecar health check + admin manuel; `BLOCKCHAIN_DEGRADATION` → T73–T75 sidecar + admin manuel. MaintenanceWindow entity + P2 endpoint T63a sahipliğinde — T50 yalnız engine. Audit (`MAINTENANCE_MODE_CHANGED` 05 §4.4) caller orchestrator'ında (T59/admin task). Outbox events (RT1 EmergencyHoldApplied/Released, RT2 MaintenanceStatusChanged) caller'da. **6/6 plan kabul kriteri ✓** (4 reason senaryosu + freeze trio set + resume reschedule). **2/2 doğrulama listesi ✓** (02 §3.3 4 senaryo + 05 §4.5 emergency hold mekanizması). Lokal Release 0W/0E + format verify (`dotnet format --verify-no-changes` exit=0) + tüm 12 test assembly **1264/1264 PASS** (T49: 1243 → T50: 1264, +21 net = 5 unit TimeoutFreezeReasonScopes + 16 integration TimeoutFreezeService). Skinora.Transactions.Tests 403/403 → 424/424 (+21), regresyon yok diğer modüllerde (Users 16, Fraud 17, Payments 6, Disputes 11, Steam 21, Shared 182, Platform 136, API 265, Auth 93, Notifications 73, Admin 20). PR pending. **Mimari kararlar (Notlar):** (1) Resume otoritesi `TimeoutRemainingSeconds` (06 §8.1) — newDeadline=now+remaining (rewrite), oldDeadline+elapsed değil — single source of truth; (2) FreezeAsync inline Hangfire delete (T47 CancelTimeoutJobsAsync yerine) — bulk path 200 tx ekstra DB roundtrip atlamak için; (3) state→deadline 06 §3.5 matrix private helper olarak servisin içinde — non-payment phase'lerde de TimeoutRemainingSeconds doğru damgalanır (CK_Transactions_FreezeActive constraint'i NOT NULL gerektirir); (4) ITEM_DELIVERED + FLAGGED MAINTENANCE scope'unda yer alır ama active deadline'ı yok — TimeoutRemainingSeconds=0 set edilir, resume SetActiveDeadline no-op (matrix uyumlu); (5) FreezeAsync idempotent (zaten frozen ise stamp atlar, job cancel yine yürütür) — çift freeze callerları (örn. STEAM_OUTAGE üstüne MAINTENANCE) ilk reason'u korur. **T50 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 (T49 ✓ PASS bağımsız validator — Timeout execution. 0 S-bulgu, 1 minor advisory: yapım raporu "Test Sonuçları" satırı toplam **1237** yazıyordu; per-assembly breakdown (Users 16 + Fraud 17 + Payments 6 + Disputes 11 + Steam 21 + Shared 182 + Platform 136 + Transactions 403 + API 265 + Auth 93 + Notifications 73 + Admin 20) **1243**'e topluyor. Lokal `dotnet test -c Release --no-build` 1243/1243 PASS, fonksiyonel etki yok — validator inline düzeltti. 6/6 kabul kriteri (4 ✓ + 2 ~ kısmi: K#5 "doğru iade tetikleme" event publish ✓ + gerçek refund execution sidecar T64–T68/T73 forward; K#6 "gecikmeli ödeme izleme başlatma" event publish ✓ + gerçek monitor T75'te) + 2/2 doğrulama listesi ✓ (03 §4.1–§4.4 reason text verbatim eşleşiyor; 02 §3.2 iade kuralları publisher mapping'inde birebir). Lokal: Release `0W/0E`, format `--verify-no-changes` exit=0, 1243/1243 test PASS (12 assembly). Task branch CI run [`25258360678`](https://github.com/turkerurganci/Skinora/actions/runs/25258360678) (HEAD `393749c`) 9/9 + Guard skipped ✓ + önceki run [`25258220783`](https://github.com/turkerurganci/Skinora/actions/runs/25258220783) (`93cbfae`) ✓; main CI startup ardışık 3 ✓ (`25256755520`+`25256755518` T48 #78 + `25255400779` T47 #77). Atomicity sağlam (publisher change-tracker only + tek SaveChanges); 0 dış bağımlılık + 0 migration; PII (BuyerRefundAddress) outbox internal kanal — sidecar handler'ı T73/T75 forward. Sırada **T50** (Timeout freeze/resume; bağımlılık T47 ✓; doc 02 §3.3 + 05 §4.4–§4.5). **T49 satırı `⏳ Devam ediyor` → `✓ Tamamlandı`.** 

**Önceki güncelleme:** 2026-05-02 (T49 ⏳ yapım bitti — Timeout execution. T44'ün `Timeout` trigger'ı + T47'nin scheduling pipeline'ı sonrası bir Timeout tetiklenmesinin observable side effect'leri bağlandı. Yeni `Skinora.Shared/Enums/TimeoutPhase.cs` (Accept/TradeOfferToSeller/Payment/Delivery, 03 §4.1–§4.4 birebir) + 4 yeni outbox event (`TransactionTimedOutEvent` notification fan-out trigger her 4 phase için, `ItemRefundToSellerRequestedEvent` Steam sidecar T64–T68 forward Payment+Delivery, `PaymentRefundToBuyerRequestedEvent` Blockchain sidecar T73 forward Delivery, `LatePaymentMonitorRequestedEvent` Blockchain sidecar T75 forward Payment). Yeni `Skinora.Transactions/Application/Timeouts/ITimeoutSideEffectPublisher` + `TimeoutSideEffectPublisher` — tek noktada phase → event mapping (caller `previousStatus` geçer, publisher uygun event(ler)i outbox change-tracker'a enlist eder, atomicity boundary 09 §13.3). Mapping: CREATED→Accept (sadece notification), TRADE_OFFER_SENT_TO_SELLER→TradeOfferToSeller (sadece notification), ITEM_ESCROWED→Payment (notification + item refund + late payment monitor), TRADE_OFFER_SENT_TO_BUYER→Delivery (notification + item refund + payment refund). `TimeoutExecutor` + `DeadlineScannerJob` `previousStatus` capture → `Fire(Timeout)` → `_sideEffects.PublishAsync` → tek `SaveChangesAsync` ile state flip + outbox satırları atomik commit (T48 `WarningDispatcher` deseni birebir). Yeni `Skinora.Notifications/Application/EventHandlers/TransactionTimedOutNotificationConsumer` — `NotificationConsumerBase<TransactionTimedOutEvent>` türevi, `notifications.transaction-timed-out` consumer name, her event için iki ayrı `NotificationRequest` (seller + buyer if registered) phase × role'a göre Türkçe `Reason` parametresi (03 §4.1–§4.4 metinleri verbatim) — mevcut `TRANSACTION_CANCELLED` template (`{ItemName} işlemi iptal edildi: {Reason}.`) tekrar kullanıldı, yeni enum/resx eklenmedi. i18n full coverage T97 forward-deferred (Türkçe literal'ler taşınacak). Sidecar consumer'lar (Steam T64–T68, Blockchain T73/T75) F4'te wire — T49 publisher tarafını production-ready yapar, consumer T45/T46 events'leri (T62/T78–T80 forward) ile aynı pattern. DI: `TransactionsModule.cs` `ITimeoutSideEffectPublisher` Scoped kayıt. Migration yok, SystemSetting yok, yeni paket yok. **6/6 kabul kriteri ✓** (4 tam + 2 kısmi: kabul kriteri #5 "doğru iade tetikleme" event publish ✓ + gerçek refund execution sidecar T64–T68/T73/T75 forward devirli; #6 "gecikmeli ödeme izleme başlatma" event publish ✓ + gerçek monitor T75'te). Lokal Release 0W/0E + format verify (`dotnet format --verify-no-changes` exit=0) + tüm 11 test assembly 1237/1237 PASS (T48: 1224 → T49: 1237, +13 net = 12 yeni Transactions + 7 yeni Notifications + 1 enum count fix). Skinora.Transactions.Tests 391/391 → 403/403 (+12: 7 unit TimeoutSideEffectPublisher + 2 integration TimeoutExecutor SideEffects + 3 integration DeadlineScannerJob SideEffects), Skinora.Notifications.Tests 66/66 → 73/73 (+7: 4 unit TransactionTimedOutNotificationConsumer InlineData phase × role + 3 unit fan-out/skip/idempotency), Skinora.Shared.Tests EnumTests count assertion 24→25 (`TimeoutPhase` eklendi). Regresyon yok diğer modüllerde. PR pending. **T49 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 (T48 ✓ PASS bağımsız validator — Timeout warning. 0 S-bulgu, 1 minor advisory: warning scope ITEM_ESCROWED + buyer; 02 §3.4 metni "ilgili tarafa (alıcı veya satıcı)" generic ifade içerir ama 05 §4.4 partition'ı per-tx Hangfire delayed warning'i yalnız ödeme aşamasında schedule eder (Accept/TradeOfferToSeller/TradeOfferToBuyer deadline'ları `DeadlineScannerJob` ile poller-based, warning yok). Tasarım kararı T47'de sabitlendi, T48 korudu — fonksiyonel etki yok, "Known Limitations" disclosed. 3/3 kabul kriteri ✓ + 1/1 doğrulama listesi ✓; lokal Release 0W/0E + format `--verify-no-changes` exit=0 + 12 test assembly 1224/1224 PASS (Notifications 66/66, Transactions 391/391, API 265/265); task branch CI run [`25256430139`](https://github.com/turkerurganci/Skinora/actions/runs/25256430139) (HEAD `c3144a8`) 9/9 + Guard skipped ✓; main CI startup ardışık 3 ✓ (`25255400779`+`25255400763` T47 #77 + `25252671939` T46 #76); atomicity sağlam (PublishAsync change-tracker only + tek SaveChanges); 0 dış bağımlılık + 0 migration. Sırada T49 (Timeout execution; bağımlılık T47 ✓, T44 ✓; 02 §3.2 + 03 §4.1–§4.5 doc'ları). 

**Önceki güncelleme:** 2026-05-02 (T48 ⏳ yapım bitti — Timeout warning. T47'nin `IWarningDispatcher` portunda forward-deferred bırakılan `StubWarningDispatcher` gerçek implementasyona DI swap edildi. Yeni `Skinora.Transactions/Application/Timeouts/WarningDispatcher.cs`: 09 §13.3 no-op guard'ları (state ≠ ITEM_ESCROWED, IsOnHold, TimeoutFrozenAt, TimeoutWarningSentAt zaten set, BuyerId null, PaymentDeadline geçmiş → silent no-op), `Transaction.TimeoutWarningSentAt = UtcNow` damgalar + `TimeoutWarningEvent` outbox publish + tek `SaveChangesAsync` ile atomik commit. Yeni `Skinora.Shared/Events/TimeoutWarningEvent.cs` (RecipientUserId + ItemName + RemainingMinutes = `Math.Floor((PaymentDeadline − UtcNow).TotalMinutes)` + OccurredAt). Yeni `Skinora.Notifications/Application/EventHandlers/TimeoutWarningNotificationConsumer.cs` — `NotificationConsumerBase<TimeoutWarningEvent>` türevi (ConsumerName `notifications.timeout-warning`, idempotent replay), event'i tek `NotificationRequest{Type=TIMEOUT_WARNING, UserId=BuyerId, TransactionId, Parameters={ItemName, RemainingMinutes}}`'e çevirir; T37 `INotificationDispatcher` platform-in-app `Notification` row + her enabled `UserNotificationPreference` için `NotificationDelivery` row + `NotificationDeliveryJob` enqueue eder (kanal handler'ları stub, gerçek SDK çağrıları T78–T80 forward-devir). DI: `TransactionsModule.cs` `IWarningDispatcher` swap (`StubWarningDispatcher` → `WarningDispatcher`); `OutboxModule.GetMediatRScanAssemblies` `Skinora.Notifications.NotificationsModule.Assembly` ile genişletildi (T48 onwards consumer registration). `StubWarningDispatcher` sınıfı `TimeoutExecutor.cs`'den silindi; T47'nin stub-referanslı 2 testi `TimeoutExecutorTests.cs`'den kaldırıldı. Migration yok, SystemSetting yok, yeni paket yok. Türkçe template `TIMEOUT_WARNING_Title` = "Süre dolmak üzere" T37 resx'te zaten mevcut. **3/3 plan kabul kriteri ✓** (eşik admin oran T47'de hazır, TimeoutWarningEvent üretimi WarningDispatcher'da, kanal fan-out T37 dispatcher'ı + consumer chain). Lokal Release 0W/0E + format verify (`dotnet format --verify-no-changes` exit=0) + tüm 12 test assembly 1224/1224 PASS (T47: 1215 → T48: 1224, +9 net = 8 yeni Transactions integration WarningDispatcher + 3 yeni Notifications unit consumer − 2 silinen stub). Skinora.Transactions.Tests 391/391 (T47 385 → +6 net), Skinora.Notifications.Tests 66/66 (T47 63 → +3), regresyon yok diğer modüllerde. PR pending. **Mimari karar (Notlar):** Inline dispatch yerine outbox event + MediatR consumer pattern T37'nin recommended pattern'i (T45 TransactionCreatedEvent + T46 BuyerAcceptedEvent paralelinde) — replayed Hangfire job'da consumer-idempotency garantisi (`IProcessedEventStore`), 09 §9.3 atomicity + 05 §4.4 explicit `TimeoutWarningEvent` üretimi. **T48 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 (T47 ✓ PASS bağımsız validator — Timeout scheduling. 0 S-bulgu, 1 minor advisory (`S1-A1`): RestartRecoveryService 05 §4.4 line 536 "otomatik timeout uzatma işlemleri AuditLog'a kaydedilir" satırını ILogger ile karşılıyor ama `IAuditLogger` (T42 hazır altyapı) inject edilmemiş — AuditLog satırı atılmıyor; **T50** maintenance mode + freeze/resume task'ında aynı 05 §4.4 platform-genel downtime politikasını kapatırken toplu çözülebilir, fonksiyonel etki yok (observability mevcut). 6/6 kabul kriteri (5 tam + 1 ~ kısmi #1: caller wiring T49/T64–T68 forward-devir dürüst disclosed) + 2/2 doğrulama listesi ✓; lokal Release 0W/0E + format ✓ + tüm 12 test assembly 1215/1215 PASS; task branch CI run [`25254429578`](https://github.com/turkerurganci/Skinora/actions/runs/25254429578) (HEAD `612efc4`) ✓; main CI startup ardışık 3 ✓ (`25252671939` T46 + `25252671951` T46 + `25250478338` T45). Sırada T48 (Timeout warning; bağımlılık T47 ✓, T37 ✓ — `IWarningDispatcher` port DI swap'ı zaten hazır). 

**Önceki güncelleme:** 2026-05-02 (T47 ⏳ yapım bitti — Timeout scheduling. `Skinora.Transactions/Application/Timeouts/` 4 servis: `ITimeoutSchedulingService` + `TimeoutSchedulingService` (per-tx Hangfire delayed job orchestrator — ITEM_ESCROWED için PaymentTimeoutJobId + TimeoutWarningJobId schedule/cancel/reschedule, `timeout_warning_ratio` SystemSetting okuma, atomicity boundary 09 §13.3); `ITimeoutExecutor` + `TimeoutExecutor` (per-tx Hangfire job hedefi, 09 §13.3 no-op pattern: Status/IsOnHold/TimeoutFrozenAt/Deadline guard, `TransactionStateMachine.Fire(Timeout)`); `IDeadlineScannerJob` + `DeadlineScannerJob` (self-rescheduling Hangfire job 09 §13.4 — 4 phase tek query: AcceptDeadline/TradeOfferToSellerDeadline/TradeOfferToBuyerDeadline + PaymentDeadline belt-and-suspenders; IsOnHold + TimeoutFrozenAt SQL seviyesinde elenir); `IWarningDispatcher` + `StubWarningDispatcher` (T48 forward-devir stub — TimeoutWarningSentAt stamp + log); `TimeoutSchedulingOptions` (Timeouts: appsettings — DeadlineScannerIntervalSeconds=30/BatchSize=200/RecoveryThresholdSeconds=60, OutboxOptions/HangfireOptions deseni, SystemSetting değil). `Skinora.Platform/Application/Heartbeat/`: `IHeartbeatJob` + `HeartbeatJob` (self-rescheduling 30s SystemHeartbeats.LastHeartbeat update); `HeartbeatOptions` appsettings. `Skinora.API/BackgroundJobs/Timeouts/`: `IRestartRecoveryService` + `RestartRecoveryService` (startup outage = UtcNow - LastHeartbeat; threshold üstü → 4 phase deadline + ITEM_ESCROWED Hangfire job delete-and-reissue; idempotent); `TimeoutSchedulerStartupHook : IHostedService` (OutboxStartupHook deseni — recovery + heartbeat + scanner prime; failure tolerance). DI: `TransactionsModule.cs` IConfiguration overload + 4 yeni Scoped + `TimeoutSchedulingOptions` bind; `PlatformModule.cs` IHeartbeatJob; `Program.cs` HeartbeatOptions bind + IRestartRecoveryService + AddHostedService<TimeoutSchedulerStartupHook>. Migration yok — entity field'ları T19/T44'ten beri var, SystemSetting yok (operasyonel tuning appsettings). **6/6 kabul kriteri ✓** (5 tam + 1 kısmi: kabul kriteri #1 "Her state geçişinde timeout schedule" — service hazır, OnEntry caller wiring T49+ caller'larda, T44 plan kabul kriteri #5 ile aynı pattern). Lokal Release 0W/0E + format verify (`dotnet format --verify-no-changes` exit=0) + 12 test assembly 1215/1215 PASS (T46: 1186 → T47: 1215, +29: 8 schedule + 7 executor + 7 scanner = 22 Transactions, 3 Platform Heartbeat, 4 API RestartRecovery). Skinora.Transactions.Tests 385/385 (T46 363 → +22), Skinora.Platform.Tests 136/136 (T46 133 → +3), Skinora.API.Tests 265/265 (T46 261 → +4), regresyon yok diğer modüllerde. PR pending. **CK_Transactions_FreezePassive dersi**: ReschedulePaymentTimeoutAsync `TimeoutRemainingSeconds`'a dokunmuyor — freeze/resume yaşam döngüsü T50'nin sorumluluğu (set during freeze, clear on resume); restart recovery yalnız deadline absolute bump yapar. **T47 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 (T46 ⏳ yapım bitti — Alıcı kabul akışı. `Skinora.Transactions/Application/Lifecycle/` paketine 2 servis: `ITransactionAcceptanceService` + `TransactionAcceptanceService` (07 §7.6 + 03 §3.2 pipeline — request validation → state guard → party guard Yöntem 1/2 → T34 wallet pipeline → refund-address cooldown → state machine `BuyerAccept` → `User.RefundAddressChangedAt` snapshot → `BuyerAcceptedEvent` outbox publish atomik) ve `ITransactionDetailService` + `TransactionDetailService` (07 §7.5 — public/authenticated branching, role resolution incl. "target buyer Steam ID match pre-acceptance" 03 §3.2 step 1, EMERGENCY_HOLD action freeze, reputation read-path 06 §3.1 ToZero). `Skinora.Shared/Events/BuyerAcceptedEvent.cs` (T62/T78–T80 consumer forward-devir). `TransactionDetailDto.cs` 11 sub-record (07 §7.5 tüm bölümler `WhenWritingNull` suppress; T46-erişilebilir alanlar buyer/timeout/inviteInfo/flagInfo/holdInfo/availableActions doldurulur, gerisi T47/T49/T51/T54/T58/T70+ forward-devir). 7 yeni hata kodu (`TRANSACTION_NOT_FOUND`, `NOT_A_PARTY`, `STEAM_ID_MISMATCH`, `ALREADY_ACCEPTED`, `INVALID_STATE_TRANSITION`, `WALLET_CHANGE_COOLDOWN_ACTIVE`, `REFUND_ADDRESS_REQUIRED`). `TransactionsController.cs` 2 endpoint (`GET /transactions/{id:guid}` `AllowAnonymous` `RateLimit("public")`; `POST /transactions/{id:guid}/accept` `Authorize(Authenticated)` `RateLimit("user-write")`). `TransactionsModule.cs` 2 yeni Scoped DI kaydı. Migration yok (T34 seed `wallet.refund_address_cooldown_hours` 24h default). T34 minor "cooldown enforcement T45/T46" devri kapatıldı. **8/8 kabul kriteri ✓** (7 tam + 1 kısmi: bildirim fan-out outbox event ile, mesaj content T62/T78–T80 forward-devir — T45 pattern). Lokal Release 0W/0E + format verify (`dotnet format --verify-no-changes` exit=0) + tüm 12 test assembly 1186/1186 PASS (T45: 1146 → T46: 1186, +40 yeni: 5 unit + 12 integration Acceptance + 10 integration Detail + 8 endpoint, +5 misc test count drift); Skinora.Transactions.Tests 363/363 (T45 331 → +32), Skinora.API.Tests 261/261 (T45 253 → +8), regresyon yok diğer modüllerde. PR pending. **T46 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 (T45 ✓ PASS bağımsız validator — İşlem oluşturma akışı. 0 S-bulgu, 3 minor advisory: M-1 `BUYER_STEAM_ID_NOT_FOUND` enum'da var ama trigger eden yer yok — T67 Steam profile lookup forward-devir; M-2 OPEN_LINK için `/invite/{token}` URL path 07 §7.2 sample'da yer almıyor (security gerekçesiyle implementation doğru karar — 06 §3.5 InviteToken opaque tek kullanımlık, sonraki 07 doc-pass'te §7.2'ye OPEN_LINK ek sample); M-3 yapım raporu kabul kriterleri tablosu 11'e bölünmüş, plan 11 §T45'te 10 madde — sayım drift, fonksiyonel etki yok. 10/10 plan kabul kriteri ✓ (10 tam + 1 kısmi #10 bildirim fan-out outbox event ile, mesaj content T62 SignalR + T78–T80 Email/Telegram/Discord forward-devir plan-aware split) + 3/3 doğrulama listesi ✓; lokal Release 0W/0E + format ✓ + tüm 12 test assembly 1146/1146 PASS (yeni T45: 6 unit + 17 integration Transactions + 5 integration Fraud + 6 API endpoint = 34); task branch CI run [`25249321302`](https://github.com/turkerurganci/Skinora/actions/runs/25249321302) (HEAD `032a4442`) 9/9 + Guard skipped ✓; main CI startup ardışık 3 ✓ (`25248332299`+`25248332298` T44 #74 + `25244910446` chore F2 #73). Sırada T46 (Alıcı kabul akışı; bağımlılık T44 ✓, T34 ✓). 

**Önceki güncelleme:** 2026-05-02 (T45 ⏳ yapım bitti — İşlem oluşturma akışı. `Skinora.Transactions/Application/Lifecycle/` paketi (eligibility + params + creation + fraud pre-check + invitation code + limits provider + DTOs + error codes), `Skinora.Transactions/Application/Steam/ISteamInventoryReader` + `StubSteamInventoryReader` (T67 forward-devir), `Skinora.Transactions/Application/Pricing/IMarketPriceProvider` + `NullMarketPriceProvider` (T81 forward-devir), `Skinora.Fraud/Application/Account/AccountFlagChecker` (port impl), `TransactionsController` 3 endpoint (`GET /transactions/eligibility`, `GET /transactions/params`, `POST /transactions`), `Skinora.API/Configuration/TransactionsModule.AddTransactionsModule()` DI extension. `Skinora.Transactions.csproj` → `Skinora.Platform` project reference eklendi (SystemSetting entity erişimi). `Program.cs` `AddJsonOptions(JsonStringEnumConverter)` global eklendi (07 sözleşme enum'ları string olarak gösteriyor; pre-T45 enum field'lı response yoktu, regresyon ✓ teyit). Migration yok — T26 seed'inde tüm 12 SystemSetting key zaten mevcut. `TransactionCreatedEvent` outbox event publish atomik SaveChanges; consumer T62/T78–T80 wire-up sonrası bildirim fan-out çalışacak. AcceptDeadline = UtcNow + accept_timeout_minutes (CREATED) / NULL (FLAGGED — 06 §3.5 + 03 §7 invariant). InviteToken yalnız OPEN_LINK'te (CK_Transactions_BuyerMethod_SteamId schema constraint). Commission `Math.Round(price × rate, 6, MidpointRounding.ToZero)` 02 §5 + 06 §8.3 truncation/scale-6. **11/11 kabul kriteri ✓** (10 tam + 1 kısmi: bildirim fan-out outbox event ile tetiklenir, mesaj content T78+ consumer'larda). Lokal Release 0W/0E + format ✓ + 1146 test PASS (yeni T45: 6 unit + 17 integration Transactions + 5 integration Fraud + 6 API endpoint = 34); Skinora.Transactions.Tests 331/331 (T44 308 → +23), Skinora.API.Tests 253/253 (T44 247 → +6), Skinora.Fraud.Tests 17/17 (T44 12 → +5), regresyon yok diğer modüllerde. PR pending. **T45 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 (T44 ✓ PASS bağımsız validator — Transaction State Machine. 0 S-bulgu, 1 minor advisory: kabul kriteri #5 "OnEntry/OnExit side effect handler'ları (bildirim, timeout başlatma)" yapım raporunda **~ Kısmi** olarak rating'lendi ve doğrulamada onaylandı — OnEntry/OnExit hook mekanizması ✓ aktif (6 state'te domain primitive seviyesinde — AcceptedAt/ItemEscrowedAt/PaymentReceivedAt/ItemDeliveredAt/CompletedAt/CancelledAt + ITEM_ESCROWED warning reset/clear), Hangfire schedule + notification publish + outbox event 09 §9.2 "side effect üretmez" normatif kuralı gereği T47/T62/T45+ caller'larına forward-devir; Known Limitations'da explicit kayıt. 7/7 kabul kriteri ✓ (6 tam + 1 kısmi) + 4/4 doğrulama listesi ✓; lokal Release 0W/0E + 226/226 TransactionStateMachineTests + 308/308 Skinora.Transactions.Tests + 172/172 Skinora.Shared.Tests + 247/247 Skinora.API.Tests; task branch CI run 25247354271 (HEAD `7b40127`) 10/10 ✓ + 25247166397 (`e8ae6e5`) 9/9 ✓ ardışık (initial run 25247023054 FAIL → aynı PR'da `e8ae6e5` fix-self-pushing, BYPASS_LOG ci-failure Layer 2 entry); main CI startup ardışık 3 ✓ (25244910446 + 25244910443 + 25235250426). Sırada T45 (İşlem oluşturma akışı; bağımlılık T44 ✓, T34 ✓, T43 ✓). 

**Önceki güncelleme:** 2026-05-02 (T44 ⏳ yapım bitti — Transaction State Machine. `TransactionTrigger` enum (15 değer, 05 §4.2 tablosu) + `Skinora.Transactions/Domain/StateMachine/` paketi (`TransactionStateMachine` Stateless 5.20.1 wrapper + `CancellationContext` record). 13 durum × 30 geçerli geçiş 05 §4.2 tablosuyla 1:1 deklaratif; geçersiz geçişler `DomainException(InvalidTransitionErrorCode)` (Stateless `InvalidOperationException` sızdırılmaz, `CanFire` ön-kontrol). Guard'lar: RowVersion (constructor opsiyonel `expectedRowVersion`, `Fire`/hold API başında), IsOnHold (her `Fire` reddi 05 §4.5), caller-set field matrisi (`HasFieldsForAccepted`/`HasFieldsForItemEscrowed`/`HasFieldsForItemDelivered`), FLAGGED invariantı (`HasFlaggedStateInvariant` — 4 deadline + 2 job ID NULL). OnEntry/OnExit handler'ları **yalnız domain primitive** (AcceptedAt/ItemEscrowedAt/PaymentReceivedAt/ItemDeliveredAt/CompletedAt/CancelledAt + ITEM_ESCROWED warning reset/clear) — Hangfire schedule/notification/outbox event publish T47/T62/T45+ caller'lara forward-devir (09 §9.2 "side effect üretmez" kuralına uyum). Emergency hold API (`ApplyEmergencyHold(adminId, reason)` + `ReleaseEmergencyHold()`) 05 §4.5 + 06 §3.5 karşılıklı invariantları (`IsOnHold ↔ TimeoutFreezeReason=EMERGENCY_HOLD ↔ TimeoutFrozenAt NOT NULL`); ITEM_ESCROWED'da `PaymentDeadline` mevcutsa `TimeoutRemainingSeconds` `Math.Floor` ile doldurulur, diğer state'lerde NULL (T47 poller-based). Yeni dep: `Stateless 5.20.1` (Apache-2.0, NuGet `net9.0` explicit). Migration yok. Lokal: Release 0W/0E, format ✓, Skinora.Transactions.Tests 308/308 (226 yeni Unit.StateMachine + 82 Integration regresyon temiz), Skinora.API.Tests 247/247 regresyon temiz. PR pending. **T44 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-02 — **F2 Gate Check ✓ PASS** — 870 test passed (F1: 462 → F2: 870, +408 test), migration rehearsal 5 migration zincir + 34 SystemSettings + idempotent, backend Release 0W/0E + frontend/sidecar lint+build temiz, CI run 25235250432 10/10 ✓, traceability §7.2 + §7.3 F2 grubu 0 boşluk, 0 kritik güvenlik bulgusu, tag `phase/F2-pass` push'lanacak. Rapor: [`Docs/CHECKPOINT_REPORTS/GATE_CHECK_F2.md`](CHECKPOINT_REPORTS/GATE_CHECK_F2.md). Sırada F3 (İş Mantığı, T44 Transaction State Machine ilk task). 

**Önceki güncelleme:** 2026-05-02 — T43 ✓ PASS bağımsız validator — User itibar skoru hesaplama. 0 S-bulgu, 1 minor advisory: rapor §Test Sonuçları "Unit (Reputation) 17/17" yazıyordu; gerçek `[Theory]` + `[Fact]` sayımı 16 (ReputationScoreCalculator 4+2 InlineData + 3 Fact + WashTradingFilter 7 Fact = 16). Fonksiyonel etki yok, T37 (PR #62 chore) tarzında rapor sayı drift'i; validator inline düzeltti. 7/7 kabul kriteri ✓ + 5/5 doğrulama listesi ✓; lokal Release 0W/0E + Unit Reputation 16/16 + UserProfileEndpoint 6/6 + Platform.Tests SystemSettings 54/54; task branch CI run 25234389032 (HEAD `0deee0f`) 9/9 + 25234165174 (`7bc3130`) 9/9 ✓ ardışık; main CI startup 25232475624/615 + 25230739077 ✓. **F2 fazının son task'ı tamamlandı — F2 Gate Check tetiklenebilir.** 

**Önceki güncelleme:** 2026-05-02 yapım bitti — User itibar skoru hesaplama. `Skinora.Users.Application.Reputation/` paketi (`IReputationScoreCalculator` + `ReputationScoreCalculator` composite skor read path, 06 §3.1 `ROUND(SuccessfulTransactionRate × 5, 1)` `MidpointRounding.ToZero`); `IReputationThresholdsProvider` (port Users) + `ReputationThresholdsProvider` (impl Platform — `reputation.min_account_age_days` + `reputation.min_completed_transactions` SystemSetting reader); `IReputationAggregator` (port Users) + `ReputationAggregator` (impl Transactions — denormalized `User.CompletedTransactionCount` + `User.SuccessfulTransactionRate` recompute; sorumluluk haritalama 06 §3.1 + wash trading filter 02 §14.1 denominator+numerator); `WashTradingFilter` (pure helper, 30-gün pencere, unordered pair); `IUserCancelCooldownEvaluator` + `CancelCooldownEvaluator` (cooldown stamping); `ICancelCooldownThresholdsProvider` + impl. SystemSettings 32→34 (`Reputation` kategori, default 30/3) — migration `T43_AddReputationThresholds` (yalnız 2 `InsertData`). `UserProfileService` 3 endpoint composite skor + `cancelRate = 1 - rate` döner — T33 forward devri kapandı. `UserProfileDtos.cs` xmldoc güncellendi. `UsersModule` 5 yeni DI kaydı. Lokal Release 0W/0E + format verify temiz; tüm 11 test assembly yeşil — yeni testler: ReputationScoreCalculatorTests 10 + WashTradingFilterTests 7 (unit) + ReputationAggregatorTests 9 + CancelCooldownEvaluatorTests 5 (integration, MsSqlContainer) + UserProfileEndpointTests +1 (below-threshold). PR pending. **T43 satırı `⬚ Bekliyor` → `⏳ Devam ediyor`.** 

**Önceki güncelleme:** 2026-05-01 — T43 BLOCKED → **çözüldü (Karar A)** — proje sahibi composite `reputationScore` formülünü onayladı: `ROUND(SuccessfulTransactionRate × 5, 1)`, eşikler `reputation.min_account_age_days` (default 30) + `reputation.min_completed_transactions` (default 3); eşik altı veya rate=null → skor `null` ("Yeni kullanıcı"). Read path'te runtime hesaplama (denormalized değil) — eşik runtime ayarlanabilir. M2 doc-pass uygulandı: 02 §13 formül + ölçek + eşikler eklendi, 06 §3.1 composite formül + örnek tablo (7 senaryo) + read-path notu + yuvarlama kuralı (`ToZero`) eklendi, 11 §T43 7. kabul kriteri (composite formül + 2 yeni SystemSetting) + 2 yeni doğrulama kontrol listesi maddesi + 06 §3.1 referansı eklendi. **T43 satırı `⛔ BLOCKED (SPEC_GAP)` → `⬚ Bekliyor`** geri çevrildi; M2 Açık Bulgular tablosundan **Kapatılanlar** bölümüne taşındı. T43 implementasyon yeni yapım chat'inde başlatılabilir (skill `/task T43` — bağımlılık T18 ✓ + T19 ✓ + spec hazır). Bu güncelleme aynı PR #71'in M2 doc-pass commit'leridir (BLOCKED kayıt + karar + doc-pass tek PR'da bundled — bundled-PR yasağının istisnası: aynı T43 numarasının BLOCKED-resolution akışı). 

**Önceki güncelleme:** 2026-05-01 — T43 ⛔ BLOCKED (SPEC_GAP) açıldı. 02 §13 yalnızca girdileri sayar, 06 §3.1 sadece bileşen `SuccessfulTransactionRate` formülünü verir, 07 §5.1/§5.2/§5.5/§6.x kontratları `"reputationScore": 4.8` emit eder ama backend hangi formülle ürettiği yazılı değildir; üstüne T43 plan kabul kriterleri composite skoru explicit talep etmiyor (kontrat 07 ile plan 11 arasında drift). T33 raporu (2026-04-23) `reputationScore: null` döndürerek "T43 forward devir" notu bıraktı; tahmini formülle T44+ caller'larca persist'e bağlanırsa formül sonradan değişince retroactive recompute borcu doğar. Çözüm önerileri: (A) 02/06'ya formül ekle + T43 plan kabul kriterlerini güncelle + implement (önerim — kontrat-uyumlu en az kırılma), (B) `reputationScore` 07'den kaldır (UI computes), (C) backend ham girdi verir, frontend hesaplar. **Açık Bulgular** tablosuna M2 olarak kaydedildi; F2 Gate Check tetiklenmeden karar zorunlu. Kullanıcı "Tam C" yolunu seçti: plan kapsamındaki maddeler (count + rate + age + cancel + wash + cooldown) implement edilebilse bile kontrat boşluğu kapanmadan T43'e dokunulmuyor; alternatif "C-light" (plan kapsamına daralt + M2 olarak bayrakla) önerildi ama kabul edilmedi. Rapor: [`Docs/TASK_REPORTS/T43_REPORT.md`](TASK_REPORTS/T43_REPORT.md). 

**Önceki güncelleme:** 2026-05-01 — T42 ✓ PASS bağımsız validator — AuditLog merkezi servisi. 0 S-bulgu, 1 minor advisory: architecture-test ile mekanik "doğrudan INSERT yasağı" enforcement T63b/T106'ya forward-devir (production grep clean — rule-level + xmldoc rehberliği yeterli, NetArchTest/Roslyn analyzer kuralı T63b retention/cleanup task'larıyla beraber eklenecek). 6/6 kabul kriteri ✓ + 2/2 doğrulama listesi ✓; lokal Release 0W/0E + Platform.Tests 133/133 + AdminAuditLogEndpointTests 7/7 + tüm solution PASS (regresyon yok); task branch CI run 25228269536 (HEAD `b929ed8`) ✓ + 25228027892 (`b592a02`) ✓; main CI startup 25226007307/297 + 25223615598 ✓ ardışık. 

**Önceki güncelleme:** 2026-05-01 yapım bitti — AuditLog merkezi servisi. `Skinora.Platform/Application/Audit/` paketi: `IAuditLogger`/`AuditLogger` (06 §8.6a aktör invariantını zorlar — SYSTEM↔SystemUserId zorunlu, ADMIN/USER Empty/SYSTEM Guid yasak; UoW disiplini — caller SaveChanges'ı yapar), `AuditLogCategoryMap` (12 AuditAction → 3 API kategorisi), `AuditLogQueryService` (paged listeleme + 5 filter + actor/subject single-round-trip hidrasyon + JSON detail surface), DTO + interface + entry record. `AdminController.ListAuditLogs` AD18 (`Permission:VIEW_AUDIT_LOG` + admin-read rate-limit). `SystemSettingsService` doğrudan `Set<AuditLog>().Add` yerine `IAuditLogger.LogAsync` çağırıyor (T41 forward-devir kapatıldı). Migration yok, yeni paket yok. Lokal Release 0W/0E + format ✓ + Platform.Tests 133/133 + API.Tests 246/246 (yeni: AuditLogCategoryMapTests 19, AuditLoggerTests 8, AuditLogQueryServiceTests 12, AdminAuditLogEndpointTests 7). PR pending.)
